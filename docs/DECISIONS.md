# EduSphere — decisions, phase 1 plan, compliance, costs

Approved by Mikey on 2026-09-04 unless marked "proposed".

## Decisions
- Schools remain the core plan. (Considered a pure direct-to-families/home-supplement pivot on 2026-09-05 — weaker core promise since a home tool adds practice time rather than reclaiming school-day time, plus a crowded market with free incumbents like Khan Academy — and decided against pivoting away from schools.)
- Two consent paths, both in scope, sharing the same product: institutional (schools and formally-organized microschools, consenting under COPPA's school exception) and direct-family (homeschool families, via standard parental consent — a parent creates the account, verified the usual way with a payment card). No new build for either; only the signup/consent flow differs.
- Marketing focus is deliberately narrow for now: microschools and homeschool families, because the mastery/reclaim-the-day philosophy is the actual differentiator for them. Marketing to traditional-school families who want supplemental practice on top of normal school is deferred, not rejected — that segment buys on content breadth and price against free incumbents, not philosophy, and the library isn't broad enough yet to win that fight. Self-serve signup isn't blocked to that audience if they find it on their own; the decision is not to spend marketing effort chasing them yet. Revisit once the coverage map is wider and the aligned segments have produced case studies.
- Branching into specialized trades and corporate training stays a goal either way. Already supported structurally: `subject` and `audience` on each course in `logic.mjs` are plain strings, not a K-12-specific enum, exactly so a course can later be "Electrical safety" for "New hires" as easily as "Math" for "Grade 3." No rework needed to add a vertical — just new courses.
- Texas first, so standards alignment is TEKS first; Common Core codes are kept as secondary tags.
- Rostering and sign-in through what schools run (Clever, ClassLink, Google Workspace for Education) are phase 1 requirements, not extras.
- Student identifier = school-issued ID created by an admin or imported from rostering. Students pick from a class list and never type an ID. Admins can edit, rename, deactivate, and merge (a merge is one event that re-tags two histories; nothing is deleted).
- Content: white-glove first (we build early customers' modules), then tiers — Standard (our library + course switches), Plus (AI-assisted upload with admin approval), Enterprise (white-glove onboarding, custom content, SSO, scheduled reports).
- Uploads never design a sequence from scratch: customers place material into a standards-aligned skeleton per grade that is already ordered foundational → advanced; the pedagogy check flags out-of-order material.
- No public ledger (Hedera or other) in the core product. Nothing about a child ever goes on an immutable public chain. Revisit only for anchoring completed credentials, if a customer asks; signed verifiable credentials come first.
- Coverage is proven, not assumed: a coverage map per grade (every standard → modules) with a test that fails while any standard has no module, and a pilot measured against an outside assessment.
- "Wonder" reflection block: open questions with no right answer; typed for readers, pick-a-picture or tell-an-adult for younger children; never graded, never gating; followed by four respectful voices (scientist, artist, grandparent of faith, skeptic) with none declared right, ending on an open question; every prompt and perspective human-approved before publication; must never imply religion or non-belief is wrong.
- Minutes per day is a first-class metric (the efficiency claim). Fun through variety, pictures, and stories — no streak pressure on young children, no dark patterns.
- Design: dark theme as a toggle (default on for educators, light by default for young learners), a modern readable sans (Lexend or Atkinson Hyperlegible), playfulness through color and illustration, no licensed characters. Done after foundations.
- The confidence score is a printed rubric tuned from data, not a copy of any other product's formula.

## Phase 1 plan (needs Mikey's approval before any service is purchased)
- Stack: Python + FastAPI, PostgreSQL, React. Managed Postgres with row-level security so an educator can only read their own students' rows — enforced by the database, not app code.
- Data: `schools`, `classes`, `learners` (ID only), `enrollments`, `courses/modules/questions`, one append-only `events` table (app role: INSERT and SELECT only). Reports are SQL views over `events`; each view's definition is printed on the report; scheduled reports run the same view on a timer and deliver CSV/email.
- Sign-in: class code + pick your ID (+ PIN for older grades); district SSO. No child emails or passwords held by us.
- Backups: the managed database's automated daily backups and point-in-time recovery. No spreadsheets.
- The educator sign-in screen needs a "Forgot password" link once real accounts exist. In the prototype there is only a shared PIN, so there is nothing to recover yet.
- Sign-in fail-safes (raised 2026-09-05, both impossible in the single-device prototype and both real once the app is centralized):
  - Two students signing in as the same person at once: the later sign-in takes over and the earlier session is ended. Both sessions are written to the event log, so an educator can see it happened and when. Whether that also raises an alert is a decision for the pilot, once we know how often it actually occurs.
  - A student picking the wrong name from the list: three protections, in order of cost. The name is shown prominently on the very next screen, so a mistake is visible within seconds. Backing out to the list costs nothing and records no progress against the wrong student. On shared devices, each student gets a short PIN or a picture password, which is how schools already handle this. If work does land on the wrong record, merging is already built and nothing is lost.
- Review scheduling: spaced review across days/weeks using the confidence score (the "reroute by confidence" behavior).
- Voice: device voice now; a paid text-to-speech service is a phase 1 choice (only question text is sent).
- Phase 2 content pipeline (a graph, not a line): Intake → Structurer (modules, order, standards) → checkers: facts (every claim cited), age-appropriateness, coverage (5+ question types, by code) → pedagogy/order check → human lead-admin approval → publish. Any checker can send the draft back with notes. Every step logged. No agent states a fact it cannot cite; practice is computable or sourced. Teacher-suggested edits enter the same flow.

## Compliance rules (every decision is made with these in mind)
- COPPA: schools consent under the school exception; we collect the minimum; parents and schools can request export and deletion; retention is limited to the agreed period.
- FERPA: we act as a school official under a data-processing agreement; data is used only for the school's purposes; never re-shared.
- State laws: Texas student-privacy law and each customer state's equivalent; sign the district's standard data privacy agreement.
- Collected: school-issued ID, class, and events. Never: names, emails, photos, audio, free text from children, ads, trackers. Read-aloud is output only; there is no microphone.
- Before real students: privacy policy, DPA template, parental/school consent flow, review by an education-privacy attorney. Later: a COPPA Safe Harbor program.
- Accessibility: public schools are held to it — readable type, contrast, screen-reader support, captions for any audio.

## When money starts (rough, changeable)
- Phase 0: $0.
- Phase 1 pilot: managed database + small backend host $15–50/month; domain ~$15/year; text-to-speech pennies per thousand characters; content-pipeline AI calls cents to a few dollars per uploaded document.
- Non-software: privacy attorney review (hundreds to a couple thousand dollars); school-required liability insurance; much later a formal security audit (tens of thousands) when a customer requires it.

## Working method (from the Claude Code best-practices guide, adopted)
- CLAUDE.md stays short: commands, rules, gotchas. Detailed how-tos live in `.claude/skills/` and are loaded on demand.
- Every change ends with `./check.sh`; a Stop hook in `.claude/settings.json` runs it automatically (verify the hook with `/hooks` on first use).
- Explore → plan → implement → check for anything larger than a one-sentence diff.
- Larger changes get a fresh-context review by `.claude/agents/reviewer.md`, gaps only.
- `/clear` between unrelated tasks; after two failed corrections, start a clean session with a better prompt.

## Built on 2026-09-04 (free improvements)
- Browser click-through test found and fixed a real bug: the educator's learner list did not refresh during a session.
- Reading course (Kindergarten, Letters): letter names, order, first letters, counting letters, big/small matching — all computable, read aloud, tap only.
- Wonder block: one typed question after fractions mastery, one pick question for kindergarten courses, four voices, closing question; words never stored; counted as reflections on the educator report.

- Transcript: a printable record of every course a student has worked on, kept whether or not the course is still switched on. Courses never opened are left out, because an untouched course says nothing about a student. It carries the school-issued identifier, the modules, the dates and the confidence figures, and nothing a student wrote. That keeps it inside what a school may share under FERPA and what we may hold under COPPA.
- Mastery is defined per module rather than platform-wide. Five questions suits counting to ten; a twelfth grade physics module may want three longer ones. Courses and modules can override both the number asked and the number needed.

- Life skills are suggestions for educators rather than content for students. Nothing is graded, tracked, or shown to a child, which keeps the section outside the student record entirely.
- They are listed as one sequence from earliest to most mature rather than assigned to grades. Grade assignment implies a precision nobody has, and a mixed age classroom has no single grade to look under. The sequence is an order, not a schedule. This also removes most of the reason an administrator would need to edit the list, so editing is not built for now.
- The screen shades each skill from the palest green at the top to deep forest green at the bottom, so the sequence is visible before a word is read. Four stage markers (Early, Growing up, Teen, Nearly grown) break up a long column without splitting the sequence.
- Educators can mark a skill as covered. This belongs to the classroom, never to a student, so no soft judgment about any child enters a record a parent could request. Covered skills are ticked and muted in place rather than moved, with a Hide toggle for when the list gets long. The word is "covered" rather than "complete", because most of these are never really finished.

- Reflection questions require approval by the school before any student sees one. This is the most sensitive content in the platform, so approval is opt in rather than opt out: an unreviewed question simply never appears. An educator reads the prompt, all four voices and the closing question, then approves, removes, or leaves it. Approval can be withdrawn at any time. This is also a selling point worth stating plainly to schools, since no reflection content reaches a child that somebody there has not read.
- There is nothing for an educator to review in a student's own reflection answers, because those are never saved.

- Onboarding asks for a rough starting level (Early years, Elementary, Middle school, High school and beyond) so the placement check begins near the child and an older student never sees pre-K questions. It is a starting point the check adjusts, not a label.
- Only the early years are asked for a sign-in picture, at the moment of adding, and it can be added or changed later from the roster. Pictures are drawn in the app from a fixed set of eight; photo upload is deliberately not offered.
- Courses containing a module that needs a touch screen carry a warning where the educator assigns them.
- For pre-reader courses, finished modules fold behind one star row so the next thing to do is seen first.

- Sign-in pictures are twelve drawn animals on six background colours, seventy-two in all, so a large school never needs two children to share one. Adding a young student suggests an unused picture automatically, and a duplicate is flagged rather than forbidden.
- Operations are written down in docs/RUNBOOK.md: managed services for everything in phase 1, a developer on retainer, a status page, and a downtime procedure that starts with rollback.

- Development in phase 1 is done through Claude Code against this repository, with `./check.sh` as the gate, rather than a developer on retainer. One human (Mikey) holds the production credentials and the three buttons: roll back, restore a backup, post to the status page. A freelancer's name is kept on file for hands-on help by the half day.
- Phase 1 hosting: Supabase for the database and sign-in (managed Postgres, automatic backups, point-in-time restore, a readable dashboard, a signable data processing agreement) and Vercel for the app (deploy from the repository, one-click rollback). Chosen for status pages and rollback buttons a non-technical owner can use.

- Local-first, decided 2026-09-10 in answer to Mikey's question. The record of what happened is the whole truth, so a file holding every record is a complete backup, and restoring it recreates the classroom on any device. This removes the database, the accounts, the hosting bill and, most importantly, any child's record on a server we would have to protect. The cost is a habit: someone presses Download backup, and the app nags after seven days. Restoring merges by student id and joins event logs, so it can only ever add history. A server is deferred until a customer genuinely needs cross-device sync, which is a large-school problem, not a microschool one. Automatic emailing of reports is not possible without a server; a downloaded file the educator emails to themselves is the equivalent.

- Backup cannot email itself without a server. On a phone or iPad the Share button opens the device's own sheet, where Mail is one tap away; elsewhere it downloads. File names carry the device name, the student count and the date (edusphere-ipad-3-12-students-2026-09-10.json) so a folder from ten devices reads at a glance. A touchless auto-save to a chosen file is possible in Chrome and on Chromebooks and is a next step; iPads do not support it.
- Educators stay signed in while active. After twenty quiet seconds a warning appears; after thirty the PIN is asked again (2026-09-11, Mikey's call: five minutes was long enough for a student to reach the settings on a shared device). Any tap or key resets the clock. Returning within five minutes of the last activity skips the PIN.

- Loop-back routing, built 2026-09-10: two failed rounds in a row on a module reopen its nearest prerequisite for a quick check, even one mastered before. The result screen says so in plain words and offers it as the one thing to do. It is recorded as an event so the educator's module story mentions it. The prerequisite graph is what makes this reach across grades once content exists there.
- An educator creates an account once, from a Create account link under Educator Login: they choose a PIN (four to six digits, a convenience lock stored scrambled on the device), name the device, and pick their state, with an i tip explaining why the state matters. After that, Educator Login asks for the PIN and nothing else. The prototype PIN 2468 still opens the door on a device with no account, so testers are never locked out.
- The Classroom page no longer shows the coverage line; the state and a Change state link sit under the i tip beside the roster text.

- Content written 2026-09-10 while Mikey was away: kindergarten math gained one more/one less, joining and taking away (story word problems), comparing numbers, and shapes; kindergarten reading gained letter sounds and rhymes. Every new generator is re-derived independently in the tests from the question's own words, across 300 seeds. Kindergarten math now covers 13 of 18 TEKS standards, kindergarten reading 5 of 10.
- Word lists for reading (SOUND_WORDS, RHYME_FAMILIES) use only words a kindergartener says and hears. Adding to them is how reading content grows; the tests check every combination automatically.

- All states, decided 2026-09-10: a module is the unit and a standard is a label, so every standard in the plan carries a framework (TEKS or CCSS) and every course is mapped to both. An educator chooses their state once at first sign-in; the Classroom page shows coverage against that state's framework. Fifty states plus DC are listed. Adding a framework means adding its codes to the standards, not new content.
- Updates reach educators by opening the site, because the app is served from a static address and holds no accounts. A backup fixture per format version is kept in tests/fixtures and must restore forever.
- Onboarding documents for whoever comes next: CLAUDE.md now opens with a reading order; docs/VISION.md holds the why; docs/ALGORITHMS.md explains every number and marks which are judgements.

- The class view ("Who needs help"), built 2026-09-10: every active student ranked by a need score built only from what is in their record (modules stuck on, loop backs, guessing flags, low confidence on mastered work, time since last practice), with the reasons listed in words so the score is never a mystery, three bands (needs help now, keep an eye on, on track), a one-sentence summary at the top, and Open report on every row.
- The spoken Wonder for pre-readers: one spoken question, large tap targets, two short voices shown one at a time and spoken as they appear, offered once after a mastery and only when approved.

- Letter tracing, built 2026-09-10, is the first touch-screen module and the answer to "how do they learn to write without paper". Twelve capital letters are defined as strokes on a 100 by 100 grid; the pad shows them faintly with a green starting dot and an arrow; the finger's path is checked by arithmetic: every stroke must be visited in order within a tolerance that forgives a wobbly hand, and most of the drawing must lie on the letter so a scribble fails. The Letters course now carries the touch-screen warning. Lowercase letters and curves beyond O and C are the next step.

- Reflection answers stay unsaved, decided again 2026-09-10 after the move to local-first. The legal exposure shrank, but two reasons stand: a child who knows a teacher will read it writes to perform rather than to think, and anything stored travels in backup files that get emailed around. If Mikey wants it, the honest form is an educator switch, off by default, with answers excluded from backups unless explicitly included.
- "Arithmetic, not AI" means: built from checked rules and word lists, never composed by a language model at question time. Open writing is the stated exception: prompts from templates, structure checked by rule, quality judged by a person or an AI assistant with a teacher confirming.
- Browser account sync does not share EduSphere data: browsers sync settings, not a site's storage. One device per student, or restore before switching. Subscription tiers by device count are fine; sync is not to be promised.
- Selling and sign-in without a server of ours: a static landing page with Buy and Log in, purchase through a payment service that issues a license key at checkout, the key checked offline by the app. The only thing maintained is the payment dashboard. The page also lists permissions (allow downloads for backups; no microphone; no account) and device advice (Chromebook or desktop Chrome for touchless backup, iPad for finger tracing).
- Pre-K courses are non-linear: parallel activities where only named prerequisites apply. Pre-K standards come from the Texas Prekindergarten Guidelines and the federal Head Start Early Learning Outcomes Framework.

- Reflection answers: confirmed unsaved, 2026-09-10.
- A vendor-held copy of every backup: declined 2026-09-10. It would make EduSphere the custodian of every child's record from every school, which is the server-side data the local-first design avoids, and it needs a data agreement under FERPA and COPPA. Support instead runs on the school's own cloud folder (touchless backup into Drive) and files shared on request. A per-school opt-in under a data agreement is a phase 1 possibility, after the attorney review.

- The product ships as `index.html`, built by `tools/build-site.mjs` with React bundled in and storage on the browser's localStorage, so GitHub Pages or any plain host can serve it and nothing is fetched at run time. The `.jsx` artifact remains for the chat preview only. A test reloads the standalone page in a real browser and checks a student survives the reload.

- Desktop layout: the content column widens to 720px on screens over 1000px, the welcome card sits lower, and on screens over 1240px a pair of quiet arcs in the logo's green fill the edges without ever touching a tap target. The backup link glows in place when a backup is due, rather than carrying a halo.

- A forgotten PIN is reset only with a backup file of this classroom, decided 2026-09-11 after Mikey pointed out that a student on a shared device could otherwise reset the PIN and tamper with records. Every backup carries a recovery code made when the account was created; the reset checks it. A student does not have the educator's backup file. Each reset is recorded in the profile and shown on the Classroom page. The PIN itself is never in a backup or a file name. Two limits stated plainly: a brand-new account with no backup cannot be reset (the Classroom page nudges for a first backup), and a student who knows browser developer tools can read anything stored locally, which is the floor for any offline app and why the PIN is called a convenience lock.
- Sign out on the Classroom page ends the educator session at once, so the next person at a shared device meets the PIN screen.
- Contact us opens a small popup with the address and an email button, rather than showing the address beside the link.

- The recovery code sits first in every backup file with a note saying what it is for, shows on the Backup page to a signed-in educator, and can be typed on the Forgot PIN screen instead of choosing the file. Nobody has to open a backup to find it, but anyone who does sees it at once.

- Reflections come round after every two mastered modules (Mikey asked for every two or three, 2026-09-11), drawn from a pool shared across courses at the same stage and rotated so the least recently answered comes first. Six more questions were written so the rotation has something to rotate through: three for the early years, three for grades 3 to 5. The logo is now the browser-tab icon, built from the same drawing.

- A reflection question is never offered a third time to the same student (WONDER_MAX_REPEATS = 2), decided 2026-09-11. Once every approved question in a stage has been answered twice, the reflection is skipped rather than repeated. Pool size therefore governs how often reflections appear, never whether one repeats. The pool grew to 36 (17 early years, 19 grades 3 to 5) and should keep growing; the teen and grown stages have none yet because no courses exist there.

- Reflection themes, decided 2026-09-11 from Mikey's list: world (curiosity), failure (failure is how learning happens), feelings (naming and handling them), ups-and-downs (hard days are normal and we get through them, without getting tied up in our own narratives). Every question carries one. The cadence counts finished rounds, pass or fail, not masteries, so a struggling child meets reflections more often; after a failed round the failure, feelings and ups-and-downs questions come first. The pool is 56 and a coverage check notes any stage with fewer questions than modules, so the pool keeps pace as the curriculum grows.

- Three.js is not used, decided 2026-09-11 when Mikey offered it. It weighs about 600 KB and needs a graphics context that some locked-down school devices disable, so it would slow the first load and fail quietly on the machines a microschool is likely to have. The 3D look is achieved with gradients in the SVG drawings we already use (shaded solids and counting balls), which weigh nothing and render everywhere. Three.js stays an option for one specific future screen, such as a solid a child can spin, loaded only there.

- Young learners, redefined 2026-09-11 on Mikey's instruction: a student with ANY early-years course (pre-K to grade 2) still unfinished gets the handheld screens, whatever else is assigned. Early courses list first in every subject, the prerequisite graph keeps advanced work locked until the early work it depends on is mastered, and the star row counts only early modules. Once every early course is complete the student graduates to the regular screens.
- A new student starts on the recommended courses for their level (the two lowest grades of their band), not on every course. Course lists on the report run pre-K first, college last.
- Every user-facing string is checked for American spelling and house style by tests/spelling.test.mjs (step 3 of check.sh). Contractions are welcome. Mikey's exact wording is never overridden once given.
- The educator report: a collapsible bulleted summary (open by default), a collapsible Assigned now list (closed), all per-course progress behind one Progress by course dropdown with an i tip (closed), and Key Words - Explained with each term folded until tapped. Reviewed Wonder questions fold under a Reviewed row per stage so the next awaiting question is always at the top. Every i tip shares one gradient look and closes on sign out and on leaving a screen.

- index.html stays one generated file on purpose: it runs from GitHub Pages, a USB stick or a double-click with nothing to fetch, and its length costs nothing because nobody edits it. The sources are already split (src/logic.mjs, src/curriculum.mjs, src/ui.jsx) and those are what get edited.
- New students start on the lowest grade of their band only (pre-K for early years). Finishing every module of a course switches on the next course up in that subject automatically, recorded as a courses_enabled event so the educator sees it and can change it. A placement check, once built, will jump further.
- Young learners' subject cards breathe (a gentle scale and soft shadow on the card itself) instead of carrying a halo that grew beyond the screen; locked modules are not shown to them; the finished-modules fold sits below the available modules. Reflection screens open with a sliding "Let's Wonder!" title. The matching lesson shows two shapes side by side.
- The test browser runs with reduced motion, because a card that breathes forever never looks still to an automated click.

- The zip is the repository: it unpacks without a wrapper folder and contains exactly what GitHub should hold (no dist/). A .gitignore keeps .DS_Store and test output out. The tab icon is a bold drawing of its own (green disc, gold arc, white E) because the full logo's arcs vanish at sixteen pixels. On laptops the welcome page is a full-height column: logo a third bigger, a taller sign-in box, the three links at the foot and larger, and the side arcs wider; the pointer fades letters as well as arcs.

- Wide screens (2026-09-11): the content column zooms by 8 percent so text and taps grow together, sections get more air, the Classroom intro is padded to wrap in two lines with the i kept on the line, Wonder Questions and Life Skills sit side by side, the Active Students dropdown and its list share one box, Add someone new and Who needs help narrow to 380px, and the backup link is larger. Phones are unchanged.
- A pre-K reading course (rhymes, first sounds, big letters; readAloud; non-linear) so early learners start with both subjects, mapped to the Texas Prekindergarten Guidelines and Head Start; with its own reflection.
- GitHub: publish the unzipped folder itself as the repository through GitHub Desktop (Add Local Repository, create a repository here, Publish); updates are drag-over-replace then Commit and Push.

- Young learners see only their early-years courses on the overview (2026-09-11): anything above grade 2, even if assigned, waits until every early course is complete. Their overview is a full-height column with the greeting centered and larger, no course labels between modules, a star row at the foot that fills from the left like counting, and the practice-again fold beneath the stars. Tapping an answer plays a soft two-note blip made by the browser itself; moving on is silent. The voice prefers warm female voices and speaks a little brighter.
- Welcome page: Educator Login is a green button, the sign-in card is calmer, and the name grid scales with the number of students. The logo's pointer fade is tighter so smaller pieces vanish, and the side arcs draw themselves in once.

- A student record with no course decision gets the starter set for the student's level the first time it is opened (withStarterCourses), so students added before the starter rule are not handed every course. The privacy note sits at the very foot of the welcome page; the PIN screen sits lower on large screens; Who needs help matches Add someone new with its explanation under an i; the Key Words card is darker.

- Spoken lesson lines read like a person talking (2026-09-11): every line must contain a real sentence of four or more words unless it only names letters or counts aloud, and every picture must match the words spoken over it (a shown word is said, a shape is named, a highlight appears in every word shown). Instruction lines are exempt from the word match because a reading lesson must show a word without saying it. tests/scripts.test.mjs enforces both and is step 4 of check.sh. Thirty-seven lines were reworded, such as "Cat. Hat. These two words have endings that sound alike."
- Letter pictures can carry a highlight ('at' for a rhyme ending, 'first' for an opening sound); those letters turn gold and grow while the rest stay quiet.
- Better voices, phase 1 plan: record every fixed spoken line once with a high-quality service at build time and ship the audio with the site, keeping the device voice as the fallback and for prompts whose numbers change. Not built now because it needs a paid key and a network.

- Young learners have no practice-again list (2026-09-11, Mikey's call). Mastered modules return by themselves as memory checks inside later rounds, a child that age does not self-direct review, and a teacher who wants a module redone resets it from the report. The star row stays as the only thing at the foot of their overview.

- Halos are gone everywhere (2026-09-11, Mikey's call): an inviting button pulses itself gently (edu-pulse, a five percent scale) instead of casting a ring around it. The Let's Wonder! title bounces in and shimmers between green and gold. The voice is a little slower (rate 0.86). Two faint spheres drawn from the logo's own arcs flank it on large screens. Wonder Questions and Life Skills cards are green and light gold with their buttons aligned at the foot.

- Cross-course prerequisites gate a module only when the earlier course is assigned to the student (2026-09-11, after Mikey found an Elementary student locked out of every grade 3 reading module). Placement is the educator's judgment: leaving grade 2 off means the student is past it, so grade 3 opens. In-course chains always hold, and loop-back still uses the full prerequisite list, so a struggling student is sent back to the earlier module regardless. gatingPrerequisites and moduleStatuses(progress, enabledIds) carry this; the report, class view and student summary use it.

- While an educator is signed in, a small chip at the top right of every educator screen counts down to sign-out ("Logging out in 30 seconds"); mouse movement, wheel, scrolling, typing and clicking reset it. Touchless backup reports what it last did on the Classroom page and the Backup page, and a failed write switches it off with a visible note instead of silently. The Hear it again control is a speaker button in the top-right corner of the card, and the "on silent" note sits at the foot of the page once anything has been spoken.

- After an idle sign-out, unlocking with the PIN returns the educator to the page and scroll position they were on; Change state does the same. A completed course on the transcript names the standards it satisfied in the state's framework (Common Core until a state is chosen), read from the curriculum plan; a coverage check requires every course to map to at least one code in each framework.

- Touchless backup writes into a chosen folder, one file per day named by the date and rewritten through the day (2026-09-12, answering Mikey's question about a file per module). A backup holds the whole classroom, so a file per completed round would run to thousands of files and gigabytes; daily snapshots give history at a few megabytes a year for a large class. Where a browser offers a file picker but no folder picker, the older single rewritten file is used.

- Touchless backup remembers its folder across browser restarts (the folder handle is kept in the browser's own store, 2026-09-12, after Mikey pointed out that a folder forgotten at every restart made the feature a false promise). After a restart the Classroom page checks: where the browser has kept its permission the daily files resume by themselves; otherwise a single Resume touchless backup tap grants it again. The wording everywhere now says exactly that. A truly unattended backup with no tap on any device would need a server, which is deferred.

- Grade 4 reading (summarizing, making inferences, similes and metaphors, how a text is built; six built passages with summaries, inferences and clues) and grade 5 math (adding and subtracting decimals, multiplying fractions, dividing by two-digit numbers, volume, order of operations) added 2026-09-12 in the same shapes as every other course: five generators per module, every math generator re-derived independently, standards mapped to TEKS and Common Core and marked ready, a reflection per course. 74 modules, 76 reflections. NEXT: grade 5 reading, then grades 6 to 8.

- Grade 5 reading (theme, point of view, two accounts of one event, idioms; five built stories, six narration lines, four account pairs, eight idioms) and grade 6 math (ratios and rates, dividing fractions, numbers below zero, one-step equations, percents) added 2026-09-12. Grade 6 opens the teen reflection pool: five questions across all four themes, plus one for grade 5 reading. 83 modules, 82 reflections, every grade-subject marked ready. NEXT: grade 6 reading, then grades 7 and 8.

- Grade 5 reading (theme, point of view, idioms and sayings, backing a claim with the text) and grade 6 math (ratios, dividing fractions, negative numbers, area of triangles and parallelograms, one-step equations) added 2026-09-12, mapped to TEKS and Common Core and marked ready. Grade 6 opens the teen reflection stage. An older unfinished draft of the same two courses was found in the file and removed in favor of the finished one. 83 modules, 82 reflections. NEXT: grade 6 reading, then grades 7 and 8.

- Grade 6 reading (claims and reasons, tone and mood, word roots, the central idea) and grade 7 math (proportions, percents, adding and subtracting integers, two-step equations, circles) added 2026-09-12, mapped to TEKS and Common Core and marked ready, with four teen reflections. The independent checks caught two generator faults before they shipped (a fractional number of students, a negative total), both tightened. 92 modules, 86 reflections. NEXT: grade 7 reading, grade 8 math and reading, then 9 to 12.

- Grade 7 reading (the author's purpose, strong and weak evidence, connotation, why a character acts) and grade 8 math (slope, exponents, square roots, the Pythagorean theorem, scientific notation) added 2026-09-12, mapped to TEKS and Common Core and marked ready, with four teen reflections. 101 modules, 90 reflections. NEXT: grade 8 reading, then grades 9 to 12.

- Grade 8 reading (flawed reasoning, irony, allusions, an objective summary) and grade 9 math, Algebra 1 (multi-step equations, functions, systems of equations, factoring, exponential growth) added 2026-09-12, mapped to TEKS and Common Core and marked ready. Grade 9 opens the grown reflection stage (four questions across all four themes) and grade 8 reading adds two teen ones. A number inside a course title, like Algebra 1, no longer counts as a grade in the course search. 110 modules, 96 reflections. NEXT: grade 9 reading, then 10, 11 and 12.

- Grade 9 reading, English 1 (rhetorical appeals, how a theme develops, the effect of word choice, credible sources) and grade 10 math, Geometry (angle relationships, similar triangles, transformations, sine, cosine and tangent, arcs and sectors) added 2026-09-12, mapped to TEKS and Common Core and marked ready, with four grown reflections. 119 modules, 100 reflections. NEXT: grade 10 reading, then 11 and 12.

- 2026-09-13: teaching text is rendered through a small markup (newline per line, **bold**, [[centered equation]]) and written in the drawn-out, repeated, Gladwell-for-a-younger-reader style Mikey specified (see DESIGN.md). Four lessons rewritten as models; equation explanations walk one step per line; question screens put the question first and center everything; a wrong answer among named kinds (appeals, flaws, ironies) gets a sentence about the chosen kind; a round never repeats a question (buildAttempt re-rolls duplicates). The appeals questions had generic job pairs that did not match some statements; each statement now carries its own pair. Share is off everywhere (failed on phone and laptop). A hidden student can be deleted for good from the Inactive list, after confirmation. My progress counts the student's own modules, not every module in the platform. docs/social-preview.png (1280 x 640) is the GitHub social preview. Touch-only modules: the tracing pad accepts a mouse or trackpad, a note says a touch screen is easier, and a round left by the X records nothing; a phone QR hand-off was declined because the phone would not hold the student's record.

- 2026-09-13, second pass: a formatting pass (formatTeachingText) opens up every lesson, key idea and explanation that is still in the old dense form (one sentence per line, equations in bold, a sentence that is mostly an equation centered, x emphasized), so the rules apply everywhere at once; 52 lessons are now hand-written in the drawn-out style (all of grades 4 to 10 math and grades 4 to 9 reading, plus the main idea); the remaining read-aloud and early lessons keep their spoken scripts and are opened up by the pass. Word pictures wrap instead of spelling giant letters off the screen.

- 2026-09-13, third pass: geometry, grades 1 to 4 math and grades 1 to 3 reading hand-rewritten in the drawn-out style. 92 of 119 lessons are now hand-written that way. The remaining 27 are the pre-K and kindergarten modules, whose teaching is carried by their spoken scripts (one short sentence at a time, governed by tests/scripts.test.mjs); their short written paragraphs are opened up by the formatting pass and left as they are.

- Grade 10 reading, English 2 (spotting bias, paraphrasing precisely, complex characters, symbols) and grade 11 math, Algebra 2 (the quadratic formula, multiplying binomials, sequences, logarithms, absolute value equations) added 2026-09-13, written in the drawn-out style from the first draft, mapped to TEKS and Common Core and marked ready, with four grown reflections. 128 modules, 104 reflections. NEXT: grade 11 reading, then grade 12.

- Mastery v2 (decided 2026-09-13, to build in phase 1; Mikey's point that one short explanation and 4 of 5 on a single round is thin). A module is mastered after two passing rounds on different days: the first pass unlocks the next module so nobody waits, the second earns the star. Two memory checks per round, drawn from any mastered module across courses. A cumulative checkpoint round of eight questions after every fourth module, no new material. Two or three worked examples per lesson at rising difficulty. This changes what an attempt_completed event means for mastery, so it needs its own migration rule (existing single-pass masteries stay mastered) and a pass through every test that masters a module with one perfect set.

- Grade 11 reading, English 3 (counterclaims, satire, sentence structure and effect, whether the evidence is enough) and grade 12 math, Precalculus (shifting a function, composite functions, the unit circle, half-life, end behavior of polynomials) added 2026-09-13 in the drawn-out style, mapped to TEKS and Common Core and marked ready, with two grown reflections. Every grade from pre-K through 12 now has math, and every grade from 1 through 11 has reading. 137 modules, 106 reflections. NEXT: grade 12 reading, then the college band, then mastery v2.

- Grade 12 reading, English 4 (reading two sources together, unstated assumptions, the precise word, why the author built it that way), college math, Statistics and reasoning (mean median and mode, spread, probability, compound interest, correlation is not causation) and college reading, Academic reading (thesis statements, the shape of an academic argument, reading numbers inside prose, logical consistency) added 2026-09-13 in the drawn-out style, mapped to TEKS or the Texas College and Career Readiness Standards and to Common Core, marked ready, with four grown reflections. Every grade from pre-K through college now has math, and every grade from 1 through college has reading. 150 modules, 110 reflections. NEXT: pre-K 3 (the only empty grade), then Mastery v2, then a third subject.

- Mastery v2 refinement (2026-09-13, after Mikey asked about three rounds): two spaced passes for the star, with the third retrieval coming from a memory check inside another module or a cumulative checkpoint rather than a third full round, because spacing holds better than volume and a third grind risks turning the platform into a chore. A missed checkpoint question lowers confidence and queues a refresher round; it never removes the star.

- Pre-K 3 added 2026-09-13: Very first steps (red and blue, find the match, one and two, more) and Listen and point (circle and square, A and B, which one is different), all spoken, two choices per question, mapped to the Texas Prekindergarten Guidelines and the Head Start ELOF. Both courses start hidden: a new early-years student still begins at pre-K 4, and an educator switches pre-K 3 on for a three-year-old. Every grade from pre-K 3 through college now has a course in both subjects. 157 modules, 112 reflections. NEXT: Mastery v2, then a third subject.

- Mastery v2 shipped 2026-09-14 (core): deriveProgress counts passes and the distinct local days they fell on; a pass (passed) unlocks the next module and the next course and joins the memory-check pool; the star (mastered) needs passes on two different days; passes dated before 2026-09-14, or with an unreadable time, keep the star on their own so nobody loses one. Status 'passed' appears on the overview (hollow star, Passed once, Pass it again), in My progress (its own group), in the report rows and module story, on the transcript, in the who-needs-help why-sentence, and on the result screen ("Come back on another day and pass it again to earn the star"). Still to build from the same design: a second memory check per round, and the cumulative eight-question checkpoint after every fourth module with a refresher rather than a lost star.

- Two memory checks per round shipped 2026-09-14: the second comes from a different passed module than the first, from anywhere the student has passed; with only one passed module there is one check, never a repeat. Both count in the review tally, in confidence, and are named on the result screen ("Memory checks from earlier: count to 5 correct, letter names missed"). The event carries review2 beside review, so old records read unchanged. Still to build: the cumulative eight-question checkpoint after every fourth module.

- Cumulative checkpoint shipped 2026-09-14: due when the number of passed modules reaches a multiple of four and no checkpoint has been done at that count; eight questions from passed modules, never a repeat, offered on the overview to readers (young learners keep memory checks only); recorded as a checkpoint_completed event with per-module results; a miss counts in confidence like a missed memory check (a weak return now costs a point) and marks the module for a refresher on the overview, the report and the module story, cleared by passing the module again; the checkpoint result screen names the modules worth a refresher and says nothing passed is lost. Mastery v2 is now complete: spaced two-pass mastery, two cross-course memory checks per round, and checkpoints.

- Science, a third subject, opened 2026-09-14 with grade 3 (solids liquids and gases, pushes and pulls, life cycles, weather and seasons) and grade 6 (elements and compounds, how heat moves, the moving Earth, cells, who eats whom), written in the drawn-out style, mapped to TEKS Science (2021) and to NGSS in the second-framework slot the way pre-K uses the Head Start ELOF, marked ready, with two reflections. Nothing in the app assumed two subjects; the Standards map, the report and the search picked science up unchanged. 166 modules, 114 reflections. NEXT: science for the other grades, then the placement check.

- Science grades 4 (forms of energy, circuits, how land changes, adaptations) and 5 (mixtures and solutions, the Earth sun and moon, the water cycle, inherited or learned) added 2026-09-14, mapped to TEKS Science and NGSS, marked ready, with two growing reflections. Science now runs grades 3 to 6. 174 modules, 116 reflections. NEXT: science 7 and 8, then K to 2 (spoken), then high school.

- Science grades 7 (body systems, photosynthesis, weather systems, natural selection) and 8 (inside the atom, Newton's laws, chemical reactions, the scale of the universe) added 2026-09-14, mapped to TEKS Science and NGSS, marked ready, with two teen reflections. Science now runs grades 3 to 8. 182 modules, 118 reflections. NEXT: K to 2 science (spoken), then high school science.

- Spoken science for kindergarten (day and night, kinds of weather, living or not living, what a plant needs), grade 1 (patterns in the sky, water changes, what animals need) and grade 2 (hard soft wet dry, magnets, habitats) added 2026-09-14 with fifteen drawn icons (sun, moon, cloud, rain, snow, plant, tree, flower, fish, bird, rock, drop, ice, fire, magnet) so every picture is named by the words a child hears; the script and picture checks and the rule tests know the icon kind; subjects now always list Math, Reading, Science in that order. Science runs kindergarten through grade 8. 192 modules, 120 reflections. NEXT: high school science, then the placement check.

- Biology, grade 9 (DNA and genes, Punnett squares, respiration and photosynthesis, evidence for evolution) and Chemistry, grade 10 (the periodic table, ionic and covalent bonds, balancing equations, acids and bases) added 2026-09-14, mapped to TEKS Biology and Chemistry and to NGSS, marked ready, with two grown reflections; a new high school student now starts on grade 9 math, reading and science. 200 modules, 122 reflections. NEXT: Physics (11) and Earth and space (12), then the placement check.

- Physics, grade 11 (speed and acceleration, kinetic and potential energy, waves, current voltage and resistance) and Earth and space, grade 12 (the rock cycle, climate and weather, the life of a star, people and the planet) added 2026-09-14, mapped to TEKS and NGSS, marked ready, with two grown reflections. Science now runs kindergarten through grade 12, and every grade from pre-K 3 through college has math and reading. 208 modules, 124 reflections. NEXT: the placement check.

- Placement check shipped 2026-09-14: on the overview, a reader with nothing done in a subject is offered a check that walks up the band one grade at a time, four questions per grade drawn from that grade's modules, three right to clear, stopping at the first grade not cleared (or the top of the band); a 'placed' event unlocks every module in the cleared grades (passed for gating, never mastered, visited by memory checks) and the starting grade's courses in that subject are switched on; the report rows, module story and transcript say "Placed past"; spoken and hidden courses are never placement material, so the early band offers grade 2 only and young learners see no offer. 208 modules, 124 reflections. NEXT: drag, order and draw question types; lowercase tracing; pre-recorded voices.

- 2026-09-14: Mikey asked whether high school science should have far more modules; yes. Biology grew to eight (added mitosis and meiosis, from DNA to protein, the carbon and nitrogen cycles, homeostasis) and Chemistry to eight (added moles and molar mass, types of reactions, gas laws, concentration), each mapped and marked ready, every calculation re-derived in the tests. Physics and Earth and space grow next, then the middle-school science courses. 216 modules.

- 2026-09-14: Physics grew to eight (momentum, work and power, light reflection and refraction, series and parallel circuits) and Earth and space to eight (the layers of the Earth, ocean currents, renewable and nonrenewable resources, the expanding universe), mapped and marked ready, every calculation re-derived. All four high school science courses are now eight modules. 224 modules. NEXT: middle-school science to six or more each.

- 2026-09-14, placement polish from Mikey's phone: the offer card sits with air above it and carries his wording; while a subject's placement is unsettled, that subject's courses, the mastered count and My progress stay hidden so a student cannot slip past the check, with a "Start at the beginning instead" link that records a placement with nothing cleared; the practice header shows the module title as written ("From DNA to protein" had become "From from dna to protein") and every title dropped into a sentence now goes through lowerTitle, which keeps acronyms and names; three-digit answer choices are sized to fit their buttons; the My progress group reads "Courses I've Passed".

- 2026-09-14: placement rounds carry a full-width "I don't know" button (Mikey's suggestion): it counts as a miss, shows only "No problem. That one does not count against you." and moves on, since a guess would add noise to where a student lands. Middle-school science grew to six modules each: grade 6 added density; grade 7 added genes and traits and energy in ecosystems; grade 8 added speed on a graph and rock layers and fossils; all mapped and marked ready. 229 modules. NEXT: drag, order and draw question types.

- Order questions shipped 2026-09-14: a new question type where the student taps pieces into a sequence (tap-to-order rather than drag, because tapping works on every phone and screen reader), with a tap on a placed piece taking it back; the answer travels as the sequence joined with ' | ' so the event log stays plain text; three generators (numbers to 100 in order, life-cycle stages, story events) wired into comparing to 100, life cycles and sequence and cause; the sweep checks every order question is a shuffle of its own answer and judged correctly, and the browser test taps them. Drag-and-drop is deferred on purpose: it is a worse interaction on phones than tapping and adds nothing a tap cannot. NEXT: lowercase tracing, then pre-recorded voices.

- Lowercase tracing shipped 2026-09-14: nine small-letter shapes (l, i, t, o, c, v, x, n, u) on the x-height with l and t reaching up, a Tracing small letters module in kindergarten reading right after the capital one (later modules moved down one), three generators, the tracing check widened to small letters, mapped to TEKS K.2E and Common Core L.K.1.A. 230 modules. NEXT: pre-recorded voices.

- 2026-09-14, after Mikey asked for more subjects: WRITING is the first educator-marked subject. Grade 4 (a paragraph with a topic sentence, an opinion paragraph, a small story) and grade 8 (a thesis and an outline, a paragraph that proves something, a five-paragraph essay). A writing question is a prompt, a word target and a self-check list; the student writes in a box and sends it; the piece is kept in a writing_submitted event (the one place the app keeps what a student typed, said plainly on the sent screen); the educator marks it from the report (Meets the bar or Not yet, a writing_marked event); a mark that meets the bar is a pass on the day of the mark, so two pieces marked on different days earn the star and a pass opens the next module; the overview, report rows, transcript and module story show the waiting state; mapped to TEKS and Common Core with two reflections. READING LISTS: six well-known titles with authors per grade from pre-K 3 to college, on the Standards map under a closed fold, titles only, nothing from the books. TRACING: capitals K, W, X, Y, Z and a third kindergarten tracing module. 239 modules, 126 reflections. NEXT: history and social studies as a fourth subject.

- 2026-09-14, from Mikey's phone: WRITING HAPPENS ON PAPER. The student sees the prompt, the word target and the self-check, writes on paper, ticks the list and taps "I have finished writing it"; the app keeps only the prompt, the self-check and the time (no typed text), and the report shows "Writing that needs your check" with Meets the bar or Not yet; the next module opens only on the educator's check; report rows and transcript read "Needs your check". EDUCATOR OVERRIDE: a locked module on the report shows "Locked by its prerequisites. Unlock it anyway", which records a module_unlocked event that the gate honors, because the algorithm advises and the educator decides. TOUCH REQUIRED: tracing and connect-the-dots no longer accept a mouse; on a device without a touch screen the module says so and does not open, and the lesson recommends a stylus. CONNECT THE DOTS for pre-K 4 (triangle, square, house, star, zigzag, boat, numbered dots on the pad, judged like tracing). Course search now answers to keywords (drawing, tracing, handwriting, stylus find the tracing and dots courses). Single-grade subjects also wait for a placement decision. Greeting has air below it; transcript section titles centered. 240 modules.

- 2026-09-14: the WRITING LADDER now runs grades 2, 3, 4, 5, 6, 8, 10 and 12 (sentences, paragraphs, several paragraphs, purpose, essays, writing about texts, writing for the world), three modules each from one table (WRITING_LADDER) with word targets rising from 15 to 900 and a short physical checklist per module, each grade mixing telling, explaining and persuading, all mapped and marked ready with two reflections. STYLUS PRACTICE: pre-K 4 gained First strokes (a line down, a line across, a wave, a zigzag, a circle) before Connect the dots, which gained kite, fish, tree and cup; kindergarten counting gained Tracing numbers 0 to 9 after count to 10 (later modules moved down one). The touch message and the README recommend an iPad or another large tablet with a stylus. 257 modules, 128 reflections. NEXT: history and social studies.

- 2026-09-14, Mikey's subject list: agreed history (core), computer science with a plain AI course, economics and personal finance, art and music appreciation, philosophy, psychology, speech, agriculture and business as electives in about that order; anatomy folds into biology and health; pushed back on sexual education (a school and family decision under state opt-in law, not something the platform ships), on AP and pre-AP labels (College Board marks; strong courses will be called advanced instead), and on foreign language until recorded audio exists; a typing elective for grades 3 to 5 is worth building. Built: writing grades 7, 9 and 11, so writing runs 2 through 12; high school writing (9 to 12) is TYPED and the piece is kept, while 2 to 8 stay on paper (a course flag `typed`); a RESOURCES row on the Classroom page with Reading Lists and Science Experiments beside Wonder Questions and Life Skills, each a printable page (EXPERIMENTS by band: four per band, ask before, do, what you see, nothing recorded); the other-courses fold reads "Other grades and electives" and courses can carry `elective`; kindergarten gained Tracing shapes (triangle, square, circle, star, heart) after Shapes. 268 modules. NEXT: history and social studies.

- 2026-09-14: WRITING NEVER LIVES IN THE APP. Every writing module names its medium: handwritten through grade 4, alternating in 5 to 8, typed in high school with one handwritten piece per year; a typed prompt says to type it in a word processor and hand it in, so keyboard competence is proved outside the app; the app keeps only the prompt (prefixed Typed or Handwritten), the self-check and the time. WALK-THROUGH MODE: from the Classroom page an educator can walk through any band as a student: every module open, a Skip button on every question, nothing recorded, a banner saying so, Exit returns to the classroom (a preview record with preview: true; addEvent does not save). READING LISTS gained a shelf for parents, educators and advanced readers with Mikey's picks (Brooks, Singer, Postman, Crouch, McDougall, Rubin, Hanson, Deutsch, and investing: Housel, Bogle, Malkiel, Graham). EXPERIMENTS now run four per grade from pre-K 3 through grade 12, sixty-four in all, on the experiments page by grade. HISTORY opened as the fourth core subject with grade 5 (the thirteen colonies, the road to revolution, the Constitution, growing west, the Civil War), dates re-derived in tests, mapped to TEKS Social Studies and NCSS themes; subjects order Math, Reading, Writing, Science, History. 273 modules, 129 reflections.

- 2026-09-14: the reading lists merged the Valor Education classical school lists (kindergarten through grade 12, titles and authors, selections marked) beside the earlier picks, 252 titles in all, with a line of attribution on the page; the grown-up shelf leads with The Meaning of Your Life (Arthur C. Brooks, 2026), the book Mikey meant.

- 2026-09-14: the reading-list attribution line was removed at Mikey's request. History grew: grade 4 Texas (the first Texans, Spanish and Mexican Texas, the Texas Revolution, Texas joins the Union) and grade 8 United States to 1877 (the founding documents, the early republic, the road to the Civil War, Reconstruction), every year re-derived in tests, mapped to TEKS Social Studies and NCSS themes, with a teen reflection on promises written in law. 281 modules, 130 reflections. NEXT: high school history (world, US, government and economics), then electives.

- 2026-09-14, from Mikey: the walk-through banner is now a small bottom-left chip ("Walk-through: nothing is recorded") on every screen, matching the logout chip. SEPTEMBER 11: each year on September 11 the student overview and the Classroom page show one quiet remembrance card, the same three plain lines for every age (the attacks, the responders who ran toward the danger, a quiet moment), which an educator can hide on the device for the rest of the year; nothing is recorded about it.

- 2026-09-14: Veterans Day (November 11) and Memorial Day (the last Monday of May) joined September 11 as days of remembrance, one quiet card each. History grew to high school: grade 10 world history (the first civilizations, Greece and Rome, the Middle Ages and the Renaissance, the age of revolutions, the Industrial Revolution, the world wars) and grade 11 United States history since 1877 (the Gilded Age and the Progressives, the Depression and the New Deal, America in the Second World War, the Cold War, the civil rights movement, recent America); years re-derived in tests, TEKS and NCSS mapped, a grown reflection on living inside unfinished history. History now runs grades 4, 5, 8, 10 and 11. 293 modules, 131 reflections. NEXT: grade 12 government and economics, then electives.

- 2026-09-14: TOUCHLESS BACKUP REMOVED at Mikey's request (it did not work for him). In its place, AUTOMATIC BACKUPS: after every finished round, a backup file is saved straight to the device's downloads folder with no prompt (plainDownload), never during a walk-through; the Backup page explains that only the newest file is needed and older ones can be deleted; educators can still download a backup to a folder they choose. The walk-through card reads "Walk through as a student" with buttons named only by band. The remembered-folder machinery, the resume links and the touchless text are gone everywhere.

- 2026-09-14, Mikey's walkthrough review: a walkthrough now enables every course in the band across every subject (pre-K included for early years), so middle and high school walkthroughs no longer showed early-years courses and the hand-held young-learner screens (the old call passed an empty event list to recommendedCourseIds, which returns every course); folds start closed; the greeting reads "Walkthrough mode. Nothing here is recorded."; the chip reads "Walkthrough Mode". The complete-sentences lesson is in Mikey's words. The writing sent screen reads "Great job! Now you just need to give your teacher the paper..." (or the printed or sent file when the piece was typed). Back to my courses buttons pulse. PLACEMENT WALKS DOWN: a student who does not clear the first grade of their band is probed one grade lower at a time until a grade is cleared, and starts at the lowest grade not cleared with everything below placed past (placementGradesAll, placementOutcome with an explicit start grade). Resource pages are "Reading List Ideas" and "Science Experiment Ideas" with Mikey's intros, laid out like Life Skills (deep green grade bars, soft green bodies); the Life Skills title is centered and its open bodies and the Wonder reviewed fold are soft green rather than white. Experiments: sixteen per grade, 240 in all.

- 2026-09-14: deleting an inactive student asks in a centered popup over the page (Yes, delete and Keep on their own line with room between them); with two or more inactive students a centered "Delete all inactive students" link at the foot uses the same popup; the inactive note adds that deleting removes history and progress from this point forward while old backup files still hold them; the empty-roster card is light green and centered.

- 2026-09-14: photorealistic pictures were asked for and declined for now: the app draws everything as license-free SVG and photos would need licensed or generated assets; instead the icons are getting shading and highlights (sun, tree, fish, plant, drop first). The bottom bar of the green frame is drawn only on devices with a mouse, because a fixed bottom edge chases a phone's hiding address bar during fast scrolling; phones keep the top and sides and the device's own edge closes the frame. Adding a student now asks which grade to start in (every grade in the chosen band), and the starter set is every subject in that grade; the placement check still moves readers up or down.

- 2026-09-14, from Mikey's laptop: the stage bars on Wonder Questions and Life Skills and the grade bars on the two resource pages were washed out on wide screens because a wide-layout rule paints every button written with background: none a pale color; those bars now say background: transparent and show their green. Backup page: the top line reports only manual backups (automatic ones no longer move it), the sentence about devices allowing automatic backups is gone, the starter-PIN note sits on its own line, the device-name card and the Restore title are centered with Mikey's wording, and the automatic card is titled Automatic Backups with his text.

- 2026-09-14: My progress folds are fully centered (titles, lines, buttons, and Nothing here yet) and the first-step sentence reads "Open a module and try one."; the Classroom page's backup tip is in Mikey's words about automatic downloads, periodic manual backups to a shared drive, and one recent file restoring everything.

- 2026-09-14: the educator session now lasts sixty quiet seconds (was thirty), warning for the last ten (Mikey). On wide screens the pale backing that keeps stars from sitting behind loose text was also painting the blurb line inside the Wonder and Life Skills stage bars; text inside any tile, colored bar or button now keeps its container's color. The resource pages have more air between the intro and the first grade bar. The Wonder note "after every two practice rounds" was checked and is still accurate: two finished practice rounds since the last question, pass or fail; checkpoints, placements and writing pieces do not count.

- 2026-09-14: the login name grid is two buttons per row at most (one when there is a single student), every button the same 84px height whatever its name, and long names wrap up to three lines instead of being cut off (Mikey).

- 2026-09-14: pre-K 3 had no stylus work at all (Mikey noticed). Its math course now ends with First marks (a line down, a line across, a circle) and Three dots (triangle, square, boat and cup, the pictures with the fewest dots), both spoken, both touch-only, mapped to the Texas Prekindergarten Guidelines and the Head Start framework. 295 modules.

- 2026-09-14: Mikey saw a college-level student's math course open before any placement. Three fixes. "Start at the beginning instead" now also switches the subject's courses to the band's first grade (it only recorded the decision before, leaving the college course assigned). The placement walk starts at the grade the student was added at, when there is one, and walks up or down from there; clearing every probed grade starts the student one above the highest cleared grade rather than at the bottom of the band. And a real bug under it all: two record writes in a row each started from the record the screen had closed over, so the second silently dropped the first; every write now starts from the newest record (recordRef), and paired writes (an attempt plus the course it unlocks, a placement plus its courses) go in one call.

- 2026-09-14, Mikey's laptop review: the placement gate compared subject objects to strings and so hid nothing; pending subjects' courses now stay hidden until the check or the skip (this was also the college student's open math dropdown). Folds close on every student login. A touch module on a device without a touch screen is grayed with the note inside the card, on the right third, no pulse. A question's answer choices share one size so 4 never towers beside -3 or 1/6. The placement result stops at "is unlocked." Pre-K 3 grew by three modules (yellow and green, triangles too, listen and tap pictures) toward a full year of material. 298 modules.

- 2026-09-14: loop-back wording is Mikey's: "Don't worry, you'll get it but first, let's route back to some of the fundamentals underpinning these concepts." and the result line drops its trailing "first"; when a student is routed back, the fresh-set button is gone and only Look back and Back to overview remain. STANDING RULE: every pass adds to pre-K 3 through grade 2. This pass: Trace a big letter (L, T, O) in pre-K 4 reading and Writing numbers in grade 1 math, both stylus. 300 modules.

- 2026-09-14: every writing lesson, all 32 across grades 2 to 12, was drawn out much further at Mikey's request: one idea per line, each example labeled with its part in bold (reason, evidence, claim, warrant, beginning, middle, end), a worked example in every lesson, and the argument-with-evidence lesson in his words (an argument is essentially an opinion essay with evidence; reasons need facts to support them, examples to demonstrate them and quotations to show what an expert or a witness said; a reason without evidence is merely a feeling). STANDING RULE, restated: lessons are drawn out to this degree, not summarized.

- 2026-09-14: automatic backups now fire when a round finishes a whole course rather than after every round (per module was too many files); all text says per course.

- 2026-09-14, from Mikey's phone: the student cards on the Classroom page now put the picture, name and band centered across the top (with the starting grade when one was chosen) and Open report at the bottom right beside the action links, so long names no longer wrap beside the button. The logout chip shows only for the last ten seconds of the minute rather than counting down the whole time.

- 2026-09-14: the logout chip counts down the whole minute after all (Mikey: it reminds an educator not to hand the device to a student too soon); on phones it sits centered at the foot, on laptops bottom right.

- 2026-09-14: after a full progress reset the placement check comes back: the gate counted practice rounds from before the reset as reasons not to offer it (the placed events were already scoped by activeEvents; the attempts were not). Fixed by scoping the attempt check to events after the last reset.

- (2026-09-14) The placement check covers Math, Reading and Science only, on purpose. Writing is marked by the educator, so nothing in it can be scored by the app. History is territories, not rungs (Texas in grade 4, world history in grade 10): clearing grade 10 says nothing about grade 4, and the walk down would land a tenth grader in Texas history. When history needs a skip, it will be per module (a short check that places that one module), never the grade ladder. Until then the report's "Unlock it anyway" covers a transfer student.
- (2026-09-14) Grade 12 social studies is two courses under the History subject, United States government (government-12) and Economics and personal finance (economics-12), because Texas teaches them as two half-year courses and the transcript's standards lines stay clean. Every economics answer is computed and re-derived in the tests; every dated government fact carries a story.
- (2026-09-14) Every dated history question explains a wrong answer with the fact and a short story behind it (HISTORY_WHY in logic.mjs, `why` on the government and amendment lists). Mikey's rule: a bare date is unhelpful, a story sticks. The generator sweep fails if any dated question lacks one.
- (2026-09-14) Style checks tightened at Mikey's request: key ideas may not run a sentence past 28 words, and every lesson sentence starts with a capital and ends with a mark (tests/spelling.test.mjs). Student names are capped at 30 characters on add and rename; names already on a roster are never cut.

- (2026-09-14) The class view names a single missed round ("missed the last round of the thirteen colonies (2 of 5 right); a second miss in a row would route back to the fundamentals") and, when nothing is mastered yet, how many rounds there have been. Mikey tested a deliberate fail and the row said "Nothing to flag", which was false. The report summary now ends with practice so far: rounds, passes, and anything tried but not passed with its best score.
- (2026-09-14) The Standards map is grouped by grade (or by subject, one tap) with light-green dropdowns and white bodies; the reading lists left it, since they have their own page. The printed map keeps the same grouping and shows everything open.

- (2026-09-14) Tracing lessons demonstrate instead of describing. Mikey saw "Start at dot number 1. Draw a line down." over a picture of one dot and said it made no sense to a three-year-old. Every tracing lesson (pre-K marks and strokes, connect the dots, letters, numbers, shapes) now shows the stroke drawing itself, slowly, on a loop, with the same start dot and arrow the practice pad uses, and the words say "Watch the line" first.

- (2026-09-14) One practice line, `practiceLine(report)`, feeds both the report summary and every class-view row ("Practice so far: 3 rounds, 1 passed. Tried but not passed yet: ..."), so the two screens can never disagree. On the report each tried module's name is a button that opens its story. On the Standards map a ready grade with gaps shows its count in clay.

- (2026-09-14) The quick check, "I already know this": the next available module in any subject except Writing and the spoken courses offers five questions, one per generator slot, no lesson, scored at the module's own pass mark. A pass emits the same `placed` event placement uses (passed for gating, never mastered, star still to earn, memory checks keep visiting), marked `quickCheck: true` so it never counts as the subject's placement. A miss records the try, opens the lesson, and never feeds the loop back. Three answers under two seconds mean nobody read the questions, and the check does not place. One try per module. Educators can switch it off for the classroom. Mikey chose every subject, the round's own pass mark, and the switch.
- (2026-09-14) History now runs every grade from 4 to 12: grade 6 world cultures, grade 7 Texas history and grade 9 world geography joined, with every number computed and re-derived and every dated fact carrying its story. Codes for these three cite the TEKS by strand or era rather than by knowledge-statement number, which I could not verify offline.
- (2026-09-14) Tracing demonstrations have two paces: pre-K and kindergarten lines draw at 40 units a second, grade 1 letters and numbers at 65 (`pace: 'quick'` on the picture). Pre-K 4 gained "Draw the shapes" under the early-years rule.

- (2026-09-14) History now runs from kindergarten to grade 12. Kindergarten to grade 2 community and civics are spoken courses (civics-k Me and my community, civics-1 Our community, civics-2 Our community's story), three modules each; grade 3 Communities and government is a reading course. Texas TEKS cited by strand. Short word choices, as the spoken science courses already use.
- (2026-09-14) The story rule reached science through one central hook: `SCIENCE_WHY` in logic.mjs, keyed by generator and answer, appended in `generateQuestion` so no generator has to remember. First batch: sixteen fact generators (states of matter, push and pull, float and sink, dominant and recessive, particle charge, mitosis and meiosis, transcription and translation, reactants and products, metals, series and parallel, reflection and refraction, weather and climate, solute and solvent, forms of energy, inherited and learned). The sweep fails if a mapped generator ever loses its story. Fifty more one-line science generators remain; add them to the map as they come up.
- (2026-09-14) The practice line counts quick checks: a missed one appears under "tried but not passed" as "quick check 2 of 5", a passed one as "Skipped by quick check", on the report and on every class-view row.

- (2026-09-15) College history is the craft, not another century: Thinking like a historian (sources, counting time, cause and effect, the big turns, writing history), five modules, with calendar arithmetic computed and re-derived and the great turns carrying stories. History now runs from kindergarten to college, every band.
- (2026-09-15) SCIENCE_WHY second batch: thirty-five more fact generators carry a story after the answer (forces, life-cycle stages, weather tools, food-web roles, cell parts, weathering, adaptations, circuits, the Earth's motions, the water cycle, organs and systems, pressure and fronts, atoms, motion graphs, genes, photosynthesis and respiration, evidence for evolution, homeostasis, the periodic table, bonds, energy, circuits, the moon, base pairs). The three generators whose answers are long example sentences (energy forms, heat transfer, reaction types) already explain themselves and stay as they are.
- (2026-09-15) Who needs help has three filters: Everyone, Needs a look (anyone not on track), and Missed a quick check (with the count), from `missedQuickChecks` on each class-view row. Grade 2 gained Maps of our town under the early-years rule.

- (2026-09-15) Who needs help prints: a Print this list button at the foot, a print-only line with the device name and date, and the filters and buttons hidden on paper. The browser test now takes a real missed quick check (four fast wrong answers), confirms it opens the lesson and offers no second try, then finds the student under the Missed a quick check filter. Explanations join the run-on rule: no explanation sentence over 32 words, checked across every generator in the sweep (two history stories were trimmed to fit). Pre-K 4 gained Taking turns under the early-years rule.

- (2026-09-15) On paper, a module unlocked without mastery says which way: "Placed past" for the placement check, "Skipped by quick check" for the five-question skip, on the transcript, the report's module rows, and a legend under the transcript's print button. The report prints from a button at the top; on paper every button is just its words. docs/TEKS-CHECKLIST.md lists the 41 history standards cited by strand, ready for the knowledge-statement numbers to be filled in on a laptop. Kindergarten gained Jobs people do under the early-years rule.

- (2026-09-15) Teacher notes live on the student's log as `note` events (removed by a `note_removed` event that stays on the log), so they ride along in every backup and survive a reset like the course settings do. They show at the top of the report, newest first, and print with it; the writing box does not print.
- (2026-09-15) A tracing demonstration has Slower and Faster buttons under it (three paces: 26, 40 and 65 units a second, from the lesson's own pace), and the hint above the practice pad follows the same choice. Grade 1 gained Signs around town under the early-years rule.

- (2026-09-15) Slower and Faster on a tracing lesson now move the voice with the drawing (three quarters speed, or a little quicker) and say the line again at the new pace. Each class-view row shows the first line of the educator's latest note. The college course gained a reflection on calendars (every count starts from something somebody chose). Pre-K 3 gained Big and little, with big and little shape choices drawn at their sizes and read as "the big circle" and "the little circle".

- (2026-09-15) Every history standard now cites a verified TEKS knowledge-statement number, read from the Texas Education Agency's currently effective text (19 TAC Chapter 113, Subchapters A to C, August 2024 update). The 2022 adoption renumbered Government and Economics, so GOVT.7 to GOVT.12 and ECON.3 from the 2018 text were replaced (GOVT.6D, 7A, 8B, 12C, 10A, 9B; ECON.2A). docs/TEKS-CHECKLIST.md records the sources. The college course's CCRS section letters follow the 2009 CCRS outline and were not re-read online.
- (2026-09-15) Who needs help has a notes search box: type a word and only students whose notes contain it stay, with the matching note shown on the row. Pre-K 4 gained Big, bigger, biggest, with a middle-sized shape choice read as "the middle-sized circle". A browser step for the Slower button was attempted and dropped: the walkthrough overview's module cards could not be reached reliably from the test's state, so the pace dial is covered by the screenshot check and by hand, not by the click-through.

- (2026-09-15) The college course's CCRS codes are now by standard (I.B.1, I.B.2, I.B.3, IV.A.1, IV.D.1), read from the TEA's CCRS-to-TEKS alignment figure, and the CEE economics standard numbers were checked against the published list of twenty; all matched. The browser test has a settled way to reach a named module: `window.__eduTest.openModule(id)`, and its page now has a touch screen, so tracing lessons open in it. The Slower step is back and passes. Pre-K 3 gained Please and thank you. Every pass now ships three files: the full zip, a zip of only the files that changed since the last delivery (for drag and drop onto the repo), and the built prototype file.

- (2026-09-15) Every pre-K guideline and Head Start goal in the bank is now verified: the plans and lesson sources cite the 2022 Texas Prekindergarten Guidelines by outcome (PK3 and PK4 wording from the Children's Learning Institute's published alignment) and the ELOF goals as published. Four families of codes were wrong and are fixed: tracing had cited VI.B.1 (Science) instead of IX.B.2 (fine motor), measurement V.C.1 (Geometry) instead of V.D.1, sorting V.D.1 (Measurement) instead of V.E.1, and taking turns I.C.3 instead of I.C.4; comparing numbers is ELOF P-MATH 4, not 5. docs/TEKS-CHECKLIST.md records the sources. Kindergarten gained Voting in class (K.9C) with picture-choice vote counts.
- (2026-09-15) tools/changes-zip.sh builds the delivery: the full zip, the changes-only zip with a CHANGES.md inside (the decisions added since the previous zip as a ready commit message, then the file list), and the prototype file. The changes zip is computed against the previous delivered zip, never remembered.

- (2026-09-15) A plan never repeats a code: the coverage test refuses it, and the nine repeats it found (pre-K sorting and shapes, kindergarten counting and shapes) were merged so each code lists every module that satisfies it once. Pre-K II.A.1 confirmed against the TEA's 2015-to-2022 side-by-side outcomes and reworded to the 2022 text. The changes zip now carries a screenshots/ folder with a phone-width picture of every module that is new since the previous zip (tools/shots.mjs, opened by id through the test hook inside a walkthrough of the module's own band). Grade 2 gained Services in our town (2.8B).

- (2026-09-15) The transcript prints "Satisfies <codes>" under every mastered module, in the state's framework, now that the codes are trustworthy. Grade 7 carries all eight TEKS 7.1B significant dates (1876, the current Texas constitution, was the missing one) and a test holds it to that. Pre-K lesson sources now use the 2022 wording at the module's own level, PK3 or PK4. Pre-K 4 gained Helpers all around (VII.B.3).

- (2026-09-15) Where the TEKS name points of reference, the course asks about every one of them and a test holds it there: grade 7 (7.1B, eight dates), grade 8 (8.1B: 1607, 1620, 1776, 1787, 1803, 1861 to 1865) and US history since 1877 (11.2B: 1898, 1914 to 1918, 1929, 1939 to 1945, 1957, 1968, 1969, 1991, 2001, 2008), each dated question with its story. The report's module rows now print "Satisfies <codes>" under a mastered module, the same line as the transcript. Grade 1 gained Symbols of our country (1.13A).

- (2026-09-15) Grade 10 asks about a dated turn in every one of the TEKS world history periods (river valleys, classical, post-classical, connecting hemispheres, revolutions, the twentieth century), with stories, and a test holds it there. History courses whose standards name dates to know (7, 8, 10, 11) show a "Dates to know" card at the top of the course on the overview, from REFERENCE_DATES in logic.mjs. The sweep now refuses any event answered with two different years anywhere in the bank (none were). Kindergarten gained Our two flags (K.9A).

- (2026-09-15) Mikey's phone review, in one pass. Quick checks: the switch had crashed under the testing pin (no saved educator, so it toggled a null profile); it now reads and writes whichever profile is live, and its card carries Mikey's own words with an i for the placed and guessing rules. Student card: on a laptop the name, grade and links stack on the left with Open report on the right, centered on them; on a phone the button sits below the links, centered. Notes: new empty-state words, examples in the placeholder, and a rename from a student ID to a nickname writes "Student ID: <id>" as the first note. Report: "tried but not passed" is a green bulleted list in title case with the subject on each line; the recommended-courses sentence names quick-check skips; writing checks say Pass and Not Yet and list the self-check vertically; the never-store paragraph is gone; "Show other courses" opens a centered Core / Electives toggle with one closed dropdown per grade, and search still overrides it. Standards wording: Common Core states no longer name the state. Transcript: in-progress modules in title case with best score and attempts, completed courses in closed grade dropdowns (open when printed), and the placed-past legend behind an i. Printing the report opens every course with each module's story and drops the other-courses section, raw data and reset. Who needs help: the filter chips are gone, the order note hides while searching notes and its tie rule sits behind an i, and each row speaks in sentences with module names in bold and title case. The writing question shows a Requirements link that opens the checklist in a popup.

- (2026-09-15) The quick-checks switch is its own stored setting (`edusphere_v1_quickchecks`), not a field on the educator profile. That is why it would not move under the testing pin: with no profile saved, the toggle was writing to nothing. Its card carries Mikey's wording, with the longer explanation and the note behind the i.
- (2026-09-15) One rectangular two-piece toggle (`SegToggle`) is used for Core / Electives on the assign page, Core / Electives on the Standards map, and By grade / By subject on the map: equal halves, square corners, butted together, full width on a phone.
- (2026-09-15) Art is the first elective subject: Looking and making (grade 3) and Color, shape and story (grade 4), three modules each, marked `elective: true`, cited against the Texas fine arts TEKS (§117.111 and §117.114, Adopted 2013) by knowledge statement and the National Core Arts Standards anchor codes. They exist so the Core / Electives toggles have something real to show, and because an elective is what the flag was built for.
- (2026-09-15) More of Mikey's phone review: the report's closing lines centered one sentence per line; the note box spans the card with Save note centered below on a phone and the explanation under it, centered; the writing check names the student and is centered; "Switching a course off" centered; the notes search placeholder centered; the student card's name, grade and links now share one left edge beside the picture. The print check now reads the PDF, because the DOM's textContent shows print-hidden text too.

- (2026-09-15) On a phone a student's picture sits above their name, so every name, grade and link starts at the card's left edge whether or not there is a picture; on a laptop the picture returns beside them and nothing else changes. Under the note box the visible line is the search hint, with "A note here stays with the student and can be restored through backups" behind an i.

- (2026-09-15) On a phone every section title is centered (the classroom's Active and Inactive folds, and the report's Notes, Summary, Assigned Now, Progress by course, Transcript, Key Words and Raw data), with a fold's count and chevron pinned to the right edge; on a laptop they stay left. The note box is taller on a phone, so the example in the placeholder fits. Electives are never part of the recommended set: they exist to be chosen under "other courses", which is what the Core / Electives toggle is for.

- (2026-09-15) A saved note sits on its own soft green card, centered, with Edit and Remove beneath it. An edit is the old note removed and the new one written in a single save, so the log still records everything that happened and the report shows one note; an edited note carries the date it was rewritten. The automatic "Student ID" note looks the same as any other, and can be edited or removed.

- (2026-09-15) The student report reads centered at every width: titles, closing lines, the note box and the course search, with a fold's count and chevron still pinned to its right edge. The notes i sits beside the Notes title and carries both sentences (what a note survives, and where to search notes), so nothing sits under the box.

- (2026-09-15) Renaming a student no longer writes a "Student ID" note; a note is only ever something the educator wrote. The Assigned Now card reads centered, course rows included, with Show other courses centered under them, and the summary's "has mastered" line and its bulleted list are centered with the bullets sitting inside.

- (2026-09-15) The page declares `color-scheme: light` and paints its own inputs: a phone in dark mode had been repainting every field dark gray, which is what Mikey saw on the PIN, the student ID and the note box. A browser check now reads the computed background and text color of a field so it cannot happen again. Course rows and the green bulleted lists read left again; on a phone a student's picture, name and grade center on the card, with the links still left and Open report centered below.

- (2026-09-15) Quick checks uses the same two-piece rectangle as the other toggles, On beside Off. "Tried but not passed yet:" is bold, the writing card's heading and each assignment's title are centered, and the note box is only tall on a phone.

- (2026-09-15) On a phone each name on the login screen carries a quieter second line naming the band the student is working in: Early years, Elementary, Middle school, High school or College (`bandTitle` in logic.mjs). It only moves up when every assigned module in the bands below is mastered, so a name never claims a stage that is not finished; a band with nothing assigned is skipped, so a grade 3 start reads Elementary at once. The two lines stand about as tall as the picture, and from a tablet up the line is left off.

- (2026-09-15) Every name on the login screen has the same three slots: a picture slot on the left, the name and band centered in the middle, and a matching empty slot on the right. Names now line up down the grid whether or not a student has a picture, and a wrapped name no longer shifts against a short one.

- (2026-09-15) Coloring is play, not work: a fold on the student's overview with a title that cycles through the crayon colors, one picture from the start and another for every course finished (`coloringUnlocked`, capped at eight). Tapping a part fills it with the chosen crayon; nothing is scored, nothing is recorded, and a coloring picture is never a module, so it touches no standard, no transcript and no memory check.
- (2026-09-15) In a read-aloud course, tapping a word answer says the word, so a child who cannot read yet can hear what they picked. The speak button sits inside the question card with room reserved above the prompt, instead of straddling the card's corner. The login grid rows grow with their content again, so a long name and its band no longer clip.

- (2026-09-16) Coloring gained a five-minute break bar: the strip empties while they color and the courses come back when it runs out, and they can leave earlier with the button or start the picture over as often as they like. Half the pictures are filled by tapping a part and half are drawn on freely with a finger (`COLORING_MODE`), so both kinds of hand are practised. The fold wears the crayons: the whole strip drifts through them with two sparkles crossing corner to corner.
- (2026-09-16) The Slower and Faster buttons on tracing lessons are gone at Mikey's request; each lesson keeps its own pace (pre-K slow, grade 1 quicker).
- (2026-09-16) The coloring fold stays rather than becoming an automatic pop-up: a student choosing which picture to color is part of the reward, and the break already ends itself. If it should also appear on its own after a module is mastered, that hooks the same screen from the results page.

- (2026-09-16) Connect the dots must be joined in order: `traceMatches` now checks that a dots picture reaches dot 1 before dot 2 and so on. Covering the same points out of sequence used to pass.
- (2026-09-16) Let's Color, rebuilt: twenty pictures ordered simplest first (a two-part ball for a three-year-old, a twenty-part city by the end of grade 2) plus a page that outlines the student's own name to draw over. One more picture is earned for every module passed, not every course. The fold is wordless: a grid of black-and-white drawings, locked ones behind a padlock, available ones breathing, and a picture that has had its five minutes rests for fifteen while its square fills with color. The five minutes and the rest are kept on the device (never in the record) and pick up where they stopped, so leaving and coming back does not hand back a fresh five minutes. The buttons are stacked, the canvas fits the screen, a picture opens at the top of the page, and a tap-to-fill picture still lets the page scroll.
- (2026-09-16) A closed subject keeps breathing while another fold is open; it used to stop as soon as anything else was opened.

- (2026-09-16) A pre-K student's screen is grouped by the skill being practised (Colors, Shapes, Sizes, Matching, Patterns, Counting, Comparing, Listening, Letters, Drawing, Getting along) instead of by subject: a three-year-old knows what colors are, not what mathematics is. Every pre-K module carries a `skill`, and `SKILL_ORDER` sets the order. This is a display grouping on the student's overview only — courses, subjects, standards, transcripts and reports are untouched, which is why it needed no changes there. From kindergarten up the screen goes back to subjects.
- (2026-09-16) A star now earns a coloring break without hunting for it: the results screen sends the student to the first picture that is ready, and the break ends itself. A break is noted on the student's log (`colored`) so an educator can see where the time went; the report says how many breaks were taken, and it is never a score, never progress and never on the transcript. Four more intricate pictures (garden, playground, farm, birthday) bring the set to twenty-four.

- (2026-09-16) The coloring pictures were redrawn after Mikey called them horrible: the beach ball is six slices of a circle rather than boxes across it, the sun's rays stand square on the rim, and the flower, garden, playground, farm, dinosaur and balloon were rebuilt. Pictures can now use curves (`{ t: 'path' }`), and tools/… a one-page sheet of every picture is rendered from the source when judging the art.
- (2026-09-16) A young learner never sees a module that is waiting for another day: passing it once takes it off the screen until tomorrow, when it returns saying "Passed once. Pass again to master." A skill or subject with nothing left to do today drops off the screen entirely. `waitingForAnotherDay` in logic.mjs decides it, so the rule is tested rather than guessed.

- (2026-09-16) Coloring belongs to the early years only: the fold is gone once a student has mastered their pre-K to grade 2 work, and the break after a star is offered only for a module in that band. Middle school and above never see it.

- (2026-09-16) The class view was printing "Next up: [object Object]": the row was title-casing the module itself rather than its title. Named properly now, with a test that no row ever stringifies an object. The reasons, the practice line and the note are spaced apart, with a hairline above the practice line.

- (2026-09-16) Who needs help names each student's stage under their name (Early years, Elementary, Middle school, High school, College), smaller and quieter, the same label the login screen shows. It is a separate field from the row's attention band, which still reads needs help now / keep an eye on / on track.
- (2026-09-16) A coloring picture keeps its colors while its five minutes last, so leaving and coming back finds the work as it was; when the five minutes run out the picture rests and starts blank next time. The square behind the drawing can be colored too. A picture ready to color now wears a pale drift of the crayon colors behind its black-and-white drawing, the same idea as the Let's Color strip but quiet enough to keep the outline readable.

- (2026-09-16) The grade line under a name on the login screen shows at every width, not only on a phone. The card behind the coloring pictures wears the crayon wash, with the picture squares white so the black-and-white drawings still read against it; a locked square is translucent instead.
- (2026-09-16) Breathing cards keep a common beat. A CSS animation starts with its element, so a fold that was opened and closed came back out of step with the others; every breathing card now carries a negative animation delay measured from when the page opened, which drops it straight back into the same rhythm.

- (2026-09-16) The name page lays a name out instead of stretching it: words (and hyphenated parts) are packed into up to three lines of about twelve characters, and the letters are sized to fit the square, so "Michael-Rafael-Angelo" reads on three lines and "Jo" fills the page. It is never squeezed sideways.
- (2026-09-16) A drawing picture has three nibs: thin, medium and thick, shown as dots of that very size in the color chosen, with thin as the default. The old single line was too fat for a name page.

- (2026-09-16) The coloring page closes with a bold X at the top right instead of a worded button: clearer for a child who cannot read, and it keeps the picture, the palette and Start over on one screen without scrolling. The same X is used at every width.
- (2026-09-16) The name page uses the name the educator gave the student, not their school ID, so renaming a student renames their picture; the grid's thumbnail follows too.

- (2026-09-16) Breathing cards share one beat for good: the animation never stops now, and a card that is open simply turns it down with a `--beat` variable inside the keyframes. Stopping and restarting was what put a reopened card on its own clock; measured with `document.getAnimations()`, every card now reads the same phase before and after a fold is opened.
- (2026-09-16) A read-aloud answer speaks only when it is a word. A picture answer used to be read out, so "tap the middle-sized one" announced "the middle-sized square" and gave the answer away.
- (2026-09-16) The coloring page carries Start over and Close as icons on the picture's own width, one at each end of the countdown bar, and the sentence under the picture is gone, which lifts the palette and the nibs onto the first screen. The name page drops anything in brackets (an educator's note, not the child's name) and outlines the letters behind the fill, so they no longer look scratched over. "Littlest" is "smallest" everywhere.

- (2026-09-16) A read-aloud answer is spoken only when it holds no colon, which is what marks a picture choice. The earlier test missed `item:triangle-blue`, so tapping a picture announced "item triangle blue".
- (2026-09-16) Fifteen crayons, five to a row. A resting picture goes grey behind a ring that empties over the fifteen minutes, rather than filling slowly with color, so it is plain at a glance that it is having a break.

- (2026-09-16) On a laptop a student's picture sits to the right of their name rather than to its left, so every name, grade and link starts at the same edge whether or not the student has one. On a phone it still sits above the name, centered.

- (2026-09-16) A coloring picture zooms: a minus in its bottom-left corner and a plus in its bottom-right, white with a black edge and laid over the picture so color goes behind them and never onto them. Four steps, always about the middle, so no arrows are needed; the drawing keeps its own coordinates, so color put on close up sits exactly where it belongs when they zoom back out. A fourth, finer nib was added for a stylus, and the grid thumbnails now sit square in their tiles.

- (2026-09-16) Zoom stops at 2.2, not 3.6: the deepest step filled the square with one region and lost the picture. The crayons are a little smaller on a phone so all three rows fit under the picture, and keep their old size from tablet width up.

- (2026-09-16) A resting picture is covered in flat grey that sweeps away like a clock hand as the fifteen minutes run down, so the drawing comes back into view rather than a ring counting at it. No green, no dimming of the picture itself.

- (2026-09-16) Zoomed in, a small arrow appears on each edge that still has picture beyond it, and on no other edge, so a student can reach the corners without a permanent set of controls cluttering the square. Zooming keeps the middle of what they were looking at. The colorable background now extends well past the picture, so a filled background covers the square at any zoom.

- (2026-09-16) A star no longer drops a student into a coloring picture. It pulled them out of a run of work, it always opened the same first picture, and it handed back whatever they had already colored on it. Pictures wait in Let's Color, where the student chooses one.

- (2026-09-16) Every nib is finer: the old second-thickest is now the thickest and the thinnest is finer than anything offered before. On a laptop the four nibs sit beside the crayons, two at each side, finest top-left and thickest bottom-right, so nothing needs scrolling; on a phone they stay in their row beneath the colors.

- (2026-09-16) On a drawing picture the outline is laid over the ink, so coloring never buries the lines a student is trying to stay inside. The name page does the same with its letters.
- (2026-09-16) On a wide screen the picture, the countdown bar and the crayons share one width in the same grid column, and the nibs stand clear to their left, ending level with the top of the first row of colors. A drawing picture's shapes are filled without their own outline and the outline is drawn once over the ink, so the lines stay single width rather than doubling.
- (2026-09-16) A wider screen gets six more crayons, twenty-one in three rows running the width of the picture, and the nibs stand in a column beside the picture with two more thicknesses, six in all. A phone keeps fifteen crayons and four nibs in the arrangement Mikey picked. The extras are hidden by class rather than by counting, so both layouts come from the same markup.

- (2026-09-16) The pictures were redrawn with curves instead of stacked shapes, and given scenes rather than single objects: the tree has a canopy, a trunk that flares at the ground, four apples, a cloud, a bird and a sun; the fish has fins, a smile, bubbles, weed and sand; the house has a chimney with smoke, a door knob and a path; the balloon has a second balloon and a curling string; the flower has real petals and leaves; the garden has three flowering plants, a cloud and a sun. More to color, and it looks like the thing it is.

- (2026-09-16) Every remaining picture was redrawn the same way: curved silhouettes and a small scene around the subject (a car on a road under a cloud, a train with smoke, a castle with flags and battlements, a farm with a silo and a sheep, a city under a moon and stars). A part can now be a line rather than a filled region (`line: true`), which is what a balloon string, an antenna or a swing rope needs; filling them left white blobs where the color should have flowed past. From here, new pictures follow the same rule: curves, a few extra things to color, and lines drawn as lines.

- (2026-09-16) A phone shows five nibs, laid out in the same five columns as the crayons and at the same size as them, so the two rows line up under the picture instead of drifting. A wide screen keeps its column of six at the larger size.

- (2026-09-16) The nib row on a phone was left-aligned because an old rule from the earlier paint-box layout was still in the stylesheet and won over the new one. The dead rules are gone and the row centres itself on the picture's width, whether or not it happens to line up with the colors above.

- (2026-09-16) The name page puts air between its letters (letter spacing of about a tenth of the size, counted in the width that decides the size) and thins the outline slightly. Sandbox fonts set the glyphs apart, but the phone's does not, so without the spacing the outlines of neighbouring letters crossed each other on Mikey's screen and not on mine.

- (2026-09-16) The doubled name was two text elements, the fill and the outline, laid out separately: they matched in the sandbox and came out at different widths on Mikey's phone, so the outline drifted across the letters. The name is laid out once now and the outline is a `<use>` copy of that very element, which cannot measure differently. Lesson: never draw the same text twice and expect the two to line up.

- (2026-09-16) The name is now a single outline element drawn last: no white fill underneath, no copy of any kind. Two text elements could drift on a device whose font measures differently from the sandbox's, and a `<use>` copy did not settle it either, so the safest thing is to draw the letters once. Their insides take whatever color the background is given, which is fine and lets a child color inside them.

- (2026-09-16) The login screen carries a build stamp under Contact us, written by build.sh at build time. Three passes were spent on a name-drawing fault that the sandbox never reproduced; a stamp settles in one glance whether the page on a device is the one that was just published.

- (2026-09-16) The name page is back to the drawing Mikey approved: one text element, white letters with their outline painted behind them, no spacing of our own, drawn under the ink rather than over it. Everything tried in between — a second outline element, a `<use>` copy, letter spacing, outline only — made it worse on his devices and looked identical on mine. The build stamp moved to the very foot of the page, under the line about the device.

- (2026-09-16) The coloring page is sized by the height that is really there (`dvh`), so a phone held upright shows the picture, the crayons and the nibs without scrolling. Turned on its side, where there is width to spare and no height, the nibs stand at one side of the picture and the crayons at the other in four columns, and the picture takes nearly the whole height. Those rules sit after the wide-screen ones, because a phone on its side is also a wide screen and would otherwise take the laptop layout.

- (2026-09-16) The coloring page is sized to the window rather than to the reading column: it may use the full width, drops the page's zoom and bottom padding, and the picture takes the height that is left once the title, the bar and the three rows of crayons are accounted for. Measured on a phone upright and sideways, a tablet both ways and a laptop: nothing runs off the bottom at any of them, and the picture is half as large again on a laptop as it was. The title is one line whatever the width, and when a phone is sideways the title bar runs the full width so the restart and close icons sit at the outer edges.

- (2026-09-16) The build stamp reads in Central time, since that is where it is read.
- (2026-09-16) A phone on its side splits down the middle: the title, the bar and the picture take the left, the nibs and the crayons the right. On a laptop or tablet the nibs sit level with the middle of the picture rather than its foot.

- (2026-09-16) The countdown is one shape everywhere: a bar across the top. Standing it upright beside the picture on a sideways phone made it too thin to read, and the title ended up beside the picture rather than above it. Sideways now reads the same way as everywhere else: title across the top with its two icons at the edges, the bar beneath it, then the picture, the nibs and the crayons in one row.
- (2026-09-16) A picture that is filled in rather than drawn on has no nibs, so it shows all twenty crayons in four rows and the paper runs a little below the drawing, which puts the zoom buttons under the picture instead of over it. A drawing picture keeps its square: the hand needs the room.

- (2026-09-16) The coloring page stopped using `dvh` units and `:has()`; a browser a version or two behind ignores both, and when the height rule was ignored the picture fell back to the laptop size and ran off a phone held sideways. Plain `vh` and a class on the wrapper do the same job everywhere. Sideways, the crayons, nibs and zoom buttons are smaller again, and a filled picture (taller than it is wide) takes less width so its height still fits.
- (2026-09-16) The picture's own edge is clipped round, so a filled background no longer paints over the card's border and corners.

- (2026-09-16) A phone held sideways is asked to turn upright rather than shown a squeezed layout, and every sideways rule is gone. The ask only appears on a screen that is short, wide and touched, so tablets and laptops never see it. This is Mikey's call: the app is designed upright, and chasing a sideways phone was costing more than it was worth.
- (2026-09-16) The twenty-first crayon is back, so a wide screen shows three full rows of seven; a phone still shows fifteen, or twenty on a picture that is filled in. The close and start-over icons sit slightly outside the picture's width. On the name page the letters' edge is copied over the ink with `<use>`, so coloring no longer buries it, exactly as it already worked on the drawn pictures.

- (2026-09-16) The name's border stays prominent without a second copy and without fading the crayon: the ink is drawn through a mask that marks the letters' line as out of bounds, so color fills the letters and the paper around them at full strength and stops at the edge instead of running over it. The mask holds a copy of the letters, but it is never drawn, so nothing can read as doubled. The mask protects only the half of the line that is actually visible — the letters are filled inside it, as they are on the page — so the color runs right up to the black edge instead of leaving a white gap beside it.
- (2026-09-16) The doubled letters were never two copies. A line drawn on a letter's outline has two sides; the white fill hides the inner one, so each letter shows a single edge. Take the fill away, or lay a second unfilled copy on top, and both sides show at once, which reads as every letter drawn twice. That explains each attempt that failed, on Mikey's screen and on any screen large enough to see it. The name is white letters with their outline painted behind them, under the ink, and that is where it stays: the edge cannot sit over the ink without losing the fill that makes the letters read properly.
- (2026-09-16) The name is one text element and nothing else: white letters with their outline behind them, drawn under the ink. Three different ways of laying a second copy of the edge over the ink — a second text, a `<use>` of that text, a `<use>` of a plain definition — all rendered cleanly here and came out misaligned on Mikey's device. A crayon crossing a letter therefore covers the line at that point, exactly as it would on paper. If the edge must always show, the alternative is letters with no white fill at all, which colors their insides with whatever the background is given.
- (2026-09-20) A lesson picture follows the logic of the sentence over it. "Red and blue are colors" shows red beside blue, "They are the same" shows two red circles side by side, "Three dots is more than one" shows three beside one. The pair picture (`kind: 'pair'`, halves of any kind) is how a line about two things is drawn, and the lesson-picture test checks each half against the words.
- (2026-09-20) Pre-K is finished before the screen changes shape. The skill folds stay, and a kindergarten course that has been switched on waits out of sight, until every pre-K module is mastered. Before this, finishing pre-K 4 math switched on kindergarten math and the pre-K screen fell back to subject folds. Each skill fold carries a small drawn picture, so a pre-reader can tell Colors from Counting without the word.
- (2026-09-20) A young learner whose screen has emptied for the day sees "Well done! Come back tomorrow." with a speaker, instead of a blank page. A student who came up through the early years never meets a placement check: when the last early module is mastered, any subject that would ask starts at the beginning by itself.
- (2026-09-20) Backups: a file goes to downloads when a student taps Exit (only if something changed), on the first sign-in of each day, and when a course is finished. A browser cannot save a file as it closes, so Exit is the last chance, and nothing is lost either way because every step is already in the browser's own storage. Chrome and Edge on a computer can keep one chosen file rewritten after every practice round for the rest of the visit (`keepOneFile`); the handle is held in memory, never stored, so no new storage call was added. Automatic backups now set the last-backup date.
- (2026-09-20) Student PIN, optional, four digits, chosen by the educator in the add-student popup or from the student's card, asked for on the sign-in screen when that name is tapped. It is a mix-up guard on a shared screen, not a lock on the device, and it is stored on the roster as typed by the educator. Wonder questions can be switched off per student (default on) in the same two places; approval still gates every question, so the switch can never show anything unapproved.
- (2026-09-20) The phone's back button moves up one level inside the app (lesson or picture to courses, courses to sign-in, an educator page to the educator's list) using one guard entry on the browser history; from the sign-in screen, back leaves the app. Speech is scheduled with a ticket: cancel, then speak a beat later, retry once if nothing started; a newer request cancels an older one, which is what stopped a dropped or late line when moving quickly.
- (2026-09-20) Coloring: the line thickness starts in the middle (2.2); a wide screen has four erasers in a column to the right of the picture (paper-colored strokes, so the outline drawn on top is never lost); the zoom-out button pulses while zoomed in; fill pictures on a phone take 50vh with four tighter rows of crayons so nothing scrolls off; the dot-and-line page background shows from 700px, so tablets have it and phones do not. The Let's Color fold has a plain arrow on the right like every other fold, and an educator's walk-through sees every picture unlocked.
- (2026-09-20) My Classroom lists the youngest band first, then by name, and an educator can press and hold a card to drag it into their own order (kept as `order` on the roster). The standards map folds each grade (or subject) behind one counted row. The module story calls a pass a pass and states mastery once, with the date it was earned.
- (2026-09-20) Art electives (grade 3 §117.111, grade 4 §117.114): the strand numbers and anchor codes match the published lists, but each grade maps three of the four TEKS strands (grade 3 lacks 3.3 historical and cultural relevance; grade 4 lacks 4.4 critical evaluation). Left as is pending a fourth module per grade; not a wrong mapping, an incomplete one.
- (2026-09-20) A question whose words repeat the one before it is still spoken: the speech effect keys on the question's position as well as its text. This was the "audio sometimes does not start" report: two questions in a row can draw the same picture and the same sentence, and the old effect saw nothing new to say.
- (2026-09-20) The backup page counts automatic backups: "Last backup today" rather than "Last manual backup", because sign-in and sign-out backups now set the same date.
- (2026-09-20) Pre-K 4 reading gained More big letters (order 5, tracing, stylus): the standing early-years module for this pass.
- (2026-09-20, evening) The student card's Save PIN and Wonder switch crashed the whole page (blank, then "Loading EduSphere"): the add-student popup's own state was named `setStudentPin` and `setStudentWonder`, the same names as the two roster functions, and inside the app the state setters won. The popup's state is now `wizardPin` and `wizardWonder`; the browser test now saves a PIN and flips Wonder Questions from the card.
- (2026-09-20, evening) The "turn your phone upright" ask is decided by the screen's real orientation (`screen.orientation`), never by the viewport: a keyboard shortens the viewport in portrait and used to trigger it on the educator PIN page.
- (2026-09-20, evening) Backups run on exactly the three triggers the page lists: a student completes a course, an educator makes changes and signs out, a student taps Exit. The first-sign-in-of-the-day backup and the Chrome/Edge "keep one file" handle are gone (unreliable). One green Manual backup button replaces Download and Share: it opens the device's share sheet where files can be shared, otherwise it downloads.
- (2026-09-20, evening) Add-student popup centered with air: PIN (optional) and Wonder Questions each under a heading with an i, the switch on its own full-width row. On the card, Wonder Questions stands on its own last line with an ON/OFF pill and opens a popup (Currently: On/Off, the switch, Close); links are centered and evenly spaced on a phone. Wording on the backup page, the classroom backup tip and the Wonder review page rewritten to Mikey's text of 2026-09-20.
- (2026-09-20, night) Backup page reordered to Mikey's text: Automatic Backups first ("when:" with three real bullets, the last automatic save, then the i), Manual Backups below with its own three-line case and the one green button, then the device name, then Restore, and the privacy line at the very foot ("where the file lives"). Last backup line in bold; classroom backup tip centered; the Wonder review note about faith sits well below the list.
- (2026-09-20, night) Installable app, no store: `manifest.webmanifest` and `icons/` (rendered from docs/logo.svg by a script, the maskable one padded for round crops) are committed; `tools/build-site.mjs` now also writes `sw.js`, stamped per build, which asks the network first for the page and keeps a copy for offline, and the page registers it only when served over http(s), never from a file or another host's viewer. On an iPad: Share, Add to Home Screen; on Chrome or Edge: the install button in the address bar.
- (2026-09-20, night) The loading screen is the logo drawing itself forward and back (the same `edu-draw` strokes, looped and alternating), both before React mounts (a boot copy of docs/logo.svg in the page shell that shows only while #root is empty) and on the app's own loading screen.
- (2026-09-20, night) Drawing makes a paper sound: noise shaped by a band-pass filter, synthesized on the device by Web Audio, louder as the hand moves faster, started by the touch itself and silent when it stops. No sound file, nothing downloaded. A mouse becomes a paintbrush cursor over a drawing picture only. Pre-K 4 reading gained Trace L, T and F (order 6, tracing): the standing early-years module for this pass.
- (2026-09-21) Let's Play: a second fold in Let's Color's clothes, for games that need no words. One game to start and one more for every course mastered (`gamesUnlocked`), in a fixed order (`GAMES`); a game keeps the coloring clock (five minutes, fifteen resting) by sharing the coloring state under `play:<id>`, and a game writes no event. Kinds: join the dots (star, house, boat, kite, and the student's own name from TRACE_LETTERS), pairs, sorting into two boxes by size or color (drag), a maze carved by a seeded random walk (drag the dot), a jigsaw of one scene (tap two pieces to swap), and Pong. Harder ones wait for a student whose courses reach K, 1 or 2 (`minGrade`). The walk-through sees them all. Rounds are seeded so "play again" is a new shuffle everywhere.
- (2026-09-21) Letter pages: twenty-six coloring pages, the capital beside its lowercase with five circles around them, filled by tapping (a text glyph is a part like any other). They take turns with the drawings in COLORING_PICTURES so the alphabet arrives a letter at a time; the page says "Capital B. Lowercase B." on opening and the speaker in its header says it again. Kindergarten reading gained Trace V, A and N (order 12, tracing): the standing early-years module for this pass.
- (2026-09-21, later) Themed matching (pass D, first three): Match the animals (pre-K 3, the sign-in animals), Match the vehicles and Match the things (pre-K 4, coloring outlines). One pair of generators serves every theme (`themedSame`, `themedDifferent`, given a theme from `MATCH_THEMES`); the explanation picture is the pair the words describe, so "Both are a fox" shows two foxes and "The owl is different from the fox" shows the owl beside the fox. Choices `pic:<animal>` and `art:<picture>` draw what the app already draws. More themes need more drawings; those come through the art ledger, not by hand.
- (2026-09-21, later) Fingerprint or face for educators, through the browser's passkey door (WebAuthn) with the device's own authenticator: the id is kept on the educator account (`educator.biometric`), enrolled and switched off on the Backup page, offered on the PIN screen. No server verifies anything; the device's own yes is the key, the PIN stays as the other one, and it only appears on a secure page (the site or the installed app).
- (2026-09-21, later) Report: Notes heading centered with its tip in plain sight; the example text sits lightly in the middle of the box until there is a note; an i beside the recommendations line explains why kindergarten courses wait for pre-K; Reset sits further down. Let's Play sits beneath Let's Color. A student keeps every picture and game they unlocked in pre-K on the kindergarten screen: both counts come from the event log alone, and both folds stay through grade 2.
- (2026-09-21, night) Story-based learning (pass E, the frame and the first thirteen): stories live in `src/stories.mjs` (bundled by build.sh), one per module id, written ahead because the app is static; the lesson shows a "Story-based learning" fold under the key idea with the story, a speaker, and its illustration from `art/stories/<serial>.webp`, or a dashed placeholder wearing the serial until Mikey makes the picture (no placeholders on coloring pages, by his choice). The method is the Miniature Gladwell arc in `.claude/skills/story-based-learning/SKILL.md`; the cast, the clock (a band is a chapter, characters age faster than the learner), and the rules (wholesome-only for Savanah, Jaxon and Harlow; Georgette is the godmother of Mike's uncle; at most one new core character per story; most stories belong to someone else or no one) are in `docs/CHARACTERS.md`; every picture has an S or C serial and a Leonardo prompt in `docs/ART-REQUESTS.md`. `tests/stories.test.mjs` (check step 5) holds the word limits (200 early years, 350 older), the serials and the cast names.
- (2026-09-21, night) The periodic table is drawn by the app (`PeriodicPic`): the lesson's example is the table with the alkali column lit, and every periodic-table question shows the table with the element, period or group it names lit up, so bromine is never asked about unseen. Anything with writing on it (tables, graphs, clock faces) is drawn in code, never requested as art. Grade 1 reading gained Trace i, t and k (order 7): the standing early-years module for this pass.
- (2026-09-21, pass F) Lessons whose picture has structure now get a diagram drawn in code instead of a word card: the water cycle, photosynthesis, the 3-4-5 triangle with its squares, a plant cell, two carts under one push, supply and demand curves, a carbon atom, a number line from minus five to five, and the sun lighting one side of the turning Earth (kindergarten, as a read-aloud line). `DIAGRAMS` in ui.jsx maps a kind to its drawing; a lesson example is `{ kind: 'cycle', caption }`. Anything with labels is drawn this way, never requested as art.
- (2026-09-21, pass F) Ten more stories (S14 to S24; the Texas Revolution carries two pictures), and ages and family ties came out of every story's text: the reader gets the person, the illustrator gets the age in the prompt, the bible keeps the ties. `docs/LEONARDO-BRIEF.md` is the standing brief for keeping the cast consistent (sheets first, character reference for every scene, age by addition, Elements if offered). Pre-K 4 math gained More and fewer up to five (order 13, pair pictures): the standing early-years module.
- (2026-09-21, pass G) Questions that name a thing now show it, the way periodic-table questions light their element: a cell question lights the part it asks about (wall, membrane, nucleus, chloroplast, mitochondrion, vacuole), a percent question shows the hundred-square grid, a ratio or what-percent question shows the two groups as dots, an angle question draws the crossing, the straight line or the triangle with the given angles, a mirror question draws the ray in and out, an eye-color question shows the two versions, a molarity question shows the beaker. Six more lessons trade their word card for a diagram (percents, ratios, angle relationships, genes and traits, light, concentration). `DIAGRAMS` drawings now take the visual's own fields as props.
- (2026-09-21, pass G) After a story, one wonder question: the fold offers an approved wonder question chosen for the course when the student's Wonder switch is on, read aloud with the story, never stored. Four more stories (S25 to S28: percents, genes and traits, light with Chloe, habitats). Grade 1 reading gained Trace c, v and x (order 8): the standing early-years module.
- (2026-09-22) Graduation certificates: a grade counts as finished when every module of every course the student was given in that grade is mastered, placed past, or passed on its quick check (`gradeCompleted`, `completedGrades`); courses never switched on are not required. The offer appears on the student's card in My Classroom ("Finished Kindergarten! Make a certificate / Not now") until it is made or skipped, remembered on the roster (`certificates`), and the report keeps a Certificates card for any finished grade forever, so nothing is a one-time chance. The screen offers four templates drawn in code (Rosette, Stars with one photo, Balloons with two, Gallery with three), a name box, and photo slots; Print prints only the sheet, sideways; Save picture draws the sheet onto a canvas and downloads a PNG (Share stays off, as on the backup page). The name and the photos exist only on the sheet in memory: EduSphere never stores either, which keeps the no-names, no-photos rule for everything the app keeps. Mikey's example images did not come through, so the templates follow his description.
- (2026-09-22) Old backups: the v1 fixture now also runs every derivation the screens use on the restored record, and restores into a classroom that already carries PINs, Wonder switches, drag order and certificates, keeping those fields (`setClassroomOrder` normalizes ids like every other roster helper). Pictures are not recycled: no two lessons may share an identical example picture (test in logic.test.mjs; dots, swatches and traced strokes are the concept itself and may repeat); eighteen lessons got their own parameters or icons. The where lines came out of the stories. docs/DIAGRAM-AUDIT.md lists every remaining word-card lesson with a decision.
- (2026-09-22, pass I) The math track's word cards are gone: every math lesson that showed "3 4 5" or "x + 5 = 12" now shows a drawing with its own parameters (equal groups, a fraction of a fraction, order of operations, a cuboid of unit cubes, pieces, a balance, a circle with its diameter, powers, growth bars, plots for lines, slope, parabola, shift, cubic, exponential and decay, algebra tiles, a sector, a trig triangle, similar triangles, a reflection, the unit circle, a scatter, dot plots, a spinner). `PlotPic` is one graph drawer with named curves. Kindergarten math gained More and fewer to ten (order 14): the standing early-years module, and the comparing generators now name numbers by value (`NUMBER_NAMES`), fixing an off-by-one that had five dots called six.
- (2026-09-22, pass I) Certificates follow Mikey's samples: Classic (a framed, sealed certificate with signature lines, serif type), Stars (one round photo), Arches (two tall arch photos, the year down the middle, Happy GRADUATION), Class of (Class of 2026 with three photos). Card templates are portrait and print upright. No script font ships offline, so names are italic serif.
- (2026-09-22, pass I) A forgotten educator PIN can be reset with the device's fingerprint or face when that sign-in is on, beside the recovery code and the backup file.
- (2026-09-22, pass J) A drawing never replaces the words. Every lesson that traded its word card for a picture keeps those words as a bold `formula` line under the drawing, above the caption (`example.formula`), so a student reads and sees. The science track's word cards are drawn: flows and loops of labeled boxes (food chains, cycles, body loops, homeostasis), three states of matter, a thermometer, circuits (single, labeled, series and parallel), orbits, mixture and solution, density blocks, an energy pyramid, a water molecule, plate boundaries, moths on a trunk, rock layers, cell division, base pairs, homologous limbs, two-way arrows, the pH scale, reactions as boxes, gas law cylinders, ionic and covalent bonds, momentum, a wave, work, the Earth's layers, ocean currents, heat transfer, and three more graph curves. Only the science and math rows left in docs/DIAGRAM-AUDIT.md are open.
- (2026-09-22, pass J) Certificates in Spanish: one toggle on the certificate screen swaps every string on the sheet (Kínder, 2.º grado, ¡Excelente trabajo!, Generación) and the date's locale. Grade 1 reading gained Trace n, u and r (order 9): the standing early-years module.
- (2026-09-22, pass K) History's word cards are drawn: timelines (dots at true years, labels spread apart, BC counted down), maps (Texas, the United States, the world as six continents, a town grid) with lit spots, a three-branch chart with its checks, stacks for levels of government, a compass rose, the two flags, road signs, and flows and bars for civics and economics. Words stay as the formula line. Pre-K 4 sizes gained Bigger and smaller (order 14, shape pairs): the standing early-years module.
- (2026-09-22, pass K) Read aloud on demand for every grade: the speaker sits on every lesson, question, wonder question and result; only pre-K through grade 2 play on their own (the read-aloud courses), and nothing above grade 2 ever speaks unasked. There is no cutoff: the button is for any student who wants it.
- (2026-09-22, pass K) What's new: the newest block of docs/WHATS-NEW.md is baked into the page (`window.__eduNews`) and shown once per build as a pop-up the first time an educator reaches the classroom after an update, dismissed with Got it and remembered on the educator account (`newsSeen`). Nothing is added to the classroom page itself. Maintain WHATS-NEW.md with each delivery.
- (2026-09-22, pass L) Never ask what was never taught: `tools/untaught.mjs` lists every module whose questions can ask for a recall answer (a name, a term, a date word) that the lesson text never says; docs/UNTAUGHT.md is the work list (91 modules, mostly history and science facts; the reading rows are decoding tasks where the word is the skill). The periodic table lesson now names every element its questions ask about, the table carries a legend of the families, and the atom lesson counts protons to elements. Rule: a generator may only ask for a fact the lesson, its picture or its story has said.
- (2026-09-22, pass L) Letter pages are colored by hand (draw mode); the letter's audio is a sentence ("On the left is a capital B. On the right is a lower case b.") and its speaker sits under the crayons, not in the header. Every speaker stops when tapped again (Stop reading). Walk-throughs show wonder questions as if approved. Backups no longer download when a course is finished (students saw the note); the two triggers left are an educator signing out after changes and a student tapping Exit. The loading screen is a bobbing word, in the shell and in the app, instead of the logo. Pre-K 3 sizes gained Big and small (order 12): the standing early-years module.
- (2026-09-22, pass M) History asks only what it taught: all 29 flagged history lessons name the people, places, years and terms their questions can ask (paragraphs for readers, spoken lines with a picture for K to 2), docs/UNTAUGHT.md is down to 62 (science facts and reading decoding rows), and the rules test now holds two lines: history stays at zero and the list can only shrink. A wrong answer for readers shows "From the lesson:" with the sentence that taught it (`taughtLine`), null for computed answers. Letter glyphs on a drawing page are drawn by the outline layer alone, which ends the doubled letters seen on a phone.
- (2026-09-22, pass N) The doubled letters on a phone had two causes and both are closed: a finger dragging across a glyph started a text selection that painted a highlighted second copy (svg text is now unselectable and ignores the pointer), and a phone with no bold face fakes one by drawing twice, which a stroked outline shows (glyphs are drawn at normal weight with a thicker stroke). On drawing pages the ink is kept off every outline by a mask, the way the name page guards the name, so lines are drawn once and never buried. Science asks only what it taught (the last six lessons name their answers); the ratchet now covers history and science at zero and reading's decoding tasks are excluded by name. Sight words show a real drawing where one exists (ball, cat, sun, fish, boat, car, tree). A missed answer is remembered for the session and quoted in a Look again card at the top of that lesson the next time it opens. Pre-K 4 matching gained Match the water animals (order 15): the standing early-years module.
- (2026-09-22, pass O) The ink guard on letter pages hugs the outline: the guard band is the visible glyph itself (normal weight, same size) with four units of stroke against the drawn 3.6, so the clearance is a hair on each side and no white shows around the letters. Taught before asked is now a hard rule: every remaining row was settled (o'clock and half past named in telling-time, spread and the third-thing lessons, the horizon line and vanishing point, four pre-K lines spoken with pictures) and the rules test requires zero untaught recall answers in every subject; docs/UNTAUGHT.md stays as the tool's report and should read zero. Stories with a fitting drawing show it under the text while the art is missing, with the serial kept in small print (`diagram` on the story), so no story wears an empty frame when a drawing already exists. A first-week tour of five cards opens on a new educator's first classroom visit (add a student, lessons and mastery, backups, wonder questions, certificates), skippable, remembered as `tourSeen`, sitting above other pop-ups, with a link on the Backup page to show it again; the what's-new pop-up waits until the tour is done. The where lines that lingered in the first stories are gone from the file.
- (2026-09-22, pass P) The white band inside the letters was the guard's inner half: the mask blocked ink on both sides of the glyph edge while the visible line is only the outer half. The guard text now paints its white fill over its black stroke (paint-order stroke, as the name page does), so only the outer half is guarded and ink runs up to the line inside and out. Story reading log: opening a story writes `story_read` (module and time only, once per module, never in a walk-through); the report's Stories card lists the stories opened and the cast met.
- (2026-09-22, pass Q) Letters on a coloring page: the outline is drawn once over the ink at normal weight, the body under the ink is plain white, and the ink guard leaves letters alone entirely. Nothing about them depends on a mask any more, so no device can halo or bury them. A student's grade on the Who Needs Help page is the grade they are working in: the first grade, in order, among their assigned courses with a module still unfinished; once every module of that grade is mastered, placed past or quick-checked, they belong to the next (`currentGrade`), so a student is in Kindergarten until Kindergarten is complete. Stories are their own page: a lesson with a story shows View story where Practice this was, the story page carries title, pictures where they fall, the words and one speaker, and its own Practice this at the bottom; the wonder question inside stories is gone, and wonder questions run only in their own four-choice flow. Every story now carries three pictures (S29 to S82 added to the ledger with prompts and character sheets), so a story reads like a book. Questions read as one sentence with their subject inside ("What is the job of the cell wall?", "Is copper a metal or a nonmetal?"), not a bare noun followed by "What is its job?"; twenty more generators still use the old split and are listed for the next pass. Lesson captions may carry lines and bold, and explanations are drawn out in more than one way (the ratio example doubles 2 to 1 into 4 to 2 and 8 to 4). The notes box shows one placeholder, the quiet centered one.
- (2026-09-22, pass R) Coloring letters are paths, not text: the 52 outlines are baked from DejaVu Sans Bold into `LETTER_GLYPHS` by tools/glyphs.py, so a letter is the same shape on every phone, with no font, no fake bold and no autosizing to double it. The pair is laid out by real advance widths with a fixed gap and shrunk when it would exceed 84 units, so nothing overlaps or drifts apart, and the bubbles are moved out of the two bottom corners where the zoom buttons sit. The thumbnail, the page body under the ink and the outline drawn over the ink all use the same path. Every split question is gone: the last twenty generators read as one sentence with their subject inside (`threeQ` takes a prompt maker), and the sweep in the rules test reports zero. The periodic column sentence is drawn out with an example (lithium, then three spots down rubidium, one outer electron each). A story prints from a small Print link at the top right of its page; only the story prints. Pre-K 4 matching gained Match the land animals (order 16): the standing early-years module.
- (2026-09-22, pass S) Coloring letters are lighter (outlines from DejaVu Sans regular, stroke 2.8), the capital and the small letter share one baseline, and the pair sits a little above center (y 48). The tracing lessons show the letter's real shape, faint, under the guide strokes in both the demo and the pad (`TraceShape`, fitted to the guide's box). The Punnett square keeps its legend clear of the grid and words its count. "Show me another way": a lesson example may carry `another`, a second explanation of the same idea, shown by a link under the caption; twelve lessons carry one. The report has a Weekly note card, one paragraph (mastered this week, stories met, next up) with a small Print link that prints the note alone. Pre-K 4 matching gained Match the shapes (order 17): the standing early-years module.
- (2026-09-22, pass T) The weekly note says what happened: questions answered and how many right, the modules worked on, modules mastered, pictures colored, stories read, and what is next; an Edit link turns it into a box the educator can rewrite before printing (memory only, nothing stored), and it signs off "Made with EduSphere, one classroom on one device." The Notes card is a centered title with an i that holds the explanation and the example; the box shows one short placeholder. Coloring letters are half as bold (stroke 1.5) and stretched a quarter wider, and no bubble sits in the letter band. Seven word pages (cat, sun, fish, boat, car, tree, ball) show the drawing above its word spelled in the same outlines; `scaleParts` and `scalePath` move a drawing into a corner. Every math and science lesson that readers use now carries `another`, a second explanation of the same idea (128 in all).
- (2026-09-22, pass U) Another way can be a list: a lesson example's `another` holds up to three more explanations, and the link steps through them (Show me another way, one more way, the first way, with a 2 of 4 counter). Eight big ideas carry three ways; every history and reading lesson a reader meets now carries at least one (238 in all). The weekly note is a logic helper (`weeklyNote`) so the report and the class printout say the same thing; Who Needs Help has Print weekly notes, a sheet built only for the printer with every student's paragraph; the sign-off is "EDUSphere: The Smart Way to Learn." The Notes box grows with its text, centered, with no example under the i. Coloring letters are stretched half again wider (GLYPH_WIDE 1.5), pairs fit within 88 units, and the five bubbles take five even spots: four across the top, one at the bottom middle. Word pages for sixteen one-word drawings.
- (2026-09-22, pass V) Every lesson a reader meets carries at least one other way (277); the writing ladder passes `another` through from its table. On a wrong answer for readers, Show me another way opens the lesson's other explanation right under the correction. The report leads with "Since you last looked (date): mastered X; N questions answered; a story read" using `reportSeenAt` on the roster (`setReportSeen`, `changesSince`), stamped on each opening. The weekly note has one Save link while editing, Edit and Print otherwise. Who Needs Help: Print this list sits lower with more air above it and Print weekly notes beneath it. The notes box is one line tall and grows with its text. Coloring bubbles spread over the card by region: two along the top, two along the bottom between the zoom buttons, one at a side when the letters leave room; the letter chooses the spots; sizes 1.5 wide with stroke 1.5 stay as approved.
- (2026-09-22, pass W) The weekly note tells what a story was for: "Jax read 'The cart that would not start', a story about a girl and the carts on the farm. Through this story, the goal was to learn about Newton's laws." Every story carries `about`, one phrase, and STORY_TITLES carries it to logic. Overlays no longer use the `inset` shorthand (older phone browsers ignore it, which is why the note placeholder sat under its box on Mikey's phone); every overlay names its four sides. Coloring letters are stretched to 2.2 wide and a pair may fill 96 of the card's 100 units; in a square card, wider means shorter, so the height gives way when a pair is wide.
- (2026-09-22, pass X) Letter coloring pages are Leonardo art: each page is an image (art/coloring/L1.webp to L26.webp, listed by the build into `window.__eduColoringArt`) the child colors over, with the ink blended in multiply so the black lines show through; until a page exists the card wears a dashed frame with its serial and a line saying what it will be. The ledger has the 26 prompts. The baked-glyph letters stay only on the word pages. The story page reads along: the title, then each paragraph in turn, the one being read lit (`speakSequence`), with the same button stopping it. Third ways for the twenty-two ideas students miss most (26 lessons now carry three ways). The class printout lists every story a student read this week on its own line with what it was about and what it was for.
- (2026-09-22, pass Y) The Notes box uses the browser's own placeholder, centered, with no overlay to misplace. The since-you-last-looked date sits on its own row, the weekly note's sign-off is centered, and an edited weekly note keeps its blank lines (pre-wrap) after Save. Read-along for the youngest lessons: each spoken line is read sentence by sentence and the sentence being spoken is lit. tests/ledger.test.mjs, step 5b of check.sh, fails if a story picture or letter coloring page has no ledger row or the ledger has a row the app does not use, so no picture can be forgotten; it is silent when all is well. Pre-K 4 matching gained Match the solids (order 18) and kindergarten science gained Hot and cold (order 5, TEKS K.5A): the standing early-years modules. Six more third ways (28 lessons now carry three or four).
- (2026-09-22, pass Z) Let's Play tiles show each connect-the-dots game's own shape (star, house, boat, kite, and dotted letters for the name game), so four games no longer look like four copies. On a wrong answer, Show me another way brings the lesson's own picture with the other explanation, so the drawing matches the words. The since-you-last-looked line sits above Print this report. The notes box shrinks back after Save. Practice the missed ones sits centered at the foot of the student's My progress page and opens the module where the most answers went wrong this week. Pre-K 3 listening gained Animal sounds (order 5): the voice says the sound, the child taps the animal. Purchased audio hooks: the build lists audio/<key>.mp3 into `window.__eduAudio`; when every clip of a story (S7-0 for the title, S7-1 onward for paragraphs) or a lesson line (module-step-sentence) exists, the clips play instead of the device voice and the read-along lighting follows them unchanged.
- (2026-09-22, pass AA) The boat's dots were eight points with two on top of each other (dot 5 sat on dot 1, dot 8 on dot 2); it is one stroke of eight distinct points now. A spoken lesson line may not name an animal it does not show: the lesson-lines test fails on it, and Animal sounds says one animal per line with that animal's picture. Report generated sits at the very foot of the report. A way may carry its own picture (`another` entries as { text, visual }): eighteen lessons swap the drawing with the words (a thermometer at -3 for money owed and cold, a quarter-shaded circle for the pizza, a clock at half past, the spinner, the balance), in the lesson card and in the wrong-answer box alike; a way without a picture keeps the lesson's own. Kindergarten reading gained Beginning sounds (order 4): the voice says a letter, the child taps the picture whose name starts with it.
- (2026-09-22, pass AB) Dragging a student card near the top or bottom edge scrolls the page on its own, so a card can reach any row. Coloring edits: the tree's apples are spread over the crown and its bird flies high between cloud and sun; the star page's moon has its round side to the right; the castle's sun and cloud float above the middle wall, clear of the flags. Ten pages became Leonardo drawings (D1 to D10: fishbowl, fish with sea plants, cat, one flower beside three, rocket, butterfly, dinosaur, farm, birthday cake, playground), colored by hand over the image, placeholders until the art arrives; the garden page folded into the flower page; the ledger has the prompts and the ledger test covers D serials. Explanations that list things are laid out as lists: bullet lines in a caption or a way render with a hanging indent and bold labels (thirteen rewritten, ethos, pathos, logos first). The transcript blurb and the weekly note are centered; the note placeholder is light. Twenty-seven science ways carry their own pictures (48 in all). Pre-K 4 listening gained Which came first? (order 7). Two kindergarten stories, The puddle by the gate (S83 to S85) and The rock and the snail (S86 to S88), with `about` lines and ledger rows.
- (2026-09-22, pass AC) The word coloring pages are gone (their letters ran off the card); in their place twelve new Leonardo drawing pages, simplest first and busier down the list (kite, ladybug, ice cream, snowman, hot air balloon, lighthouse, treehouse, submarine, pirate ship, dragon, space station, jungle waterfall, D12 to D23), and the city is a drawing page too (D11). Sight-word questions for cat, sun, fish and tree use the sign-in pictures and icons, since those coloring drawings are placeholders now. Lesson text lines that begin with a bold label read as a list, like bullet lines. Step chains are lists (multi-step and two-step equations, order of operations, cross-multiplying, counting up, long division, carrying). The tree's six apples sit at uneven spots; the castle's sun sits left, nearly touching the flag. The weekly note puts a blank line between its sentences, on screen, in print and on the class sheet. tests/pictures.test.mjs, step 5c of check.sh, fails if any lesson, way, script or question picture names a kind the app cannot draw. Two more kindergarten stories, The bean on the windowsill (S89 to S91) and Two cups (S92 to S94).
- (2026-09-22, pass AD) The result screen's forward button sent the last module of a course round into its own practice again (there was no module with the next order); it goes home now, and the next module is the next unmastered one by order rather than exactly order plus one. Letter placeholders fill the card with no bubbles, like the drawing pages. Fifteen history lessons carry a pictured third way (timelines of the revolution, the sectional crisis, the civil rights movement, the world wars, the three amendments; the branches, the stack of governments, the bill's arrows, a map for latitude and longitude). Eight pre-K stories in the fewest words, three short paragraphs each, with three pictures (S95 to S118): Red ball, blue ball; Big dog, small dog; One duck, two ducks; The magic word; One color at a time; Red, blue, red, blue; One swing; Cat, hat, mat.
- (2026-09-22, pass AE) Every pre-K module has a story now (34 more, S119 to S220, three short paragraphs and three pictures each). The result screen says "Passed today. Pass it again another day and the star is yours." on a first pass, and at the end of a course "Well done! That was the whole course." with the next course named; the forward button opens that course's first unmastered module. Pre-K 4 listening gained Second and last (order 8): three sounds, tap the animal that came second or last. The account screen reads as Mikey wrote it: the PIN under an i, the device name and state texts with a blank line before their example and the My Classroom name in bold. The tour is a bottom sheet with the page in view behind it, ten cards (the five rewritten, plus Life skills, Reading, Experiments, Transcripts, Student summaries), and the backup nudge waits until the tour is done. Roster links sit in a three-column grid so every student's links stack the same way whatever their labels.
- (2026-09-22, pass AF) Roster links never wrap inside a link: three to a row at 15px, nowrap, rows two pixels apart (Merge into on one line). The Backup page ends with "Start over as a new educator": two taps, the educator profile (PIN, device name, state, tour) is removed and every student stays, and the first screen offers Create account again; one educator per device stays the rule. The tour points: each card names a target (Add someone new, Walk through as a student, the backup link, Wonder Questions, the first Open report, Who needs help) and that element glows in gold while the card is up; TOUR is a module constant and the glow effect lives at the top of the component (an effect inside a screen branch broke React's hook order). Every kindergarten module has a story (32 more, S221 to S316). Kindergarten reading gained Ending sounds (order 5): say the word slowly, tap its last sound.
- (2026-09-22, pass AG) The tour is nine small cards (Certificates dropped, Student Summaries before Transcripts) in Mikey's words. The sheet is small, and it sits at the top or the bottom of the screen, whichever leaves its target in view, after scrolling the target to the middle; the body carries the target's name in `data-tour-on` (never `data-tour`, which made the selector find the body itself) and the target glows. Three cards (Lessons, Student Summaries, Transcripts) open a sample report for a made-up student (`sampleRecord()`: nine days of kindergarten math with three modules mastered, one with misses, two stories, a note; nothing saved), pointing at the Summary fold and the Transcript card; the other cards return to the classroom page; Done or Skip on the sample report returns there too. The tour starts once per sign-in (a ref), so returning to the classroom page does not restart it. The Backup page ends with both housekeeping links together, well down; a line says what a backup file holds. A student's first visit says "Tap a glowing button to start your first lesson." Every grade 1 module has a story (23 more, S317 to S385).
- (2026-09-22, pass AH) On a phone (under 700px wide) the tour no longer starts on its own: the classroom page says it is best on a laptop or tablet and offers Show it anyway or No thanks. On wider screens it runs as before, with the sheet scrolled to its own top on every card, its height capped at 36vh, and three more targets (Reading Lists, Science Experiments, Practical Life Skills). The Student Summaries card opens a sample Who needs help view of three made-up students (`sampleClass()`: one doing well, one struggling with Count to 10, one who has not started) pointing at the heading; Done or Skip there returns to the classroom page. The Backup page's what-a-file-holds text sits under an i beside Backup classroom, in Mikey's words; the recovery code title is centered and the code is 17px on phones so it stays on one line. Every grade 2 module has a story (19 more, S386 to S442).
- (2026-09-22, pass AI) While the tour is up the idle clock stands still and the Logging out chip is hidden, so reading a card never signs anyone out. The Wonder Questions, Reading Lists, Science Experiments and Life Skills cards glow whole, like the Transcript card. The glow is one class, `.edu-tour-target`, set by the tour on the one element it points at (a body attribute had let a reduced-motion rule light every marked element at once): a slow gold pulse and a dark spot that circles the frame; reduced motion gets a steady glow. Every grade 3 module has a story (23 more, S443 to S511).
- (2026-09-22, pass AJ) Roster links are two centered rows, Rename, picture and PIN on the first, Hide and Merge into on the second (a flex line break after the PIN link), whatever the labels say, so every card looks the same. On phones (under 700px) there is no tour at all: no auto-start, no New here line, no Show the tour again link; Start over as a new educator keeps its place at the foot of the Backup page. A student's very first available button wears the tour's circling spot as well as its pulse (`orbit` on Btn), on a first visit only. Every module through grade 5 has a story (42 more for grades 4 and 5, S512 to S637).
- (2026-09-22, pass AK) The Backup page's title and everything below it sit 36px further from the top. Student buttons pulse as before with no circling spot (the `orbit` prop stays on Btn, unused). Every middle school module has a story (61 more, S638 to S820), so every module through grade 8 has one. Fifteen reading lessons carry a pictured third way from the drawing kinds already in the app: a flow of signal words, a stack for topic under theme, two arrows for point of view and for a counterclaim, a signpost for fact against opinion, branches for claims and reasons.
- (2026-09-23, pass AL) Story Log is the last link on My Classroom and opens Story-based Learning: a short description in Mikey's voice with an i for more, then one card per active student with a dropdown (Most recent, Read, Unread). Most recent is today's reading or, on a quiet day, the last five. Each row is the story's title, when it was read, a line from its `about` and what it teaches, and an Open button; Open shows the story in its own window (read it, Print it alone, or close with the x). Print all prints the page. The stories test now checks every `about` line reads inside the weekly note sentence (lowercase start, no closing punctuation, not itself "a story about"); three were reworded. High school has stories: 103 more for grades 9 to 12 (S821 to S1129), told for readers; 376 stories in all, with 14 college modules and Ending sounds left.
- (2026-09-23, pass AM) Story-based Learning reads as Mikey wrote it, with the i inside the sentence after "recently" (an inline i, `inlineInfoStyle`). Each student's name sits in a light green band with "12 of 26 read" beside it; the date a story was read is on its own line under the title; a list longer than five rows shows five and a "Show 21 more" link ("Show fewer" folds it back, and changing the dropdown folds it too). The story window has Mark as Read, which logs a story_read event for that student and moves the count, and shows "Read" once it has been read. Every module has a story now: 391 stories for 391 modules (college and Ending sounds added, S1130 to S1174).
- (2026-09-23, pass AN) Content: four modules with stories and three or four ways each (Finding a word, Simple machines, The coordinate plane with a drawn grid point, Where the water comes from), 75 more science experiments (five per band, 21 each), 18 more life skills placed inside their stages, four busier coloring scenes (D24 to D27), text-pair decks as games for older students (Fraction twins, Word roots, Element symbols, Dates and events, Formulas; PAIR_DECKS; the Let's Play fold now shows for every age, older students seeing the decks and Pong). Story thumbnails in the story log rows (StoryThumb, the serial until the art lands) and a My Stories shelf on My progress with Read again. Layout: roster links left-aligned from 700px up; the PIN-last-reset line is gone and My Classroom sits lower in its place; the class view's second and third rows are the tour's target instead of the heading. The tour has a placement engine: each card names where it sits (below the Who needs help button, over the right part of the summary with its left edge past the middle, above the Experiments and Life Skills buttons, beside the Wonder card, left of Life skills and Reading, right of Experiments, flush right over the class view scrolled to its top, below the Transcript card), every card whole with no scrolling, side placements falling back to above or below when a tablet has no room, and positions divided by the laptop zoom (.edu-wrap is zoomed 1.08 from 1000px, and the sheet lives inside it). Measured on 1280x800 and 820x1180: 18 of 18 cards fit without scrolling.
- (2026-09-23, pass AO) The tour's Lessons card opens a sample lesson (Fractions, grade 3, in a walk-through record) with the card over the right part of the example; Done or Skip drops the sample record and returns to the classroom page. Every Pairs game says how to play in one line above the board, and each text deck has its own line (twins say the same amount another way). No deck repeats a card (a rule now): the fractions deck had two 3/4 cards from different pairs that could never match. Three decks by grade: Times table (grade 3), Word meanings (grade 4), Capitals (grade 6). Four more modules with stories and three or four ways each: Quarter past, half past, quarter to (grade 2, the clock now reads quarter hours); Sound is a vibration (grade 3); The microscope (grade 6); Where things come from (grade 6 history). 399 modules, 399 stories, S1 to S1198.
- (2026-09-23, pass AP) Play and rest by age: a picture or game runs 5, 4, 3 or 2.5 minutes and then rests 15, 30, 45 or 60 minutes for the early years, elementary, middle and high bands (`playSecondsFor`, `restMinutesFor`), so the youngest fill more time with coloring and games and older students tilt toward lessons. Every game has an Instructions link centered under its card that opens a popup (`gameInstructions`, by kind and by deck); the inline line is gone. Let's Read: for students past the early years, a fold under the games with one longer story per course, unlocked when every module in the course is mastered, showing how many modules are to go until then; reading logs `story_read` with `course:<id>`, the weekly note names the course, and the Story-based Learning page lists course stories under each student with Open into the same window. Twelve course stories so far (COURSE_STORIES in stories.mjs, CS1 to CS12 in the ledger): fractions, reading 3 and 4, science 3 to 8, math 6, history 4 and 5. Science review decks, one per science course from grade 3 (key word and meaning), as games shown once the course is on the student's list. Seventy-two experiments carry a module id and appear as a Try this card at the foot of that lesson. A Story Log tour card sits above the link.
- (2026-09-23, pass AQ) Every science module has an experiment (33 more, 348 in all; the Try this card shows up to two at the foot of a lesson). When the last module of a course is mastered, the result screen says "You've unlocked a story related to this course!" with a Let's Read! button that opens the course story; the transcript lists course stories read. A new subject, Technology: How computers think (grade 3: inputs and outputs, steps in order, patterns and loops), Programs that decide (grade 5: variables, if-then-else, finding the bug), Bits, networks and safety (grade 7: binary, how the internet moves a message, passwords and privacy), each module with three or four ways, a story, TEKS Technology Applications and CSTA standards (the national framework shown in the Common Core column), two Wonder questions, and a Computer words deck. Eleven more course stories (CS13 to CS23): the three technology courses, math 5, 7 and 8, reading 5 and 6, history 6, 7 and 8. The branches drawing is the three branches of government only; the if-then lesson, specialization, claims and reasons, and rhetorical appeals had borrowed it and now have drawings of their own. 408 modules, 408 stories, 23 course stories.
- (2026-09-23, pass AR) Two more kinds of game for older students: Quick fire (sixty seconds of the student's own text questions, drawn from the modules on their list, math, science or everything; a streak from three in a row) and In order (shuffled steps or events tapped first to last, three sets against a clock: how things happen, a timeline, the math way; ORDER_DECKS). The Times table and Dates decks became those games. The Try this card is now an Experiment link, centered and closed, between Read it to me and View story; it opens in place, and every experiment that appears under a K to 8 lesson has a `tell`, one warm paragraph that says what to grab, what to do and what to watch for; experiments without one are read as three friendly sentences. A Health subject: Taking care of me (kindergarten, read aloud: washing hands, brushing teeth, sleep, my plate) and A body that works well (grade 4: sleep and the brain, reading a food label, the heart at work, screens and you), with ways, stories, TEKS Health Education and NHES standards, a Wonder question and a Healthy words deck; Health sorts last among subjects. Nine unplugged Technology activities under the Experiment link. Twelve more course stories (CS24 to CS35), including health 4 and the high school sciences. 416 modules, 416 stories, 35 course stories, 357 experiments.
- (2026-09-23, pass AS) The moat travels with the files: LICENSE.md (source-available, read and evaluate only, no reuse of code or content, no training on it), NOTICE.md, a License section in README.md, and a one-line notice at the top of every source file and of the built page. Layout: the Wonder Questions line with its pill is centered on wide screens; the Backup page's automatic-backup bullets are centered; the biometrics card says Sign in with fingerprint or face instead of typing a PIN. Story-based Learning: module rows under a Modules title, three shown then more, course stories only under Read and Unread (never Most recent), the i says the prior three, no Print all. The student report: Stories card gone, a Story log link centered above Raw data opens that one student's story log (same page, three toggles, printable, Back to report); the Weekly note title is centered and the page breathes below the name. The weekly note lists mastered courses as bold bullets and the stories read as bold-titled bullets under one "read:" line; the sample student is Frederick. Tour: card 2 targets the whole lesson card, flush right, page at the top; card 3 overlaps the top edge of the Experiments and Life Skills buttons; card 8 glows the green summary, the second and the third student and sits over the second student's right side, overlapping the third card's corner; card 9 sits over the bottom edge of the Experiments and Life Skills cards; card 10 sits above the Transcript card over the progress dropdown. Games keep a best score or best time per game, shown on the tile (onScore). A Music subject: Beat, high, low, loud, soft (grade 1, read aloud) and Reading music (grade 4: note lengths, the staff, tempo and dynamics), with ways, stories, TEKS Fine Arts and NCAS standards, a Wonder question, a Music words deck and a course story (CS36). 422 modules, 422 stories, 36 course stories.
- (2026-09-23, pass AT) Variety: every writing module has five prompts and every small question pool has five or more questions, and a rule holds every non-tracing module to at least four different questions. Rounds avoid the prompts of the last two rounds on that module (`buildAttempt` options.avoid, `recentPrompts`), and the memory checks draw fresh the same way. Practice the missed ones opens a triple dose (options.dose 3: fifteen questions instead of five, the bar scaled to twelve of fifteen in `isMasteredAttempt`), one dose per visit, and a mastered triple dose gets its own words on the result screen: You did it, and a line naming what was overcome. Course stories appear under Most recent when they were read today or among the last three. The biometrics note says why a reader is unavailable (a preview rather than the app as its own page, or no reader). Every older course has a course story now: 26 more (CS37 to CS62), each about one of the four core characters growing up, Frederick, Chloe, Georgette or Mike, with Savanah and the family stepping in; the math-9 story became Frederick's (CS27). 62 course stories.
- (2026-09-23, pass AU) An audit pass, no new ideas: every module opened at phone width in a browser (lesson, story, first question) with zero errors and overflows after two fixes. Grade 1 read-aloud lessons written as paragraphs with [[ ]] words crashed the read-aloud screen (ten modules); the screen now builds a script from the paragraphs, instructions spoken and bracketed words shown. The stack drawing went negative past five levels; it scales to eight now, with a rule, and the Staff example uses five. The memory check favors the mastered modules with the most recent misses (`recentMisses`, options.missed). Rules added for course story length and shape and for the remembrance dates. Test hooks visibleModuleIds and goHome. docs/AUDIT-2026-09-23.md holds the findings, the hours-per-grade table (7 to 17 hours a grade today, about 90 for a year at half an hour a day) and the plan for what fills a year. docs/PROMPT-ILLUSTRATIONS.md is the prompt for an illustration-placing chat; docs/HANDOFF.md is the prompt and map for a new build chat.
- (2026-09-23, pass AV) The audit continued across the rest of the product at phone width: all 26 older-student games with their Instructions, Let's Read and a course story, My progress, four coloring pages, the classroom with a student, the report, the per-student story log, the transcript, the standards map, the Backup page, Wonder Questions, Reading Lists, Experiments and Life Skills: zero errors and zero overflows. Copy that read wrong was fixed: the device-name example (Chromebook, not chrome book, and no i.e.), insures became protects on the Backup page and its tips, the Quick checks sentence no longer says allow for by allowing, students log in by tapping their name, and the transcript no longer says for obvious reasons.
- (2026-09-23, pass AW) The two browser sweeps live in the repo (tools/sweeps/modules.mjs and pages.mjs, run by tools/sweep.sh at 360px and 1280px) and are the step after check.sh before a commit; docs/RELEASE-CHECKLIST.md is the one-page manual pass a person does after the machine. readAloudScript(mod) moved into logic, and a rule holds every read-aloud lesson to a script with a spoken line and a shown card.
- (2026-09-23, pass AX) Depth, the first step of the year-filling plan: two modules appended to each of eight core courses, each with a six-question pool, three ways (one drawn), a story and TEKS plus national standards. Math 4: area and perimeter, angles and lines. Math 6: mean, median, mode and range; the four quadrants. Math 8: linear functions; volume of cylinders and cones. Reading 3: clues around a word; characters and setting. Reading 6: plot and conflict; how a poem is built. Science 3: soil and rocks; food chains. Science 5: balanced and unbalanced forces; fossils and what they tell. History 5: immigrants and growing cities; industry and invention. 438 modules, 438 stories (S1 to S1315). The sixteen were walked in a browser with every way, the story and a round: clean.
- (2026-09-23, pass AY) Three at once. Every high school experiment has a warm telling, so the Experiment link reads in one voice from kindergarten to physics. Older students get longer rounds at the same bar: eight questions and seven to master from grade 6, ten and eight from grade 9 (`moduleRules` by grade; a module or course that sets its own numbers keeps them); quick checks stay at five questions and four to pass. The second depth batch: two modules for each of the other eight core courses, with six-question pools, three ways, stories (S1316 to S1363) and standards. Math 3: measuring things; graphs and tallies. Math 5: multiplying decimals; data and line plots. Math 7: probability; scale drawings. Reading 4: cause and effect; text features. Reading 5: comparing two texts; reading an argument. Reading 7: figurative language; how a text is organized. Reading 8: purpose and bias; theme across texts. Science 4: properties of matter; food webs. 454 modules, 454 stories; every one of the sixteen walked clean in a browser.
- (2026-09-23, pass AZ) A new chat's first pass, from Mikey's list of Sep 23. Sign-in: a full PIN opens the record on its own, student or educator, so the phone keypad never has to come down for Go (the Go button is gone; a wrong student PIN clears and says so). Let's Read wears the same crayon strip as Let's Play, its title centered, a chevron alone at the edge, Mikey's sentence "Finishing every module within a course unlocks a story which ties all of the concepts together." centered with the count on its own line under it; "Tap a glowing button to start your first lesson." is gone. The story log shows three course stories, then Show more and Show fewer, and Mark as Read sits centered on its own line. Life skills gained Breathing exercises for calming down (smell the flower and blow out the candle, five-finger breathing, box breathing, balloon belly). Games: the pairs grid pins its columns with minmax(0, 1fr), three wide for text decks on a phone, because a 1fr column still grows to its longest word and four columns overflowed the box once a pair turned over (the page never widened, so the sweep missed it; the countdown bar is clamped to 100% too). Early titles in capitals (Big and Small, Red and Blue, Big Maze, More Pairs, Big Puzzle, Many Pairs, Huge Maze); sort instructions in Mikey's words; the maze says star and has one in its end cell; puzzle pieces drag (two taps still swap). Connect the dots: the name game is gone (it never looked like a name), DOT_GAP holds every picture's dots 11 units apart with a test, the house has a door between 4 and 5, and the pictures rise from kite (4) through boat, house, star and fish to rocket (12) down the list with a test that the count never falls. Older students' games are spread so the kinds take turns (spreadKinds, even spacing by kind; with 20 decks against 7 others the full walk-through list still runs decks up to five deep, and only the ten-kinds plan fixes that). Quick fire: a wrong answer costs a point, never below zero; points, right answers and the longest streak are shown and the streak kept on the tile; New High Score in Mikey's words; only readers' courses from grade 3 up feed it, picture answers are skipped, the generators take turns in a shuffled cycle and no prompt repeats within a round while another is left; the setup line is shown (its absence was why "Whose turn is next?" read as nonsense). Questions reworded to read as one sentence: the claps question names both words, need-or-want puts the thing in the question (kindergarten and grade 12), whose-turn lets either child be next, the magnet question is "Which object cannot be moved with a magnet?" against a paper clip or a nail (two new icons, taught in the lesson), and the three rule answers can no longer be confused (to let the other cars go; so it is fair for everyone; so nobody bumps or falls), all taught in the lesson. Pong moves by the clock with one speed that climbs 4% a hit and never drops, and saves a best. In order numbers the steps in any order, a tap on a numbered step takes its number back, and the check comes only when every step has a number. Young learners get a speaker under Instructions that reads them when tapped. Browser test 167 (a right PIN opens the classroom without a tap); rules 1,543.
- (2026-09-23, pass BA) Games first, in Mikey's direction: visual, hands-on kinds over decks (he finds decks dull and Quick fire mediocre), reaching every grade. Three kinds that work on any topic, each fed by a rule deck (RULE_DECKS: two named groups of short chips; number groups are computed, 'shape:kind:color' chips are drawn for the youngest): Catch (chips fall one after another, faster as the round goes, and a basket under the finger catches group A; a right catch is a point, a wrong one costs one, twenty chips a round), Path (a grid of chips with a guaranteed path of group A from the top corner to a star, two loose stones off it; drag the dot, a wrong step turns red and the dot stays; the clock counts up), and Buckets (eight chips, two labelled buckets, drag each to its group, a wrong drop bounces back; the clock counts up). Twenty games on them: Catch the Circles, Step on the Circles and Catch the Red Ones for the early years (kept inside the no-two-of-a-kind order), then even numbers, multiples of 3, 4 and 7, primes, square numbers, halves, nouns and verbs, adjectives and adverbs, solids and liquids, living things, conductors, mammals, elements and compounds, renewable resources, states and countries, acids and bases, each at the grade it teaches best. Older students now have seven kinds (63 games in all); the tiles' best line shows seconds for the timed kinds. The remembrance cards carry Mikey's words, four lines each. Upgrade 1: the page sweep looks inside every .edu-game-box with every pair of cards turned over (0 overflow at 360px across 44 games). Upgrade 2: the browser test signs a student in with a PIN: a wrong one clears and says so, a right one opens the record with no tap (170 checks). Rules: every rule deck has two clean groups of short chips, every rule game names a deck of a listed kind, the older list holds at least seven kinds.
- (2026-09-23, pass BB) Story log rebuilt on Mikey's notes: one Student picker (All students or one) and one Most recent / Read / Unread toggle at the top instead of a select on every card, name bands that line up (name left, count right), Short Stories (module) and Long Stories (course) sections in every view, and a card with the title and summary on the left, the picture top right and Open bottom right. Number answers never wrap onto two lines (columns as wide as the longest answer, nowrap, 22px from four characters). Air above View story. The sock summary reads "two striped socks, and the hole that told them apart". Veterans Day: "who have served". Stories and the method: Mikey asked why the stories are not in the Miniature Gladwell Method. Honest answer: the module stories were written as three terse lines, and from grade 3 up 296 of 319 fall under four paragraphs or eighty words (tools/story-arc.mjs writes docs/THIN-STORIES.md). Two are rewritten as the full four-step arc as the model: Four flashlights (binary: Priya sends the number ten across a dark field with four flashlights worth 8, 4, 2 and 1) and The reason that fell (claims and reasons: Marcus at the podium, and the cousin in Ohio). Every story touched from now on is rewritten to the arc, and the report must trend to zero. The binary lesson walks 1010 switch by switch. Games, Mikey's yes to both: Frog jumps (a frog on a drawn number line, eight sums a round, drag it to the answer's tick; a wrong landing sends it back; decks for sums to 10, take away within 20, fractions in quarters and negative numbers) and Where is it? (a schematic map of regions to tap: the six inhabited continents and the four Texas regions; a right tap fills the region and plants a pin). Older students now have nine kinds and 69 games; the young list keeps its no-two-of-a-kind order. Rules: every frog jump lands on a tick of its line, every map region is a named polygon, a point is inside its own region only, the older list holds at least nine kinds (1,550).
- (2026-09-23, pass BC) Mikey's yes to all three, with games and story rewrites as the standing focus. Stories: sixteen grade 3 math and science stories rewritten to the four-step arc (Two pizzas, Which slice is bigger?, Marks on the ruler, Blocks of a fifth, Three states in the kitchen, Pushes and pulls, The jar of eggs, The rubber band, The jar, The bucket line, Three bags, Seven times eight, Deal them out, Column by column, Three ways to measure a puppy, The cars), each with a named child, a friction point, the insight as the tool and an echo, 130 to 200 words, one sensory detail; their 48 ledger rows were rewritten to the new scenes so the Leonardo pictures match. 280 of 319 still to go (docs/THIN-STORIES.md). Games: Balance the scale (the left pan shows a value, weights drag from a tray onto the right pan, the beam tips toward the heavier side and levels when they match; decks for making a number to 12, times tables, fractions in quarters and eighths, and expressions with brackets and squares; every target is checked reachable from its tray) and a third map, the five regions of the United States. Maps now carry art serials M1 to M3 with ledger rows and Leonardo prompts: Mikey's painting goes to art/maps/<serial>.webp and sits behind the tap regions (which turn translucent), and until it arrives the game shows a dashed placeholder frame with the serial. Europe and the states of the US are the next maps. Older students have ten kinds and 74 games in all; the young list keeps its order.
- (2026-09-23, pass BD) Story log on Mikey's notes: the empty lines say the true thing (a student whose courses have no long stories sees "No long stories on these courses yet.", never "every one has been read"); All students is centered in the picker; Unread shows two short stories and one long before Show more, Most recent two and two, Read three; the three tabs are slightly smaller bold words (SegToggle size small). Stories: the eleven grade 3 reading, writing and history stories rewritten to the arc (Happy, unhappy, happily; Check it or argue it; Because and so; The friends it came with; What she did at recess; The shape of it; The paper boat; I think, because; Three levels; Rule or law?; The eleven dollars), 33 ledger rows refreshed; grade 3 is done and 269 of 319 remain. Maps: the oceans (Pacific in two parts, Atlantic, Indian, Arctic, Southern) and the hemispheres (four overlapping halves, judged against the asked region first, drawn as dashed translucent halves over the land); both share the world painting M1 and draw the continents plain behind them. A region may now be several polygons (parts) and a map may name another map as its land. 76 games in all.
- (2026-09-23, pass BE) Mikey's yes to all three. Stories: the fourteen grade 4 math and science stories rewritten to the arc (Five kinds before breakfast, The loop, The sandcastle, The right tools, The piano, Four tests, The strings, Twenty-three boxes, Sharing in rounds, Twelve chairs, The quarter, Half and a quarter, Around and inside, The door), 42 ledger rows refreshed; 255 of 319 remain, grade 4 reading, writing and history next. Maps: Europe, the twelve largest countries (Russia, Ukraine, France, Spain, Sweden, Norway, Germany, Finland, Poland, Italy, the United Kingdom, Romania) placed where they really are on a box from 12 degrees west to 45 east and 72 north to 35 south, with its own painting M4 and ledger row; and the first stage of a states map, the ten largest states of the lower 48 (Texas, California, Montana, New Mexico, Arizona, Nevada, Colorado, Oregon, Wyoming, Michigan in two parts), drawn over the plain country from the regions map and sharing M3. A rule now says no region's corner may lie inside another region of its own map, which caught six overlaps in the first drawing of Europe and six in the states. 78 games in all.
- (2026-09-23, pass BF) Mikey's yes to all three. Stories: the thirteen grade 4 reading, writing and history stories rewritten to the arc (One minute, The muddy boots, Brave as a lion, Road signs, The wrong page, The icy road, Two minutes first, The big thing first, Longer recess, The bird in the house, Three kitchens, The mission bell, The 28th star), 39 ledger rows refreshed; grade 4 is finished and 242 of 319 remain, grade 5 next. Maps: the states map grew from ten to twenty-five (Minnesota, Utah, Idaho, Kansas, Nebraska, South Dakota, Washington, North Dakota, Oklahoma, Missouri, Florida, Wisconsin, Georgia, Illinois, Iowa; Montana's west edge moved for Idaho's panhandle), and Asia arrived as a fifth painted map, M5, with its twelve largest countries (Russia, China, India, Kazakhstan, Saudi Arabia, Iran, Mongolia, Indonesia, Pakistan, Turkey, Japan, Thailand). A real bug found by tapping every state in a browser: a tap's y was scaled to the map's height without the 14-unit strip of words above it, so taps landed compressed toward the top and missed small regions like Oklahoma; fixed, and all twenty-five states now answer a tap at a point inside them. The no-overlap rule caught four corner overlaps in the new states before that. 79 games in all.
- (2026-09-23, pass BG) Mikey's yes to 1 and 2, and his call on the maps: the code regions are gone from view and he paints every map in Leonardo for accuracy. Where is it? now keeps its regions as invisible hit areas only; until art/maps/<serial>.webp exists the game shows a placeholder card ("This map is being painted", with the serial) and real students do not see the game at all, while the walk-through shows every tile so the placeholders can be checked. When a painting arrives the hit areas are traced to match it: tools/map-overlay.mjs writes an SVG of the regions over the painting. Africa (M6: Algeria, DR Congo, Sudan, Libya, Chad, Niger, Angola, Mali, South Africa, Ethiopia, Mauritania, Egypt) and South America (M7: Brazil, Argentina, Peru, Colombia, Bolivia, Venezuela, Chile, Paraguay, Ecuador, Guyana, Uruguay, Suriname) join as painted maps with ledger rows; the no-overlap rule covers all five country maps and caught four overlaps in the first drafts. Stories: the thirteen grade 5 math and science stories rewritten to the arc (Two prices, Half of a half, Cartons of 24, Sugar cubes, Three east two north, The package, The dimes, Ten beans, Two glasses, Top lamp marble, Eyes and wheels, The rope that did not move, The shell on the mountain), 39 ledger rows refreshed; 229 of 319 remain, grade 5 reading, writing and history next. 81 games in all, nine of them maps waiting for paintings M1 to M7.
- (2026-09-23, pass BH) Stories first, on Mikey's word, and the art ledger and the Leonardo brief rebuilt around his image template. Forty stories rewritten to the arc: all sixteen of grade 5 reading, writing and history (What it was really about, Two cameras, Break a leg, Point to the line, Two storms, The cousin, The tour, Five paragraphs, The moment, Three rows, Four steps to a war, The second plan, Doubling the map, The split, The ship in the family tree, The week and the day), the eleven of grade 6 math and science (How many halves, Half a rectangle, The mystery box, The friend who moved the mean, East and north, Letters and words, Three ways, Two rugs, Follow the energy, Feathers and rocks, The rooms in the cork), on top of the thirteen of grade 5 math and science from pass BG. Grades 3, 4 and 5 are complete and 202 of 319 remain (grade 6 reading, writing and history next, then 7). The ledger: every one of the 1,338 story rows without a character sheet now carries Mikey's template, with the playful modifier and the visual anchor set by the subject (Science wonder-filled and vibrant with a dynamic close-up; History friendly and whimsical yet respectful with a heroic composition and soft cinematic lighting; Math cheerful and neat with distinct geometric shapes; Reading and Writing imaginative and expressive, character-focused with dreamy lighting) and a minimalist scene background; the 25 cast rows already had it and keep their sheet references. The brief carries the template, the subject table, the four sample prompts and the golden rules (no text, one model pipeline, minimalist backgrounds, wholesome always).
- (2026-09-23, pass BI) Mikey's yes to all three. Stories: the fifteen grade 6 reading, writing and history stories rewritten to the arc (Two rains, The root port, The same job, The hill, The song without music, Show the numbers, Two towns, Let them talk, The orange globe, What we would have to teach, The remote, Who decides, Push and pull, The neighborhood on the map, The tag); grades 3 to 6 are complete and 187 of 319 remain (grade 7 next). The core-cast batch turned out to need nothing: the twelve module stories that star the cast were written with the arc from the start (five paragraphs, 165 to 215 words), which the previous reply had got wrong. The story summaries flow everywhere on their own: the story log, the weekly note and the report read title and about from STORIES through STORY_TITLES. tools/prompts-csv.mjs writes docs/LEONARDO-PROMPTS.csv, one row per serial in ledger order (serial, where, scene, prompt, negative prompt, status), for working through Leonardo without a markdown table. Early readers: five grade 2 math stories (Stacks of pennies, Bundles of stickers, Borrow a ten, Counting the change, Rows of eggs) retold in a small arc as samples for Mikey to judge: one child, one snag, the idea as the fix, one echo, four short beats in sixty to ninety words with something to touch or hear. Cast placement, as advised: one core character per long story with brief meetings, and the cast in a few significant short stories per grade, never more than two in one.
- (2026-09-23, pass BJ) Mikey's yes to all three, and his verdict on the samples: the Mia story (Stacks of pennies) is the model for the early years. Stories: the eleven grade 7 math and science stories rewritten to the arc (The price of one, The debt, Shoes then socks, The string and the can, A hundred flips, The bedroom on the page, The city inside, The slope of the air, The moths, The pyramid, The crumpled bag); 176 of 319 remain from grade 3 up, grade 7 reading, writing and history next. The early years begin: all fourteen kindergarten reading stories retold in the small arc (The song that holds them, Big G small g, Buh for ball, What starts with mmm, The sound at the end, Cat hat bat, The T on the window, The small a sits low, The E one line at a time, Clap your name, The word on the door, Which way the finger goes, The card and the picture, Slants in the sand): one child, one snag, the idea as the fix, one echo, forty to seventy words. The read-aloud rule is a hard test now: an early-years story keeps every sentence to twelve words or fewer and every word to three syllables or fewer (vowel groups, a silent e dropped); thirty-five sentences in twenty older early stories were split or reworded to pass, and the five grade 2 samples were tightened. docs/LEONARDO-PROMPTS.csv regenerated.
- (2026-09-23, pass BK) Mikey's yes to all three, plus his notes. Frog jumps: while the frog is held, the tick it will land on lights up and a dotted line runs to it, so letting go is never a guess. Closing a game, a coloring page or a long story returns to the overview where the student left it, fold open, scrolled to the tiles (verified in a browser: scroll 2116 before, 2116 after). Quick fire tiles appear only when the list holds readers' lessons from grade 3 up (sprintPool, shared by the tile filter and the game), and the empty line says so plainly. Let's Read: the two lines above the list sit on a light green band, and the course stories are spread so no core character carries two stories in a row (spreadLeads, with a test per grade). The Story log's student picker is light green, and above 500px a story card puts the picture on the left filling the height, the words in the middle and Open centered on the right; phones keep the picture top right and Open bottom right. Stories: all thirteen remaining kindergarten math stories retold in the small arc (Ten steps, Start at the dot, One more one less, Cars in the garage, Which is bigger?, Counting corners, All the way around, Ten fingers each, The stick and the rock, The sock pile, Roll stack point, Partners for ten, Two buckets); the grade 7 reading, writing and history stories turned out to be full stories already (three long paragraphs), so the thin count is now by words alone: 161 of 319 from grade 3 up are under eighty words. A spoken-story check joined the browser test: an early story read aloud speaks its title and every paragraph in order and lights the paragraph being read. The backup page anomaly in Mikey's photo (Hide and Merge into… below the Restore card) did not reproduce at laptop width in a fresh session; those links exist only on student cards, so a stale build is the likeliest cause.
- (2026-09-23, pass BL) Mikey's yes to 1 and 2. Kindergarten is complete: the six civics stories retold in the small arc (One at a time, Bread or the toy, Up went the flag, Off to work, Hands up, Two flags one pole), each forty to seventy words with a child, a snag, the idea and an echo; the kindergarten science stories were already five-paragraph stories and stayed. Grade 8 math and science, ten stories to the arc (Two hills, Five folds, Forty-nine tiles, Sliding the point, The taxi meter, A circle stacked, Fold it burn it, Boxes inside boxes, The walk on a graph, The laundry pile), 30 ledger rows refreshed; 151 of 319 remain from grade 3 up, grade 8 reading, writing and history next, then grades 1 and 2 in the small arc.
- (2026-09-23, pass BM) Mikey's yes to all three. Grade 8 is finished: thirteen reading, writing and history stories to the arc (The car and the rain, The fire station, One word one story, The mirror, Outside the frame, One tune two songs, A fight worth picking, The missing sentence, Body first, Why how and what may not, Learning to drive, The coin toss, Three promises); 138 of 319 remain from grade 3 up, grade 9 next. Grades 1 and 2 are in the small arc: all twenty-six grade 1 stories (from Ten and three and Two pockets through Shell chair thumb, The quiet e, The clock in the song and Bird high cow low) and the fifteen grade 2 stories still short (Quarter past, The o that talked, Rab bit, Who where what why, The words around it, Who ran?, The fluffy cat, The wobbly tooth, Then and now, The wallet, The lemonade stand, The key on the map, Who sent the truck, The kite jar, By feel), every one under the read-aloud rule; 162 ledger rows refreshed. Of the 135 early-years stories, 46 are still short, nearly all pre-K 3 and pre-K 4, which are spoken to three- and four-year-olds and get the smallest arc of all.
- (2026-09-24, pass BN) Mikey's yes to 1 and 2. The early years are finished: forty-two pre-K stories retold in the spoken micro-arc, thirty to fifty words in four beats (Two balls, The other mitten, Like me?, One small bed, Splash, Two bowls, The first line, One two three, Sun and grass, Pizza corners, Two shoes, The magic word, Roll or sit, A then B, The green apple, Where is the dog?, Who said that?, One color at a time, The other red car, Twins on the table, Two of each, Two by two, The right hole, Stack or roll, Five then two, The pumpkin wagon, Red blue red blue, Lines on the window, What is it?, Back to the dot, Count to ten, Three bowls, Helpers, Cat mat hat, Buh and sss, The roar came first, Whistle tick woof, B U S, A letter in the sand, Sticks make letters, Down then across, Two socks); every one of the 135 pre-K to grade 2 stories now has a child or a creature, a snag, the idea and an echo under the read-aloud rule. Grade 9 math and science, twelve stories to the arc (Two sides of the bed, The honest machine, Two clues, Un-multiplying, The rumor, Four tickets, The leaf and the lung, Three witnesses, The recipe book, The book that stays, The atom on the bus, The thermostat inside); 126 of 319 remain from grade 3 up, grade 9 reading, writing and history next. Read-aloud speed (asked about, not built): a snail and a hare under the story's speaker, no words; pre-K starts on the snail and K to 2 on the hare; the choice is remembered on the device, and an educator can set a student's starting speed on the card.
- (2026-09-23, pass BO) Mikey's yes to all three, and two notes. The Wonder Questions link with its On/Off pill now sits under the whole student card, after Open report, so it centers at every width (it had been centered inside the left column only, which on a laptop is not the middle). A new life skill for the early years, Finding an aww moment wherever you go: one small good thing on every outing, a beetle, a kind stranger, a flower in a wall, said out loud and asked about at dinner, kept small on purpose, with the child leading; noticing is the root of wonder, and a child who can find the small thing learns to enjoy the road and not just the place it ends. The snail and the hare: an early-years story page (pre-K to grade 2) shows two round buttons under Read the story to me, a snail that reads at three quarters speed and a hare at the usual pace; pre-K starts on the snail, K to 2 on the hare, and the last choice is remembered on the device (localStorage, per viewer); older stories show no control. The browser test opens a kindergarten story through a new hook (__eduTest.openStory), reads it on the snail, then on the hare, and checks the spoken rates differ by the 0.75 factor, that the choice is remembered, and that the sentences spoken, joined, equal the title and every paragraph in order (the earlier spoken-story check had been silently skipped, because the lesson page of an early module has no View story button). Stories: grade 9 is finished (thirteen reading, writing and history stories: Three ways to sell a bicycle, The key, Sprinted or ran, The page with no name, The sentence after the quote, The phone rang, Taking it apart, The cracked egg, Three dials, The shrunken photograph, The block and the ranch, The paycheck and the jar, The map behind your eyes) and grade 10 math and science (twelve: The bow tie, The enlargement, The picture on the wall, Standing at the corner, A quarter of the pizza, Two ways to share, Bricks in bricks out, The ruler from 0 to 14, A dozen for chemists, Snap split swap burn, The balloon, How strong the lemonade is); 101 of 319 remain from grade 3 up, grade 10 reading, writing and history next.
- (2026-09-23, pass BP) Mikey's yes to 1 and 2, and his correction: the Wonder Questions link goes back above the Open report button. The card's right side is now one centered column (edu-student-side) holding the Wonder link with its pill above Open report, so on a laptop the two stack on the right and on a phone they stack under the links; checked in a screenshot at 1280px. Stories: grade 10 is finished (twelve reading, writing and history: The missing middle, A different truck, Two wants, The green light, A claim not a summary, Where did you get that?, Before and after, The river that kept time, Everyone some one, Land plague press, One idea three endings, Twenty years apart) and grade 11 math and science (eleven: Where the arch lands, Four patches, Two staircases, How many doublings, Four steps from three, The ball on the balcony, The rope, The water pipe, The bike and the truck, The same box twice, Two strings of lights); 78 of 319 remain from grade 3 up, grade 11 reading, writing and history next, then 12 and college.
- (2026-09-23, pass BQ) Mikey's yes to 1 and 2 only, plus two notes. The What's new pop-up on the classroom page is written the way Mikey talks to a teacher: conversational, complete sentences, what changed and why it matters (the newest block of docs/WHATS-NEW.md; the rule is in its head and in CLAUDE.md). Fixing that surfaced a real bug in tools/build-site.mjs: the block was read with a multiline regex whose $ stopped at the first line end, so every pop-up since the feature shipped had shown only its first item; the first dated block is now taken whole, and the built page carries all five. The student card is as Mikey asked: Wonder Questions with its pill is a third line of links, left aligned with Rename and Hide, and Open report sits centered at the foot of the card at every width (the side column from pass BP is gone; screenshot checked at 1280px). Stories: grade 11 is finished (thirteen reading, writing and history: The objection that got there first, A straight face, The long one then the short one, A big claim on a small pile, By reasons not by sources, Three times from three places, Thirty seconds, Gold on the outside, One in four, The morning that ended the argument, Two powers no shots, Courts feet crowds laws, The shocks) and grade 12 math and science (thirteen: The picture moved, Two machines in a row, A walk around the circle, The halving clock, What the ends do, Never finished, One Tuesday and thirty years, The long campfire, The way a doctor does it, The peach, The conveyor belt, Slower than it grows, Raisins in the dough); 52 of 319 remain from grade 3 up, grade 12 reading, writing and history next, then college.
- (2026-09-23, pass BR) Mikey's yes to 1 and 2 only, and one note: the student card's three lines of links center at every width now (the flex-start rule at 700px is gone), under the name on a phone and across the card on a laptop, with Open report centered at the foot; screenshot checked at 1280px. Stories: grade 12 is finished (seventeen reading, writing and history: Two witnesses, The skipped step, Walk stroll march trudge, Why it starts here, The question the sources the answer, One small moment, One point one ask, The ideas underneath, Three teams, The school and the district, The fence, The obstacle course, Two rounds 270, Sixty dollars two wants, Two stands on one street, A promise everyone accepts, The country's paycheck) and so is college (fourteen: The rich friend, Same average different rooms, Three slices, The snowball, Ice cream and the pool, A fight worth picking, The sentence that explains why, A number with no clothes on, Cheap and the most expensive ever, The letter and the textbook, The century off by one, Dry wood and a match, Eight hinges, Bricks and a house). Every core-subject story from grade 3 through college now carries the arc; 21 of 319 remain, all electives (Art, Music, Technology and Health in grades 3 to 7).
- (2026-09-23, pass BS) Mikey's yes to 1 and 3. The rewrite is finished: the last twenty-one elective stories (Art, Music, Technology and Health in grades 3 to 7: A cup seven ways, Three jars, Four looks, A face a flower a fence, The road into the distance, The handprint, The pizza of notes, The ladder of letters, Four Italian words, The night librarian, The serving size, The top of the stairs, Twenty twenty twenty, The talking toaster, Exactly what she was told, One instruction repeated, The scoreboard, The thermostat that never sleeps, Facing the wall, One piece at a time, The message that said now) carry the arc, and docs/THIN-STORIES.md reads 0 of 319. Every module story in the app, pre-K through college, is a story now. The story book: the Story Log page gains a card with a course picker (every course with a long story) and Open the book; the book page shows a cover (course title, grade, the count), then every module story in course order, then the long story, with Print or save as PDF at the top; the print stylesheet (body.edu-book-mode) prints the sheet alone with one story per page. The browser test opens the grade 4 math book and counts the stories against the course (176 checks).
- (2026-09-23, pass BT) Mikey's photo showed the student card on a laptop with the name and picture on the left and the links off the card's center line while Open report sat true center. The card is one centered column at every width now: the picture above the name, the name and grade under it, three lines of links, then Open report, all on the center line; the laptop two-column rules are gone (screenshot checked at 1280px). His yes to 3 only: the story arc is a hard test. Every module story must have at least four beats and run eighty words or more from grade 3 up, twenty-five or more in the early years; the four kindergarten health stories that still had three beats (The glitter, The fence of teeth, The phone and the boy, The rainbow plate) were retold in the small arc so the rule holds on the whole store (4,607 story checks).
- (2026-09-23, pass BU) Mikey's yes to 1 and 3. Clean unillustrated printing: the dashed placeholder frame (Illustration Sn to come) carries a class, and the print stylesheet hides it in both the story-book and the single-story print modes, so a book for a course with no paintings yet prints as words alone; on screen the frames stay, because they tell an educator what is coming. The classroom at laptop width joined the browser test: after the classroom checks it resizes to 1280 by 800, measures every student card, and fails if the name row, any row of links or Open report sits more than four pixels off the card's center line; it also saves tests/e2e/out/classroom-1280.png, which the release checklist now names as step 4 so a person looks at the same card the test measured. Today's four photos of that card were each a layout the checks could not see; this one they can.
- (2026-09-24, pass BV) Mikey's bug: on the Story Log page the phone's back button led to a screen that loaded forever. The back handler goes up one level per screen, and any screen its list did not name fell through to the courses page; with no student signed in the courses page is the loading screen, and it never finishes. The Story Log now goes back to the classroom, the story book to the Story Log, a certificate to the report it opened from, and the fallback for any unnamed screen is the courses only when a student is signed in and the classroom otherwise, so no screen can reach the spinner by going back. The browser test presses back from the book and from the Story Log and checks where it lands (179 checks).
- (2026-09-24, pass BW) Mikey's yes to 1: the back-button sweep. tests/e2e/back-sweep.mjs reads every screen name the source can set, lands on each through a test hook (__eduTest.goTo, which also sets the state a screen needs: a module, a course, a picture, a certificate), presses the phone's back button, and fails if the page goes blank, stays put, shows the loading screen or leaves the app; educator screens run with no student signed in and student screens inside a walk-through, the courses page last because leaving it ends the walk-through. Twenty screens, all green; the transient ones (a result after an answer, a sent piece of writing, a spoken wonder question) stay with the click-through test. The sweep was proved on the bug it exists for: with the Story Log line taken back out of upScreen, it fails on the Story Log with 'blank page', which is what a crashed render looks like (an empty root with the test hook frozen on its last good screen). It is step 11 of 12 in check.sh.
- (2026-09-24, pass BX) Mikey's yes to 3: the crash guard. A React error boundary wraps the whole app; a screen that throws while drawing now shows a card ("Something went wrong on this page. Nothing you did was lost. Tap the button to go home.") with a Go home button instead of a blank page. Go home remounts the app fresh, which reloads everything from storage, so it opens on the sign-in screen with every student and educator intact. The browser test crashes the app on purpose through a test hook (__eduTest.crash), sees the guard, taps Go home, signs the educator back in and carries on (181 checks; the on-purpose crash's own console lines are excluded from the no-errors tally). The back sweep counts the guard's words as a failure too, so a screen that crashes on back still fails the build rather than hiding behind the card.
- (2026-09-24, pass BY) Mikey's notes on the story book. The card is titled Let's Read!, the picker's text is centered, the buttons read Open Book and Print or Save, the cover's top line is EduSphere and its subtitle is the grade alone. The picker had listed only courses with a long story, which left out pre-K and kindergarten; it now lists every course with any story, youngest grade first and then in the order the grade's courses appear, so the list reads pre-K, pre-K, K, K, grade 1, grade 1 down to college. Printing was broken: the sheet was lifted out of the flow with position absolute inside a hidden body, which printed one page with the cover pushed down; the book now prints in place, with the chrome hidden, the page and wrap stripped of their screen height and background, the cover on a page of its own and every story breaking to a fresh page. The browser test prints the grade 4 math book to a real PDF (tests/e2e/out/story-book.pdf) and checks the page count and that the first page holds the cover alone (183 checks).
- (2026-09-24, pass BZ) Mikey's notes on the printed book, from his phone's Save as PDF preview. The card's line now reads "Open it, print it or save as a PDF for a class that likes to read on paper"; Print or Save is a link in the same place as Back to Story Log. Printed, short stories (under eighty words) go three to a page with even space between them, while a long story keeps a page of its own; the page's sky (the fixed star field that had printed on every sheet) stays on the screen. The cover carries arcs and scattered dots in the theme's green and gold, drawn from a fixed seed so every printing matches, on screen and on paper. The browser test prints the first pre-K book and checks that page two holds stories 1, 2 and 3 and not 4 (184 checks).
- (2026-09-24, pass CA) Mikey's yes to 1, and his word on the sky: it may print behind the stories as long as it never reaches the words. Every printed page after the cover now ends in a footer with the course title and its page number (Very first steps · Page 2 of 5), rendered per page group since the book sets its own page breaks and Chrome cannot count pages from CSS; the footer is paper-only. The page sky prints again, fixed so it repeats on every sheet at half strength, behind story cards that are opaque, so no dot touches a word. The browser test checks the footer on page two and the last page of the pre-K book (185 checks).
- (2026-09-24, pass CB) Mikey's word on the printed book: the paintings print by default when they arrive, and the layout should look pleasant, with enough space to tell one story from the next and no more. A story whose painting exists (the page's art list) takes a page of its own, because a picture is half a page; unpainted short stories still share a page three at a time, now stacked from the top with a third of an inch between them and the footer pinned to the foot, instead of being spread evenly down the page. The browser test pretends the first pre-K story's painting has arrived and checks that it prints alone on page two while stories 2 to 4 share page three (186 checks).
- (2026-09-24, pass CC) Mikey's yes to 3 and his word to keep building the moat, so the printed book became a classroom product in one pass. Two older stories a page: an unpainted story of eighty to 260 words shares a page with one more in a slightly smaller face (13.5px body, 18px title), which halves the paper for grade 3 and up; short ones still go three a page, and a painted or very long story keeps its own. A class set: an educator with students sees "Print one for each student (N copies, P pages each)"; checked, the book prints once per active student with "This book belongs to <name>" on each cover and the name in every footer, each copy starting on a fresh sheet, as one print job. Read the whole book to me: a reader above the sheet speaks every story in order, scrolls the one being read into view and says "Reading story N of M", with the snail and the hare when the course is an early-years one. The browser test prints the grade 4 book (stories 1 and 2 on page two), a two-student class set (two covers, twelve pages), and starts the reader (189 checks).
- (2026-09-24, pass CD) Mikey's yes to 3, two print notes, and a new standing program. The printed book ends on a page titled What these stories teach: the state's standards each story serves, pulled from the standards map for the educator's framework (TEKS in Texas), one line per code under the story's name; the page counts in the footers. The paper has more room: the page margin is 0.9in at the top and 0.85in at the sides. And the program: every story in the app is to be doubled, starting at pre-K, still on the Gladwell arc (a named child, a snag, the idea, an echo, now with more context so a story with numbers in it stays easy to follow), with up to six pictures a story so a student has more to look at; printed, one or two stories a page (up to 260 words share, longer or painted stand alone). Pre-K 3 is done: seventeen stories at sixty to a hundred words in six or seven short beats, each with six pictures (51 new serials, S1364 to S1414, appended to the ledger with the subject template), under the read-aloud rule. tools/story-length.mjs writes docs/STORY-LENGTH.md, the list still to double (437 of 454 module stories); driver v3 keeps a story's old picture serials, mints new ones after the ledger's last, and appends their rows. Scale, said plainly: at fifteen to twenty stories a pass this is twenty-odd passes, so the early years go first.
- (2026-09-24, pass CE) Mikey's yes to all three, with the word that the stories come first from here on. Pre-K 4 (twenty-six stories, Mike's Three stones among them, the driver now reading the older literal form a cast story is written in) and all of kindergarten (math fourteen, reading fourteen, science, civics and health fifteen) are doubled: sixty to a hundred and twenty words in six or seven beats, six pictures each, under the read-aloud rule; 207 new picture serials, S1415 to S1621, appended with the subject template. The picture-spread rule is a hard test: a story with six pictures keeps them on at least four different paragraphs with one in each half, which caught twenty-six batches' worth of pictures bunched in the first half and moved them. docs/STORY-LENGTH.md reads 368 of 454 still to double; grade 1 is next.
- (2026-09-24, pass CF) Mikey's three print notes and the next batch. Titles are in title case everywhere a story shows (the Chicago and AP habit: every word capitalized except short articles, conjunctions and prepositions in the middle; a title already in capitals kept), through one titleCase in logic applied to the story page, the story log, the Let's Read fold, the weekly note and the book's covers, headers and footers; the printed book centers every line, the standards page included. The sky reaches the paper's edge: the page margin is zero and every printed page is one group sized to the sheet with its own inner padding (0.85in top and sides, 0.6in foot), the cover included, so the fixed sky paints the margins too; the story cards stay opaque. Grade 1 is doubled: twenty-six stories, six pictures each (S1622 to S1699). docs/STORY-LENGTH.md reads 342 of 454; grade 2 is next.
- (2026-09-24, pass CG) Mikey's notes and the next batch. Picture counts by band, the call being mine: pre-K four pictures a story, kindergarten to grade 2 five, grade 3 up six, and never two pictures back to back (every extra picture sits after a different paragraph; a hard test, with the spread test loosened to three extras and the length report counting against the band); 155 pictures came off the pre-K and kindergarten stories done earlier and their ledger rows went with them. The printed cover sits just above the page's center and carries a subtitle: the goal all of a course's stories add up to, one line per course in COURSE_GOALS (81 courses, "Counting with confidence and seeing how numbers grow and shrink", "Reading with judgment: theme, point of view, evidence and argument"). Grade 2 is doubled: twenty-three stories at a hundred to a hundred and forty words, five pictures each (S1700 to S1745; Mia's Stacks of pennies, the model Mikey named, grew with the rest and kept its shape). docs/STORY-LENGTH.md reads 319 of 454; pre-K through grade 2 are done, grade 3 is next.
- (2026-09-24, pass CH) Mikey liked the subtitle and moved it: title, then the goal line, then the grade. He is moving the work to a new chat on a cheaper model, so the handoff was rebuilt: docs/HANDOFF.md is the prompt to paste (roles, the vision in the order it was built, the doubling program with its targets and method, the end-of-pass ritual, how Mikey likes to work, the hard rules) plus the map of the repo; the story batch driver left /tmp for tools/stories/batch.py with tools/stories/batch-example.py, proved by re-running an already-doubled story with no change to the store or the ledger. Nothing crucial lives only in this chat's memory now; the repo carries it.
- (2026-09-21, pass G) Questions show the picture their words describe: fractions draw the bar with the eaten parts shaded before the answer; negative numbers draw a number line from minus twelve to twelve with the numbers in the question marked (gold for the first, blue for the rest), the temperature question runs minus twenty to eight, and the opposite question's explanation marks both the number and its opposite; triangle and parallelogram area questions draw the shape with its base, height and slanted side labeled (the missing height shows a question mark, then the answer). `NumberLinePic` takes `from`, `to` and `marks`; the older zero-based number line (`parts`, `mark`) is unchanged and both coexist in the rules test.
- (2026-09-21, pass G) A story with the core cast carries a `where` line, one quiet italic sentence under the title saying where that person stands in their life, never an age ("Savanah, still on the farm. The city is a few years off."). Stories without the cast carry none. A story ends with one approved Wonder question for its course, the same one every time (seeded by the module id), shown only when the student's Wonder switch is on; nothing is typed or kept. `wonderFor` had been shadowed by a classroom state variable (now `wonderPopupFor`); the browser test caught it.

## 2026-09-24 (pass CI): the printed book with paintings, grade 3 batch 1, pictures checked against their words

- Mikey's two notes: the Let's Read card on the Story Log is deep green with a white title, so it stands apart from the student cards, and it never prints with the log. The option on top of the story book to print one copy for each student is gone, with its copies, owner lines and names in the footers.
- Grade 3 batch 1 of the doubling program: 18 stories (fractions, reading skills, science) at 240 to 260 words with six pictures each, new serials S1746 to S1799. They stay at or under 260 words so two still share a printed page before any painting arrives. Choices: the main-idea boy is Mateo with his abuela; the tadpoles grow back legs at about six weeks, not two; the clay garden is fixed with compost, not sand, which can make clay worse; the prefix story uses a word creature in costumes so no picture needs legible writing; closing pictures show the idea of the last paragraph rather than the cast smiling.
- The printed book with paintings (Mikey: no huge gaps, and a picture never on a different page from the words it shows; the design is my call). A painted story may now take more than one sheet. printParts in logic.mjs splits it between paragraphs, never between a paragraph and its picture, using sizes measured from a real print (PRINT_PAGE). On paper a painted picture floats beside its own paragraph at two fifths of the width, sides alternating, the main painting prints about half wide, and every painting prints four by three whatever shape it arrives in, so the estimate holds. On the grade 3 fractions book with all thirty pictures pretend-painted, three of five stories fill two sheets at 86 to 87 percent and two need a short third sheet; nothing is cut off. The sizes get re-measured when real paintings arrive. Print or Save now loads every painting before printing, since pictures load lazily on screen, and one story printed from its own page or the Story Log prints in place instead of being lifted out, so a two-sheet story keeps its second sheet.
- Pictures checked against their words (Mikey): tools/story-art-audit.mjs lists every picture whose description shares no meaningful word with the paragraph it follows (for the main picture, the title and first two paragraphs), in docs/STORY-ART-AUDIT.md. A one-time pass moved 151 pictures that sat one paragraph away from the words they show, keeping the picture rules. 132 of 1,706 stay flagged, mostly in stories not yet doubled (their pictures are rewritten when the doubling reaches them) and closing pictures after an echo paragraph. A synonym can fool the check, so a person reads each row; it is a reading aid, not a test yet.
- Long stories join the doubling queue (Mikey): 400 words and six pictures each, done after each grade's short stories. STORY-LENGTH.md lists all 62. The stories test still caps them at 350 words and three paragraphs; that loosens with the first doubled long story.
- Planned, not built: a game attached to every course with many more kinds and the same kind held far apart, and a coloring page placeholder with a Leonardo prompt for every early-years module. Both are next passes, one piece per pass beside each story batch.

- Story pictures on screen (Mikey: pictures and their words side by side, alternating sides, sliding in as you scroll). From 720px wide, each picture sits beside its paragraph at two fifths of the width, sides alternating, and paragraphs without a picture run the full width. Phones keep the picture under its paragraph: beside a picture, a phone's column of words runs about twenty characters a line, too short for a young reader to track. Each picture eases into place once as it scrolls into view (IntersectionObserver adds edu-in; a picture beside words comes in from its own side, a stacked one rises), never loops, and is simply in place under reduced motion and in print. Native lazy loading already downloads a picture only as it nears the screen. The class is edu-par-pic, because edu-side was already the fixed side art and its display none hid every picture on the first try.
- Audit after the batch fixes: 153 pictures moved one paragraph to the paragraph they show, and 127 of 1,706 are still flagged. About 50 of those are early-years stories, many of them letter and number pictures whose descriptions spell the letters, which the word check cannot match; they are reviewed next pass.
- Standing direction (Mikey): he says continue and the next pass starts from the checkpoint and docs/HANDOFF.md. Claude also works as the education expert (research-backed technique, his fun, concise, clever tone with sentence lengths varied on purpose) and as the motion designer (Leonardo.ai-like movement, judged against distraction for young learners). New code gets plain-language comments. A security pass comes at the end, before the paintings.

## 2026-09-24 (pass CJ): grade 3 finished, early-years pictures matched, the license widened

- Grade 3 batch 2: 18 stories (writing, math, art, technology, history) at 241 to 260 words with six pictures each, serials S1800 to S1853. Every grade 3 short story is now doubled. Accuracy got a second, separate check (Mikey: one wrong answer costs all credibility): every number a story states is re-computed in the batch check, and every fact was read against its lesson. Fixed while doubling: the paper boat now gives all seven real folds (the old four steps only made a hat); the rule-or-law story no longer says a walk signal law comes from Congress (it is a Texas law, made by the legislature in Austin); the puppy story no longer states how much water a puppy needs (the girl measures what hers drank); pictures no longer ask Leonardo to draw numbers or signs; repeated names across grade 3 were changed so each story has its own kid.
- Early-years pictures: 49 pictures in pre-K to grade 2 were rewritten to show the paragraph they sit beside, as scenes rather than letterforms wherever the old description asked Leonardo to draw letters. The audit now flags 77 of 1,760, none in pre-K through grade 3.
- License (Mikey did not object to the proposed wording): LICENSE.md now forbids using any part to train, fine-tune, test, evaluate or prompt an AI system or to build a dataset for one, and forbids scraping, crawling, mirroring or bulk downloading; NOTICE.md says the same in one line. A robots.txt waits for a custom domain, because crawlers only read it at a domain's root.

## 2026-09-24 (pass CK): grade 3's long stories doubled

- Grade 3's eight long stories are doubled to 401 to 405 words in eight paragraphs, each with five more pictures after five different paragraphs, serials CS63 to CS102. The stories test now accepts a doubled long story (six or more paragraphs, up to 460 words, five pictures spread over both halves) and still holds the rest to three paragraphs under 350 words. The ledger test counts long-story pictures. The driver is tools/stories/long-batch.py, because long stories are one-line entries in COURSE_STORIES.
- Facts fixed while doubling, after a second check of every number and claim: the robot birthday no longer has a toy robot near matches (it forgets to take the cap off the juice); the science fair goes indoors when thunder roars, and thunder is felt and heard at the same moment rather than felt first; Frederick's ant farm arrives without a queen, so it cannot grow by hatching, and his big-number sum counts trips into two backyard anthills instead; a red sail is the brightest color in Chloe's painting, not the lightest value; Mike's traffic light needs the state's permission because the corner sits on a state highway. Pictures no longer ask for numbers, maps with names or boards with writing.
- The printed book now splits any story too long for one sheet, painted or not, between paragraphs. A doubled long story is about twelve inches of text on a nine-inch page, so it would otherwise have been cut off.
- The coloring page for every early-years module moves to pass CL, so each pass stays small enough to check properly.

## 2026-09-24 (pass CL): a coloring page for every early-years lesson

- Mikey asked for a coloring page placeholder and a Leonardo prompt for every early-years module. COLOR_PAGES in src/stories.mjs gives all 135 pre-K to grade 2 lessons a page, serials D28 (red-and-blue) to D162 (habitats) in grade order, and docs/ART-REQUESTS.md has a new section with a prompt for each in the drawing-page style.
- Each scene shows its lesson's idea with no letters or numbers drawn: letter lessons get objects that start with those letters (a goose and a gosling for big and small G, a van, an apple and a nest for V, A and N), counting lessons state their counts in words, and clocks and signs are asked for without numbers or words. Image models miscount, so fifteen rows carry a check note saying what to count before saving. Flags with exact star and stripe counts were left out on purpose: the flag lesson's page is a fireworks night, and the two-flags page is the Texas flag alone.
- In the app, a page joins Let's Color under From your lessons once its lesson is passed and its art exists (lessonColorPages in logic.mjs); without art nothing shows, so nothing changes on screen until the paintings arrive. Each page is titled by its lesson and opens on the coloring screen like the drawing pages. The stories test checks one page per early lesson, D28 upward with no gaps, scenes without numbers, and the listing rule; the ledger test knows the new serials.

## 2026-09-24 (pass CM): grade 4 batch 1, and a commit message for every pass

- Grade 4 batch 1: 17 reading, science and writing stories at 240 to 260 words with six pictures each, serials S1854 to S1904. The second check found a real error in the old dictionary story: it put jungle between the guide words jug and jump, but jungle comes after jump. The page is now jumpy to junk, and the batch check tests the alphabet order itself. Also fixed while doubling: weathering now breaks the rock of a cliff instead of a sandcastle, heat travels up a metal spoon instead of through a floating cork, the mouse card drops three strings because the snake eats mice too, and the heat test names a wooden spoon so there is something that insulates.
- Commit messages (Mikey: he updates by phone and commits every few passes). docs/COMMIT-MESSAGE.md now carries a GitHub title of 72 characters or fewer and one line per pass since his last commit, ending with a Passes line. No GitHub connector exists in the directory and the build sandbox has no internet, so Claude cannot watch the repository; the baseline is the last pass Mikey says he committed, assumed to be CH, and it resets when he says he committed.
- The games step moves to pass CN with grade 4 batch 2, so this pass stays small enough to check properly.

## 2026-09-24 (pass CN): grade 4 finished

- Grade 4 batch 2: 21 stories (Texas history, math, art, music, health) at 240 to 260 words with six pictures each, serials S1905 to S1966. Every grade 4 short story is now doubled. Every number the batch states is re-computed in the batch check (23 times 14, 97 divided by 4, the factors of 12 and 13, 0.7 against 0.65, the note values in a measure).
- Errors found in the old versions and fixed: the note-lengths story said a half, a quarter and four eighths make four beats (they make five; it is now a quarter and six eighths); the Texas statehood story said the star came off the flag during the Civil War (the United States never removed the stars of the states that left); the decimals story said a quarter reads twenty-five cents (it reads quarter dollar); the Texas Revolution story was titled as if the Alamo was lost on purpose and put the siege after the declaration (it began February 23; the declaration came March 2, during the siege); the first Texans story and its lesson placed the Comanche in Texas long before any flag (they rode onto the Texas plains in the 1700s on horses that came with the Spanish; the lesson line now says so); the heart story judged a boy's fitness against a grandfather who had walked the stairs (it now compares the boy with himself after a month of daily play, and says what recovery is).
- The games program gets its own focused pass next (CO), after being held back twice for story batches.

## 2026-09-24 (pass CO): a game for every course

- Mikey asked for a game attached to every course, the same kind held far apart as courses finish, and many more kinds. COURSE_GAMES in logic.mjs now gives each of the 81 courses one or two games, chosen by subject and grade; finishing the course unlocks them (unlockedGameIds: the starter, the first game on a student's list, and the games of finished courses). Let's Play lists the games of a student's own courses, and the educator walk-through opens them all. Star is the starter game, open from the first day.
- 28 courses had no game that suited them, so each got a quick fire built only from its own lessons (sprint-<course>; sprintPool keeps to the course).
- Inside a grade the same kind is avoided wherever today's fourteen kinds allow. The rules test holds two ceilings, 28 kind repeats inside grades and one course holding two games of one kind (science 11), and they may only go down. docs/GAMES-PLAN.md lists every course's games and the six kinds to build next, each one making the course's idea the rule of the game: sentence builder, fix it, color mixer, debug the robot, history timelines and a periodic table game.
- The first attempt at this pass was cut off after its code was finished; this attempt checked it (ALL CHECKS PASSED) and delivered it.
- Name and domain (Mikey: edusphere.com is taken, edusphere.study is free). Advice given: new endings load in every browser and take HTTPS like any other, but some school web filters distrust new endings, and the name EduSphere is already used by several education products, including an adaptive learning platform, so a new name with its own .com is worth choosing before launch. Mikey decides; the name is not changed in the app.

## 2026-09-24 (pass CP): The Wise Human

- Mikey renamed the product The Wise Human (he owns the .com), with the tagline Where Knowledge Meets Wisdom, after EduSphere turned out to be used by several education products. He chose the night animated logo from the design canvas.
- Everything people see now says The Wise Human: the logo, the install name (short name Wise Human), the page title, the life skills note, the report footer, the backup picker and share title, certificate and backup file names (wise-human-...), the passkey name, the license, the notice and the readme, and every file's header. Nothing that stores data changed: the storage keys (edusphere_v1_*), the story pace key, the history marker and the build file keep their names, so every student's progress carries over. A backup now says The Wise Human, and a backup made before the rename, which says EduSphere, still loads (the backup-forever fixtures prove it).
- The logo component was rebuilt from the chosen design: a gold sphere and a tilted orbit that draw themselves, the words The, Wise and Human stepping down to the right and landing on the old logo's timing (0.5, 0.75 and 1 second), the tagline sliding in at 1.9 seconds, and then a gold planet circling the orbit every 16 seconds, dimmer while it passes behind. Only the welcome screen moves; certificates and other screens show it still, and so does every device that asks for less motion. The words use the reader's own serif fonts (Iowan Old Style, Palatino, Georgia), so no web font is downloaded and nothing leaves the device. The design canvas used Fraunces; embedding it would need the font file, which is free under its license.
- The dark theme moves to the next pass, as planned, with Mikey's note that Let's Color, Let's Read and Let's Play stay colorful and attractive in both themes.

## 2026-09-24 (pass CQ): the dark theme

- Two palettes live in THEMES (light is the palette the app always had; dark is the logo's night green with ivory words and brighter green, gold and clay), and C is the live palette every screen reads. applyTheme swaps it in place and rebuilds the shared style objects (page, frame, card, tip boxes, link buttons, the game board) from their recipes, and the style sheets are now built from the live palette each time the page draws. New colors: onAccent (the words on a green, gold or clay button: white in light, near-black in dark), frame, paper (story pages), glass (the welcome card), tip, veil (picture placeholders), panel, and the logo's words, orbit and tagline, so the dark logo matches the night design Mikey chose.
- The switch is a sun and a moon in one pill: in the top corner of the welcome page, and in a row of its own at the top of the Classroom page, because that page's popups reach the corner (a first version sat over the Add student popup's close button and the click-through test caught it). Until someone taps it, the app follows the device's setting; after that it remembers the choice on this device (edusphere_v1_theme).
- The printed book, certificates and transcripts always draw light, so paper gets dark ink on white.
- The color rows stay colorful in both themes, as Mikey asked: the bright rainbow wash is unchanged, with dark words inside it; the soft pastel wash turns to deep jewel tones in dark so ivory words read on it.
- A new browser test (tests/e2e/dark-contrast.mjs, step 12 of check.sh) taps the moon, visits every screen, and measures every word against what it sits on: 4.5 to 1 or better (3 to 1 for large words), the web's accessibility standard. Its first run found 14 screens with low words (old green written in directly, white words on the brighter dark green, light chips and panels, the picture placeholders); all were fixed. It also checks that paper screens stay light, that the color rows open with readable words, and that the choice survives a reload. Words over a picture or a gradient are left to the eye.

## 2026-09-24 (pass CR): dark by default, and the contrast holes Mikey found

- From Mikey's phone screenshots: the app now starts dark on every device (it no longer follows the device setting), the sun and moon switch lives on the welcome page only (removed from the Classroom page), and each science experiment sits in its own card.
- Why the holes got through: the dark-theme check skipped any word sitting on a gradient, and the Classroom cards (Active Students, Wonder Questions, Reading Lists, Science Experiments, Life Skills) and the report's key-words card were light gradients, so ivory words sat on light cards. The Story Log was never really visited, because it only loads its rows from its own button, and a picker's words were never measured. The check now treats a gradient as every color it passes through and requires the worst one to pass, measures every visible picker, and opens the Story Log by its button (31 checks).
- The fixes: tint tokens in THEMES (mint, sage, blue, rose, amber, plus a heading and a deep tint) that are the old light gradients in light and deep versions of the same colors in dark; pickers name their own word color and ground; form controls take the dark color scheme in dark; a dark screen that prints comes out as dark words on white.
- The rainbow on Let's Color, Let's Read and Let's Play was failing in both themes: dark words on its purple read at 2.8 to 1. Its red, purple, blue and green are now softer (#EE7B5B, #A58BD1, #6FA8DC, #7CC26B) and its words and arrows use the dark ink in both themes, so every color passes.
- The other browser checks (click-through, back sweep) were written for light and now start in light on purpose; dark has its own check.

## 2026-09-24 (pass CS): one clear next step for young learners, and pickers that draw their own words

- Mikey asked for the young learners' animations to be a little more apparent: hold their hand and make it obvious, but not booming. What was there was right in spirit and too faint: the breathing ring on a course group was dark green at 18% (invisible on the dark theme) with a 1.5% swell, every group with work in it breathed at once, and every ready module's button pulsed at 2.5%.
- Now there is one next step. While every group is closed, only the first group with something ready breathes, with a gold ring (gold reads in both themes) at 35% and a 2% swell. Inside an open group, only the first ready module's button invites a tap: a 4% pulse and a soft gold ring that ripples out every two seconds. The modules rise in one after another when a group opens. All of it is transform and opacity only, and with less motion asked for, nothing moves and the ring stays still around the button, so the hand-holding survives.
- The page carries data-young=1 while a young learner is on, so older students' screens are unchanged.
- The book picker under Let's Read showed dark words on its dark field on Mikey's phone even though the desktop browser drew them ivory. Every picker now draws its own words, ground and arrow (appearance none, the text fill set to the ink color, a chevron drawn in the same ink), so no phone's own picker styling can recolor the words.
- Also fixed: a student's name button under a mouse pointer turned pale green in the dark theme, with ivory words on it; it now deepens instead.
- The dark check (35) now also proves one breathing group and one inviting button for a young learner, and measures pickers on their own ground.

## 2026-09-24 (pass CT): no muddy browns, a weekly note header, lifted panels

- Mikey called it almost perfect and flagged three things on his phone: the dark browns (the Transcript card and other warm areas) looked bad, the weekly note's header needed contrast, and the Wonder Question review wanted mild contrast between its panels.
- Why brown: any dark gold, amber or coral fill is brown, and gold or coral laid over the night green reads olive or rust. So in the dark theme warm color moves to the edges: gold cards (goldSoft) are now a lifted night green (#233B31) and their borders use goldEdge (gold at 55%); the amber (Life Skills) and rose (Science Experiments) cards are night green with a faint warm glow in one corner (10% and 9%) and a warm edge. Light theme colors are unchanged. Lesson diagrams are drawn as paper in both themes, so their gold squares use the light gold (#F7EAD1) directly rather than the token. The dark story paper is a neutral night green (#1F2D27) instead of near-black brown.
- Panels inside a card were the same shade as the card (greenSoft and panel were both #1F3A31 against #1D302A). greenSoft is now #22413A and panel #25463B with a hairline (panelLine), so the Wonder Question's panels read as panels.
- The weekly note's header is a band of its own (the heading tint with a hairline under it), the title bolder, and Edit and Print in the accent color rather than muted gray.
- The small Awaiting review pill keeps its dark coral: changing it to a see-through coral dropped its words below 4.5 to 1.

## 2026-09-24 (pass CU): gold edges, every open door glowing green

- Mikey marked up twenty phone screenshots. Cards get the logo's gold at their edges in the dark theme (the welcome card, student cards, the Classroom's feature cards, the Quick checks and walkthrough card, inactive students, reading list and experiment cards, the modules-mastered card, question, lesson and story cards, the PIN card, Wonder Question panels, My Completed Skills and every pop-up). It is done with tokens: cardEdge on the shared card style, softEdge where a card's edge matched its fill, and the dark tint, panel and paper lines all set to #D9A441. The light theme keeps its quiet lines; module cards keep their status colors (green ready, gold mastered).
- The reading list's grade band and its books now differ: the books sit on a darker body (listBody) under a gold rule. Pop-ups sit on a deeper scrim (scrim) with a gold edge, and Add someone new has a title band of its own.
- Young learners: Mikey reversed my one-step-at-a-time cue. Every group with something ready breathes, every ready module's Open button pulses with a ripple, and Let's Color and Let's Play breathe when they have something open and disappear for a young learner when they don't (locked modules and work waiting for another day were already hidden). The ring changed from gold to a light, bright green (107, 255, 184 on dark; a deeper 26, 186, 110 on the light theme so it shows on ivory). The dark check proves every ready group breathes and every ready module pulses.

## 2026-09-24 (pass CV): the light theme exactly as it was, and Mikey's dark theme notes

- Mikey: don't touch the light theme, and put back anything the dark theme work changed in it. A pixel comparison of 28 light screens (the pass CO build, before any dark theme work, against this one; motion frozen, the same random seed, 1% tolerance) found the light changes: experiment cards (light had divider lines), the reading list's edge and gold rule, pop-up edges, the Add someone new title band, the weekly note band, the Wonder Question panel edges, the custom pickers and two picker colors, the welcome card's edge (#E3EBE2), and the rainbow's colors and arrows. All are back. Each palette now names its mode (C.mode), and every dark-only look checks it. After the fix the comparison shows only the brand changes (the new logo and name), the light and dark switch on the welcome page, and the recovery code, which is random per device.
- Kept in both themes on purpose, because they are how the app behaves rather than how the dark theme looks: the young learners' guide (green glow on everything they can open, rows that step aside when nothing is ready) and the Classroom page without a theme switch.
- Mikey's dark notes: Let's Color, Let's Play and Let's Read keep only their changing colors, with no frame and no ring (they already move); the Active Students tab has its own quiet edge (tabEdge) while the card around it keeps the gold; the recovery code card has a subtle yellow wash over the night green; the young learners' glow is softer (a 1% swell and a 4px ring at 50%, the button pulse 2.5%, the ripple to 1.07) but still easy to see.

## 2026-09-24 (pass CW): the white bars on wide screens

- Mikey's laptop showed titles, intros and plain links sitting on white bars in the dark theme. From 700px wide, loose text outside the tiles carries a backing in the page's own color so the starfield never shows behind words; that backing was hard-coded to the light page color (#F5F7F1). It is now a variable (--edu-plate), #F5F7F1 in light (unchanged) and #14231E in dark.
- Why the check missed it: the dark check only ran at 360 wide, where the backing does not exist. It now walks every educator screen, the Story Log and the welcome page again at 1280 (49 checks); run against the previous build it fails on exactly those white bars. Two things the wide walk needs: on a wide screen the Classroom starts its laptop tour the first time it is shown and holds the page there, so the walk skips the tour as an educator would (the same happens in the build from before the dark theme; it is the tour, not a fault), and it taps an empty edge now and then because an educator is signed out after a quiet minute.
- Dropdown rows get no gold while closed (Mikey): the reading list and experiment grade rows show the gold edge on the outer card only once opened. The light theme is unchanged (the light comparison still shows only the brand changes, the welcome switch and the random recovery code).

## 2026-09-24 (pass CX): mint edges, playful feature cards, paper-board games

- Mikey: every gold card edge in the dark theme becomes the Let's Read card's light green (#7CC3A6); the logo keeps its gold, and status edges keep their meaning (a mastered module's gold edge stays, since the ready module's edge is already that green).
- The Classroom's four feature cards (Wonder Questions, Reading Lists, Science Experiments, Life Skills) get light, playful fills with no green in them and no contrasting edge: lavender #C9B8F0, sky #A9D1F5, peach #F7B9A1 and light yellow #F6E3A1, with dark words (featInk). They are new tokens (feat*) so the other cards that shared the old tints are untouched. The recovery code card is the same light yellow with dark words.
- Games: the board was always white (GAME_BOX), but everything drawn on it read the dark palette, so words turned ivory on white (Quick fire, Solid or liquid, In order, Frog Jumps, Balance) and tiles turned dark. Games now draw with the light palette (B = THEMES.light, 89 uses inside the game components; Btn takes a palette), the board's words default to the light ink, and its edge is the green in the dark theme. A sweep of one game of every kind (15) in both themes was compared by eye; the dark check now opens three word-carrying games. The path games' squares hugged the left edge of the board (the grid is 94 of 100 units wide); they are centered now, in both themes.
- The light and dark switch is back on the Classroom page, top center, level with Home and Sign out (a compact switch in a three-column row). The change-state screen's title, picker and subtitle are centered. The speaker button in the dark theme has no ring and no fill, so it sits in the card. The Classroom switch, the change-state centering and the path game centering show in the light theme too, since they are layout and a control rather than dark theme colors; the light comparison shows nothing else new.

## 2026-09-24 (pass CY): almond paper, softer cards, and the last contrast marks

- Mikey loved the feature card colors and asked for the same shades a little lighter: lavender #DCD1F5, sky #C7DFF8, peach #FAD1C2, light yellow #F9EDC2 (about a third of the way to white).
- Paper in the dark theme is almond (#EFE8DC, token paperBoard; white in light): game boards, the coloring page and its unpainted parts, the picture and game tiles and their thumbnails. Lesson pictures and diagrams now sit on that paper and draw with the light palette (Picture wraps PictureInner in the dark theme; the diagram components and two small pictures use B, which now lives beside C), because the drawings were made for paper: dark labels on a dark card were the hard-to-read text Mikey saw. The paper color stays a JavaScript token rather than a CSS variable in SVG attributes, because Safari's support could not be checked here and a failed variable paints black.
- A story picture still to come shows the dark placeholder in the dark theme instead of the lesson drawing (Mikey prefers it); the light theme keeps the drawing.
- Slight contrast where Mikey marked: the Transcript card is light peach with dark words (transcript tokens), the mastered lists in the Summary (and the notes list, which shares the style) are light green with dark words, the weekly note's body is lifted a shade, the Assigned Now grade rows use the panel shade, and the Let's Read note in the student's Let's Read row is light green with dark words. The reset card is centered (in both themes, since it is layout) with the green edge in the dark theme.
- A Back to my courses link sits at the end of a course story, the one story screen with no buttons under it.
- The light comparison shows nothing new beyond the Classroom switch and the centered state screen from pass CX.

## 2026-09-24 (pass CZ): feature card buttons in their own colors, centered boxes

- Mikey asked for better buttons than green on the four pastel feature cards. In the dark theme each button is a deeper shade of its card with white words: violet #6246C2 on lavender, blue #1F64A8 on sky, terracotta #B8472A on peach, amber #B45309 on light yellow (white words at 4.6 to 6.6 to 1). The light theme keeps its buttons.
- PIN boxes (PinInput) and the device name boxes center their words, and the note after Merge into... is centered. These are layout, so they show in both themes; the light comparison shows only these and the earlier known differences.

## 2026-09-25 (pass DA): centering, softer buttons, almond typing boxes

- Feature card buttons keep their hues a step softer (violet #6A58B0, blue #3A6A9A, terracotta #A85A45, amber #9C6628; white words 4.8 to 5.8 to 1); the Transcript button takes the peach card's terracotta. Softening kept the darkness, not lightened toward white, so the white words still pass and the buttons still stand out from their pale cards.
- In the dark theme every typing box (inputs other than checkboxes, radios and sliders, and text areas) is almond #EFE8DC, the note box and the note search among them. The weekly note's body is light green with dark words under its dark header band.
- Centered: Enter your PIN, every tip (tipStyle), the Wonder review sheet (status, prompt, the youngest-children panel with lines between its heading, summary and two voices, Answered by and its choices, The four voices label and each opened voice, the closing question, the buttons), the Reviewed toggle and the question rows. The four voices' rows stay left-aligned. A removed question's row was light gray under ivory words in the dark theme; it uses the panel shade now.
- Un-approve all is now Remove All (Mikey's wording). It does what it always did: every approved question goes back to waiting for review, hidden from students; the click-through check follows the new name.
- The Story Log title reads Name's Story Log, with more room above the Most recent, Read and Unread toggle and above the All students picker. Layout changes show in both themes; the light comparison shows only those.

## 2026-09-25 (pass DB): a new tab icon, dimmer buttons, the Wonder review's last touches

- The browser-tab and home-screen icon was still the EduSphere mark. The new one is a night green rounded square with the gold ring, a bold ivory W and the gold planet sitting on the ring, drawn to fill the square so it reads at 16 pixels (checked at 16, 32 and 64). It is the inline favicon in tools/build-site.mjs and the four installed icons (180 for iPhone and iPad, full square; 192 and 512 rounded; 512 maskable with the mark in the middle 78%). The Claude preview's own tab takes an emoji, so it shows the ringed planet 🪐.
- The five colored buttons (the four feature cards and the Transcript) are dimmed again: violet #695D98, blue #4B6B8B, terracotta #905E51, amber #88633A, white words above 5 to 1.
- Wonder review: the line under What the youngest children hear is gone (Mikey meant the line between the summary and the two voices, which stays); the four voices' rows are centered with the arrow at the right edge; the closing question is light green with dark words in the dark theme.
- Fraunces: Mikey sent Google Fonts' embed code. It was not added: the link makes every device that opens the app contact Google (its address goes with each request), fails offline and on school networks that block it, and European courts have fined sites for it. That breaks the app's nothing-leaves-the-device promise. The font files themselves (Download all on Fraunces' Google Fonts page, free under the Open Font License) can be built into the app so nothing leaves the device and it works offline.

## 2026-09-25 (pass DC): the logo in Fraunces, built in

- Mikey uploaded the Fraunces download (variable roman and italic plus statics). fontTools pinned the variable fonts to the design (Wise and Human at weight 600 and optical size 100, The in italic at weight 400 and optical size 38, soft 0, wonk 0) and kept only the logo's letters, as WOFF (the sandbox has no Brotli for WOFF2): 2.8 KB and 2.1 KB. They sit as data inside the logo's own drawing (an @font-face in the SVG), so the welcome page, the transcript and a saved certificate picture all get the face, offline, with no request leaving the device. The reader's serif fonts remain the fallback.
- Fraunces is under the SIL Open Font License 1.1 with no Reserved Font Name, so a subset under the name Fraunces Logo is allowed; the license ships in licenses/Fraunces-OFL.txt and NOTICE.md credits it.
- The Forgot my PIN link is centered (layout, both themes).
- The standing reminder to upload the font ends: the font is in.

## 2026-09-25 (pass DD): the two short voices, one idea to a line

- In the Wonder review's youngest-children panel, each short voice now reads as its name on a line of its own with a colon (A scientist says:), its sentence on the line under it, and a clear gap before the next voice, all centered (Mikey's layout). Layout, so both themes.

## 2026-09-25 (pass DE): sentence builder and fix it, the first two new kinds

- Mikey committed CI to DD; this commit message starts fresh at DE. He will say continue, committed or continue, not committed.
- Sentence builder (kind build): each set is a short sentence and a longer one that says more; tiles are tapped in order, a placed word taps back with every word after it, and the sentence is checked by its words so two tiles that say the same word can trade places. Every sentence has one capital (its first word, I aside) and one end mark (its last), so both ends are pinned, and each was written to have one order that makes sense: adjectives before one noun only, in English order (size before color), and the joined and opened sentences keep their comma on the tile before the join. Decks: describe (grade 3), join (grade 4, and, but, so), open (grade 5, because, when, although, if, since); five sets each, three a round.
- Fix it (kind fix): each sentence carries one wrong word, capital or mark, named by its spelling (it appears once; a first draft that counted positions miscounted two, which the spelling rule makes impossible), its fix and the rule in a sentence. Decks usage7, usage9, usage10, eight sentences each, five a round; the clock stops while the rule shows. Every corrected sentence was read whole. Usage that careful writers now accept both ways (data is or are, singular they) was left out on purpose.
- They replace six quick fires: writing 3, 4 and 5 hold the builders; writing 7, 9 and 10 hold fix it. Quick fire: everything no longer belongs to any course and is gone. Kinds repeated inside a grade fall from 28 to 22 and the rules test's ceiling follows; kinds of game 14 to 16; quick fires 31 to 25.
- docs/GAMES-PLAN.md is now written by tools/games-plan.mjs from the app's own lists.
- Inactive Students: a soft line under each student row (ivory at 45 percent in the dark theme, the usual line color in the light), running from where the name begins to the end of Delete; the card's padding keeps it off the edges.

## 2026-09-25 (pass DF): the color mixer, a sleeker weekly note, tour fixes

- Mikey committed DE; this commit message starts fresh at DF.
- Color mixer (kind mix): MIX_TABLE holds what any two of eight pots make in the painter's red, yellow and blue wheel (28 pairs): primaries to secondaries, white to tints (pink, cream, light blue, peach, mint, lavender), black to shades (dark red, navy, olive, brown from orange, dark green, dark violet), black and white to gray, a primary and its neighboring secondary to the six intermediate colors, complements to brown, and the three secondary pairs to muddy mixes. mixOf names the result and the rule behind it, so a wrong mix still teaches. The secondary is called violet, the art term, and whenever violet is made the note adds that violet is the art word for purple. Decks: mix3 for art 3 (Looking and making: orange, green, violet, pink, light blue, gray from five pots) and mix4 for art 4 (Color, shape and story: dark red, navy, cream and the six intermediates from eight pots). Every target is made by exactly one pair of its deck's pots, tested. They replace the two art quick fires; repeats inside a grade 22 to 20, kinds 17, quick fires 23.
- Weekly note: WeeklyNoteBody lays the note out from its own words (so an edited note still reads): parts split at blank lines with a short dark green line between them, a heading line ending in a colon becomes a small uppercase heading with its items under it, each item's bold title on its own line and the rest smaller beneath, and Next up names the module large and bold. Layout and type, so both themes; the dark colors pass contrast on the light green body.
- Not started (the locked tag everywhere) is ivory at 10 percent in the dark theme, not a near-white pill. The tour card has a 2 px light green border in the dark theme; an older 1 px line later in the same style had been overriding the earlier border.
- Tour: the Student Summaries step rings every sample student's card (the first was left out). The Backup classroom step rings the whole backup line, as the Story Log step rings its line, and the link's own due-for-backup halo rests while the tour ring shows, so both steps look alike. The state picker on Create account is centered like the other one.

## 2026-09-25 (pass DG): debug the robot

- Mikey re-sent the DF request list word for word with Not committed. Everything on it had been delivered in DF, so nothing was redone; DF is not committed, so this commit message covers DF and DG (DE was committed, per his message before).
- Debug the robot (kind debug): a robot on a five-by-five grid runs a program, and one step is wrong. Arrows (tech 3, How computers think) move one square up, down, left or right; turns (tech 5, Programs that decide) go forward or turn left or right in place, starting from a stated heading. Run plays the program a step at a time and leaves a trail; tapping a step cycles it and sends the robot back to the start; home says which step was the bug and what it needed, and a bump says which step ran into a rock or the edge.
- The puzzles came from a search (a separate script, not the app's runner) that kept only programs that fail as written and that exactly one single-step change brings home without bumping; the rules test checks the same with the app's own runRobot, so two independent runners agree on all sixteen. Eight puzzles a deck, four a round.
- The two games join tech 3 and tech 5 as second games beside pairs-technology and pong, rather than replacing them: those are their courses' only other games, and neither is a quick fire. Kinds of game 18; quick fires 23; repeats 20.

## 2026-09-25 (pass DH): the tour's cards where Mikey wants them

- Mikey committed DF and DG; this commit message starts fresh at DH.
- New card places for the laptop tour, all measured from the live page each time: lesson-low (card 2: flush right with its top partway down the lesson's picture, so the question reads and the picture's lower part sits under the card), over-life-low (card 3: over Life Skills, its foot just above the backup line), over-exp-by-life (card 5: over Science Experiments beside Life Skills), over-exp-high (card 6: over Science Experiments up toward Reading), over-life-high (card 7: over Life Skills up toward Experiments), over-second moved 36 px up and 80 px right (card 8), and story-right (card 9).
- Card 9 was never on the Story Log page: it pointed at the classroom's Story Log link, which a new account without students does not have, so the card floated. It now opens the Story Log page with the tour's three sample students (sample storylog), rings the first student's block, and sits low and to the right so the title, the choices and the newest stories with their Open buttons stay readable. The tour card now renders on the Story Log page during that step, and ending the tour there returns to the classroom and drops the sample rows.
- The new places apply at 900 px and wider (the laptop tour); narrower screens keep the old rule of the screen's far end from the target.

## Open items
- Verify audio plays inside the chat artifact sandbox (works in a normal browser).
- Common Core / TEKS codes on existing modules to be checked against the official lists.
- (done 2026-09-04) Browser click-through tests: 22 checks in headless Chromium, run by check.sh.
