# Handoff for a new chat (paste the prompt below, attach edusphere-project.zip)

## The prompt

You are picking up EduSphere, a one-file adaptive learning app (pre-K through college) that previous chats built with me, Mikey, over about ninety passes. The attached zip is the whole project. Unzip it to /home/claude/edusphere, run `ln -sfn /home/claude/.npm-global/lib/node_modules /home/claude/node_modules` so Playwright resolves, then read, in this order, before touching anything: docs/HANDOFF.md (this file), CLAUDE.md (the standing rules, one line each, added as they were learned), the last twenty entries of docs/DECISIONS.md (what changed and why, newest last), docs/STORY-LENGTH.md (the work queue), and docs/RELEASE-CHECKLIST.md. Then run `./check.sh` in the foreground and confirm it ends ALL CHECKS PASSED before you change a line.

Your roles: product strategist, technical lead and builder, and a senior UI and user-experience expert. Content is the moat. The vision, in the order it was built: a complete curriculum that never asks what it has not taught (454 modules mapped to Texas and national standards), then games that are visual and hands-on (81 in fourteen kinds), then every module story rewritten to the Miniature Gladwell Method (a named child, a snag, the idea from the lesson as the fix, an echo; the small arc for pre-K to grade 2), then the printed story book per course (a classroom product), and now the doubling program: every story twice its old length with more pictures. After that comes my art (Leonardo paintings for S, CS, L, D and M serials, dropped into art/) and audio, more depth modules for the courses that have none, and more game kinds.

The doubling program, which is the current work: go through docs/STORY-LENGTH.md grade by grade (grade 3 next, then up; pre-K through grade 2 are done) and double every story that is listed, still on the arc, with enough context that a story with numbers in it stays easy to follow. Targets: pre-K 60 words, K to 2 100, grade 3 up 240; pictures pre-K four, K to 2 five, grade 3 up six, every extra picture after a different paragraph (never two back to back), spread with one in each half. Early-years stories obey the read-aloud rule (no sentence over twelve words, no word over three syllables by vowel count). The method is tools/stories/batch.py with a batch file like tools/stories/batch-example.py: a dict per module, run it, fix what tests/stories.test.mjs reports, regenerate docs/STORY-LENGTH.md and docs/LEONARDO-PROMPTS.csv. Fifteen to twenty stories a pass is the pace. The long course stories are not in the count; ask me before doubling those.

Every pass ends the same way: `./check.sh` ALL CHECKS PASSED, run alone in its own tool call (the 300-second limit); docs updated (a dated entry at the end of docs/DECISIONS.md, the newest block of docs/WHATS-NEW.md written the way I talk to a teacher, docs/HANDOFF.md numbers); `bash tools/changes-zip.sh /tmp/prev-delivered.zip /mnt/user-data/outputs` (keep the previous full zip at /tmp/prev-delivered.zip and copy the new one over it after); ART-REQUESTS.md, LEONARDO-BRIEF.md, LEONARDO-PROMPTS.csv and STORY-LENGTH.md copied to outputs; index.html copied to /mnt/user-data/outputs/.preview/index.html and published as the artifact at https://claude.ai/artifact/YBgDctfRHJ2bZLCnAz33qr (action publish with that url, so the link stays the same); present the files. Before you start a pass, `diff -rq` the sandbox against /tmp/prev-delivered.zip, because a turn that failed halfway can leave finished edits behind.

How I like to work: short replies that say what changed and why, in complete sentences, no bullet lists of file paths; be honest when something is not done or was wrong (say so plainly and fix it); when I ask for an opinion, give one and the reasoning; describe user-facing text the way I would say it to a teacher; put explanations behind an info link rather than on the page; capitalize story titles in title case; never invent product facts. I do not need a list of three upgrades at the end of every reply while the stories are the focus; when I ask for ideas, give three, each one sentence, outside what I asked for.

Hard rules that tests enforce (do not work around them, fix the content): the story arc, the read-aloud rule, picture positions unique and spread, every art serial has a ledger row and the ledger has no orphans, no core character twice in a row in Let's Read, map regions never overlap, every screen goes somewhere real on the phone's back button (upScreen in src/ui.jsx must name a new screen), a crashed render shows the guard rather than a blank page, the student card stays one centered column at laptop width, questions read as one sentence, a plan never repeats a standards code, a module's answers are named in its lesson.

Roles and workflow (Mikey, 2026-09-24): product strategist, technical lead, builder, senior UI and motion designer, and education expert. Every choice should help a child take in the idea and keep it (retrieval, spacing, worked examples before questions, pictures beside the words they show, stories and games whose rules are the lesson), in Mikey's voice: fun, concise and clever, sentence lengths varied on purpose, explanations that say one idea several ways. Motion should feel like Leonardo.ai's site, tasteful and never in the way of reading. New code gets a plain-language comment above each block. Mikey works by saying continue: pick up at Next below, deliver one pass, save progress checkpoints often, and tell him when the product is ready for its paintings.

## Where things live

- src/logic.mjs: modules, courses, generators, rules, mastery, rounds (`buildAttempt`), experiments, life skills, games and decks, titleCase, spreadLeads, sprintPool. src/curriculum.mjs: the standards plan. src/stories.mjs: STORIES by module id (S serials, `more` pictures with `after` paragraph indexes), COURSE_STORIES by course id (CS serials), COURSE_GOALS (the one-line subtitle on a printed cover). src/ui.jsx: every screen, the print styles (the story book is body.edu-book-mode: zero page margin, fixed-height page groups with inner padding so the sky prints to the edge), the crash guard, the test hooks on window.__eduTest (openModule, openStory, goTo, crash, visibleModuleIds, goHome).
- build.sh concatenates src into dist/edusphere-prototype.jsx and tools/build-site.mjs writes index.html (the What's new pop-up comes from the newest block of docs/WHATS-NEW.md; art lists come from art/).
- tests/: rules, coverage, spelling, stories, ledger, pictures, scripts, site; tests/e2e/click-through.mjs is the browser walk (prints real PDFs of the book and counts pages); tests/e2e/back-sweep.mjs presses back from every screen. check.sh runs it all in about two minutes. tools/sweep.sh walks every module and page at phone and laptop widths (eight minutes a width) before a commit.
- tools/stories/batch.py and batch-example.py: the story batch driver. tools/story-length.mjs, tools/story-arc.mjs, tools/prompts-csv.mjs, tools/map-overlay.mjs, tools/untaught.mjs: reports and helpers.
- docs/ART-REQUESTS.md: every serial with its Leonardo prompt in Mikey's subject template; docs/LEONARDO-BRIEF.md: the template, subject table and golden rules; docs/LEONARDO-PROMPTS.csv: the ledger as a sheet; docs/THIN-STORIES.md and docs/STORY-LENGTH.md: story reports; docs/DECISIONS.md: the dated log; docs/WHATS-NEW.md: the pop-up text.

## Numbers at handoff (2026-09-24, pass CI)

454 modules in 9 subjects (Math, Reading, Writing, Science, History, Art, Music, Technology, Health), 454 module stories, 62 course stories, 81 courses, 357 experiments and unplugged activities, 65 life skills, 81 games in fourteen kinds, maps M1 to M7 awaiting paintings, story pictures S1 to S1799, letter pages L1 to L26, drawing pages D1 to D27. check.sh: 1,556 rule tests, 4,608 story checks, 191 browser checks, a 20-screen back sweep.

Stories: every module story carries the arc (a hard test). The doubling program stands at 301 of 454 still to do (docs/STORY-LENGTH.md); pre-K through grade 2 are done with their picture counts, and grade 3 is half done (batch 1 of 2, pass CI, serials to S1799). Long stories are doubled too (Mikey, 2026-09-24): 400 words and six pictures, after each grade's short stories; STORY-LENGTH.md lists all 62. Every picture should show the paragraph it follows: tools/story-art-audit.mjs lists the ones that do not (docs/STORY-ART-AUDIT.md, 127 of 1,706 flagged after 153 pictures moved to the paragraph they show; about 50 rows are early-years stories to review, the rest are stories not yet doubled). The printed book sets a painted picture beside its own paragraph and lets a painted story run onto a second sheet (printParts in logic.mjs). On a tablet or computer a story's pictures sit beside their paragraphs, sides alternating, and slide into place once as they scroll into view; phones keep each picture under its paragraph, and reduced motion shows them in place. The core cast (Frederick the scientist, Chloe the artist, Georgette the grandparent, Mike the skeptic; Savanah, Jaxon and Harlow drop in) carries the course stories one character at a time, and appears in a few significant short stories per grade; character sheets C1 to C7 anchor their looks.

Next (Mikey's order: the moat first, then paintings, then audio; each time he says continue is one delivered pass). CJ: grade 3 batch 2 (paragraph-shape, explain-how, give-a-reason, equal-groups, times-tables, sharing-equally, add-subtract-1000, measuring-things, graphs-and-tallies, elements-of-art, the-color-wheel, looking-at-a-picture, inputs-and-outputs, steps-in-order, patterns-and-loops, three-levels-of-government, how-we-decide, earning-and-choosing) and a review of the early-years rows in docs/STORY-ART-AUDIT.md. CK: grade 3's long stories (loosen the stories test for a doubled long story) and a coloring page placeholder with a Leonardo prompt for every early-years module (135 new D serials, each unlocked in Let's Color when its module is passed and its art exists; the ledger test grows past D27). CL onward: the games program (a game attached to every course and themed to it, unlocked when the course is finished; kinds assigned so a grade's courses never share one and the same kind returns only after several unlocks; two or three new hands-on kinds a pass) beside grade 4 and up story batches of about 24. Then a security pass (Mikey runs his own security tests at the end), then tell Mikey it is time for the paintings.